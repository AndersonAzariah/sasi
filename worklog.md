# SASI — Worklog / Handover Document

Project: SASI — South African Service Intelligence
Stack: Next.js 16 (App Router, single `/` route, client-side state router), TypeScript, Tailwind 4, shadcn/ui, Framer Motion, Zustand, cmdk.
Design: BLACK/GREY/WHITE base + subtle South African accent light (chilli red #E53935, blue #42A5F5, green #43A047, gold #D4AF37). National colors are LIGHT, not paint.

## IMPORTANT ENVIRONMENT CONSTRAINTS
- Only `/` route is user-visible. ALL views are client-side state-routed via Zustand store (`useSasiStore.navigate(view, param)`). DO NOT create additional Next.js routes.
- Dev server on port 3000 (`bun run dev`, log: `/home/z/my-project/dev.log`).
- Logo asset: `/public/sasi-logo.png` (also favicon via `src/app/icon.png`).

---
Task ID: 1
Agent: main
Task: Inspect workspace, read phase specs (upload/*.txt), init worklog, plan build.

Work Log:
- Read both phase specification files (Phase 1 master prompt, Phase 2 product experience prompt).
- Inspected scaffold: Next.js 16 + shadcn/ui components + Prisma + Tailwind 4.
- Created todo plan and this worklog.

Stage Summary:
- Plan: Phase 1+2 combined build as single-route SPA. Foundation first (tokens/types/store/data/primitives/shell), then parallel view builds by subagents, then integration + browser QA + polish.

---
Task ID: 1 (complete)
Agent: main
Task: SASI foundation — tokens, types, store, demo data, primitives, shells, router.

Work Log:
- Rewrote globals.css: full SASI token system (bg #050505/#080808/#0B0B0C, surfaces 1-4, SA accent light variants: red #ef5350/#c62828, blue #64b5f6/#1976d2, green #66bb6a/#168a45, gold #e3c567/#d4af37), glow system (.sasi-glow-*), SASIPulse perimeter animation (@property --pulse-angle + reduced-motion fallback), shimmer/breathe AI indicators, sasi-card, rainbow hairline, grid bg, custom scrollbars, command focus glow, marker pulse.
- layout.tsx: Inter font, SASI metadata, icon=/sasi-logo.png (favicon from SASI.png), themeColor #050505, viewportFit cover.
- src/lib/sasi/types.ts: complete domain types (TrustStatus, CaseStatus, AIState, SasiCase, Incident, SasiSource, EvidenceItem, Finding, TimelineEvent, ProposedAction, VerificationState, AppNotification, ActivityEvent, AgentEvent, View union of 25 views).
- src/lib/sasi/utils.ts: SERVICES meta (11), TRUST/CASE/PRIORITY/CONFIDENCE/AI_STATE metadata (color+text always), date utils (timeAgo/formatTime/formatDate/formatDateTime), locationLabel, caseProgress, initials.
- src/lib/sasi/data.ts: coherent demo dataset, all isDemo:true, stable base date: DEMO_USER (Thabo Mokoena, Johannesburg), 4 CASES (CASE-000123 flagship water outage INVESTIGATING w/ proposed action + full events; CASE-000118 ACTION_REQUIRED; CASE-000104 WAITING; CASE-000097 RESOLVED+VERIFIED), 6 SOURCES, 9 EVIDENCE items, FINDINGS per case, 9 INCIDENTS (Gauteng w/ mapX/mapY for SVG map), 6 NOTIFICATIONS, 14 ACTIVITY events, GAUTENG_MUNICIPALITIES, SERVICE_REPORT_OPTIONS.
- src/lib/sasi/store.ts: Zustand store = client-side router (view/param/navigate), commandOpen, notifications w/ read state, mutable cases/findings/evidence, openCase/openIncident/openService, reportDraft + submitReport() (creates case), startInvestigationFor, setCaseAIState/addCaseEvent/addFinding/addEvidence, approveAction/rejectAction/setActionState/setVerification, savedLocation.
- src/components/sasi/primitives.tsx: SasiLogo (uses /sasi-logo.png), StatusBadge/CaseStatusBadge/PriorityBadge/ConfidenceBar, ServiceIcon+SERVICE_TINT (lucide mapping), AIStateChip, DemoBadge, SectionLabel/SectionHeader, EmptyState, StatTile, SasiPulse, CardSkeleton/ListSkeleton, PrimaryButton/GhostButton.
- src/components/sasi/domain.tsx: CaseCard, IncidentCard, SourceCard, FindingCard, EvidenceCard+EvidenceThumb (photos load /demo/{imageKey}.jpg w/ gradient fallback), TimelineRail, ActionApprovalCard (approve/edit/reject + "nothing submitted without approval"), VerificationCard (PENDING/IN_PROGRESS/VERIFIED/COULD_NOT_VERIFY), ActivityRow, NotificationRow.
- src/components/sasi/app-shell.tsx: desktop sidebar (sections OVERVIEW/INVESTIGATE/EVIDENCE/SYSTEM + Services + profile/admin), topbar (logo, command trigger w/ Cmd+K, location, notification dropdown, avatar), mobile bottom nav (Home/Investigate/Cases/Map/Profile), floating Ask SASI button, Cmd+K global listener.
- src/components/sasi/public-shell.tsx: marketing header (Services/How it works/About/Security + Sign in + "Tell SASI what is happening" CTA, mobile menu), sticky footer w/ rainbow hairline + independence statement + DEMO ENVIRONMENT tag. Footer is mt-auto (sticky bottom).
- src/components/sasi/command-palette.tsx: cmdk palette, groups ASK SASI/SERVICES/CASES/INCIDENTS/EVIDENCE/NAVIGATION, intelligent filtering, keyboard nav, ESC, pulse border.
- src/components/sasi/sasi-app.tsx: root — splash screen (SASI logo + ambient national light, 650ms), chrome split (public shell vs app shell), AnimatePresence view transitions, CommandPalette mounted globally.
- page.tsx renders <SasiApp/>. All 25 view files exist as placeholders in src/components/sasi/views/.
- Lint passes. Dev server restarted on :3000, GET / = 200.

Stage Summary:
- FOUNDATION COMPLETE. All views are placeholders awaiting parallel build.
- CONTRACT for view builders: import { useSasiStore } from "@/lib/sasi/store"; types "@/lib/sasi/types"; data "@/lib/sasi/data" (DEMO_USER, CASES, INCIDENTS, SOURCES, EVIDENCE, FINDINGS, NOTIFICATIONS, ACTIVITY, DEMO_NOW, evidenceForCase, sourcesForCase, POPULAR_SERVICES, GAUTENG_MUNICIPALITIES, SERVICE_REPORT_OPTIONS, DEMO_USER); utils "@/lib/sasi/utils" (SERVICES meta record, *_META maps, timeAgo, formatTime, formatDate, formatDateTime, locationLabel(loc,'suburb'|'city'|'full'), caseProgress); primitives "@/components/sasi/primitives"; domain "@/components/sasi/domain".
- Design language: black/grey/white only + SA accent as light; sasi-card class; 1px white/8 borders; rounded-lg/xl; text sizes 10-15px; never color-only status; DEMO badges required on data.
- Views must be 'use client', no next routes, mobile-first responsive, pb for mobile bottom nav handled by shell (main has pb-24).

---
Task ID: 2-b
Agent: full-stack-developer (dashboard + cases)
Task: Build the 5 core authenticated views — dashboard, cases list, case detail (tabbed command center), activity center, notifications — against the SASI store/primitives/domain contract.
Work Log:
- dashboard.tsx: greeting computed from DEMO_NOW hours (morning/afternoon/evening, "Good morning, Thabo."), sub uses savedLocation; GhostButton "Start an investigation"; command card (Search + "What do you need help with?" + ⌘K) wrapped in sasi-command-focus → setCommandOpen(true); 4 StatTiles (Active cases = status not RESOLVED/CLOSED, Investigating = aiState not IDLE/COMPLETE, Action required, Nearby incidents=INCIDENTS.length w/ "Demo dataset" hint; tones default/blue/gold/red); 12-col grid: left 8 = CURRENT CASES (store cases sorted updatedAt desc, sm:grid-cols-2 CaseCard → openCase(ref) + "View all cases" → cases), NEARBY (2 IncidentCards → openIncident(id)), CIVIC INTELLIGENCE (3 latest ACTIVITY ActivityRows + DemoBadge); right 4 = NEXT STEPS (SasiPulse gold — flagship case-123 proposedAction states: PROPOSED→"Approval needed"+PrimaryButton Review→navigate("case-detail","case-123"), APPROVED/IN_PROGRESS→"Action in progress", COMPLETED→"Action completed", else generic tips), SERVICE SHORTCUTS (water/electricity/roads/waste → openService), RECENT ACTIVITY (4 rows + link to activity).
- cases.tsx: header + DemoBadge + PrimaryButton "Report an issue"→report; segmented status tabs (All/Open/Investigating/Action required/Waiting/Resolved) with live counts, active = bg-white/8 text-white; native dark-styled FilterSelects for Service (SERVICES 11), Location (GAUTENG_MUNICIPALITIES, matches municipality OR city), Priority (PRIORITY_META); useMemo filtering sorted updatedAt desc; stats line "N cases · X investigating · Y need(s) approval" (investigating = aiState-active, same as dashboard tile); 300ms ListSkeleton on mount; empty → EmptyState(FolderLock, "No cases match these filters", "Clear filters" resets all); grid sm:2/lg:3 CaseCards → openCase(ref).
- case-detail.tsx: resolves case by param (ref or id) then activeCaseId; not-found EmptyState + back to cases. Header: back → cases, mono ref, title, location line (full + ward), badges (CaseStatusBadge/PriorityBadge/AIStateChip/DemoBadge), Share GhostButton w/ popover "Sharing is coming soon — demo", "Open investigation" (INVESTIGATING only) → startInvestigationFor. shadcn Tabs (restyled: bg-white/8 active triggers, scrollable list): Overview (summary+impact, VerificationCard, ActionApprovalCard when PROPOSED wired to store, facts card w/ ServiceIcon+icons+badges, caseProgress bar + status hint, "What SASI can and cannot do" can/cannot lists), Investigation (TimelineRail w/ live dot when aiState active + FindingCards from store.findings / "No findings yet"), Evidence (EvidenceCard grid w/ showCase + "Add evidence" → use-toast "coming soon"), Sources (SourceCards + "N sources · N official"), Actions (full ActionApprovalCard + VerificationCard + "Why approval is required" explainer), Activity (custom AuditRail w/ full formatDateTime timestamps + kind chips + agent names). Approve verify-loop simulation: approveAction → 2500ms setActionState IN_PROGRESS → 5000ms setActionState COMPLETED + setVerification IN_PROGRESS → 8000ms setVerification VERIFIED("Demo verification: utility notice updated (simulated).") + addCaseEvent(verification/Verification Agent) + setCaseAIState COMPLETE; timers in useRef, cleared on unmount (effect cleanup); double-approve guarded via busy state; toast feedback on approve/reject. Tab switches get subtle framer-motion fade (TabFade).
- activity.tsx: header + DemoBadge + "N EVENTS · DEMO RECORD" mono line; 7 filter chips (All/Cases/Evidence/Sources/Findings/Actions/Verification → ActivityKind mapping; INCIDENT_REPORTED visible in All); day grouping vs DEMO_NOW using UTC-day buckets (Today/Yesterday/Earlier) — deterministic + hydration-safe; left rail (border-l) with sticky-ish SectionLabel day headers (sticky top-14, bg blur); ActivityRow lists; empty filter → EmptyState(Inbox).
- notifications.tsx: header + DemoBadge + unread pill (gold, sasi-breathe when >0) + "Mark all read" (CheckCheck, disabled at 0); segmented filter All/Cases/Investigations/Actions/Updates w/ counts (SYSTEM grouped under Updates); sasi-card divide-y list of NotificationRow → markNotificationRead + openCase(caseRef) when present; EmptyState(BellOff, "You're all caught up."); subtle all-caught-up confirmation line when unread===0 with rows visible.
- Style: black/grey/white only, accents via existing badge/tint components; sasi-card, rounded-lg, 10–15px text, font-mono refs/timestamps/counters; aria-labels/aria-pressed/role=group on segmented controls; all icon buttons labeled; no horizontal overflow (truncate/min-w-0, flex-wrap toolbars, scrollable tablist w/ sasi-scroll); DemoBadge on every data section.
- Verified: bun run lint clean for all 5 files; tsc --noEmit clean for all 5 files; dev server GET / = 200.
Stage Summary:
- Files written: src/components/sasi/views/{dashboard,cases,case-detail,activity,notifications}.tsx (all 'use client', default export, root mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8).
- Decisions: demo clock pinned to DEMO_NOW (NOW_MS) for all timeAgo/progress calls in my views; UTC-day bucketing for activity day groups; "investigating" metric defined as aiState ∉ {IDLE, COMPLETE} consistently across dashboard tile + cases stats line; case resolution order = param first (openCase sets both, navigate("case-detail", id) sets param only) then activeCaseId; evidence tab uses evidenceForCase (static lookup) per contract; verify-loop keeps case status unchanged (spec did not require status transition); shadcn Tabs restyled via twMerge-safe class overrides.
- CONTRACT GAPS (for main/QA): (1) domain.tsx components (CaseCard/ActivityRow/IncidentCard/FindingCard/SourceCard/EvidenceCard/NotificationRow/TimelineRail) call timeAgo() with default now=Date.now(), but sandbox clock is Sep 2026 vs DEMO_NOW Feb 2026 → every relative time renders as "31w ago". Recommended one-line fix in src/lib/sasi/utils.ts: default `now` param to a DEMO-clock constant (e.g. export const DEMO_CLOCK_MS = new Date(DEMO_NOW).getTime() from data.ts and use it as timeAgo default). Not fixed by me — utils.ts/data.ts/domain.tsx are outside task 2-b scope. (2) /public/demo/*.jpg assets referenced by EvidenceThumb are missing (404s in dev.log) — gradient fallback works, but adding the 4 demo images (burst-pipe, dry-tap, leak-street, reservoir) would complete the premium look. (3) Lint errors exist in src/components/sasi/views/investigate.tsx (react-hooks/set-state-in-effect at lines 668/702) — not my task file, left for its owner.

---
Task ID: 2-a
Agent: frontend-styling-expert (landing + public)
Task: Build the six public views — landing (homepage), about, how-it-works, security, privacy, terms — replacing their placeholders, per the view contract (store/types/utils/data/primitives/domain).

Work Log:
- Read worklog + contract files (types, store, utils, data, primitives, domain, public-shell, sasi-app, globals.css) to lock exact APIs (navigate/setCommandOpen/openService/openIncident; SERVICES blurbs; POPULAR_SERVICES; INCIDENTS; SasiPulse/TimelineRail/IncidentCard/StatusBadge/ConfidenceBar/DemoBadge; sasi-grid-bg/sasi-ambient/sasi-command-focus/sasi-breathe/sasi-pulse-multi/sasi-rail classes).
- landing.tsx: 9 sections — hero (sasi-grid-bg + 3 ambient radial glows @~0.1 alpha, eyebrow chip with national-light dot, "Civic intelligence for South Africa." with blue→green→gold gradient on "South Africa", exact sub copy, both CTAs), command bar (sasi-command-focus wrapper + ⌘K kbd + 3 example prompts, all setCommandOpen(true)), trust strip (Independent/Transparent sources/Human control/Demo environment), popular services grid 2/4 (SERVICE_TINT icon box + bg-current dot, openService), "Live civic intelligence" panel (DemoBadge DEMO DATA + sasi-breathe "SASI is monitoring" chip + 3 latest IncidentCards by updatedAt → openIncident), 3-step How SASI works (01/02/03, MessageSquareQuote/Sparkles/ShieldCheck, whileInView once -80px stagger), investigation workflow preview (SasiPulse multi card + 5-event inline TimelineRail, DEMO badge, agent names, copy "never acting without your approval"), evidence & transparency trio (StatusBadge row CONFIRMED/REPORTED/INFERRED/UNVERIFIED, ConfidenceBar, ShieldCheck approval card), trust band ("Clear sources. Transparent findings. Human control." + How it works/Security buttons), latest civic information rows (title + ref + StatusBadge sm + timeAgo(DEMO_NOW clock) → openIncident).
- about.tsx: independence callout up top, 8 editorial sections (what/why/how-short/does/does-not/AI transparency/human control/independence statement) with Check (green) and X (red) icon lists, Scale icon independence block, bottom CTAs (report + security).
- how-it-works.tsx: agentic-loop strip (OBSERVE→…→REMEMBER mono chips + ChevronRight, wraps on mobile), 9-step vertical sasi-rail with mono numbers, tone-coded rail dots mirroring timeline language (user=white, ai=gold, verification=green, system=zinc), per-step icons, whileInView stagger, end CTA "Tell SASI what is happening" → report.
- security.tsx: 8 honest cards (Data protection/Authentication/Permissions/Audit trails/AI controls/Human approval/Evidence security/Responsible AI) using designed-to/planned language, gold-tinted "Honest limitations" callout (encryption at rest, RLS, SOC 2-style audits = planned, not claimed), demo-data disclaimer line.
- privacy.tsx: what we collect / what we never do (no selling, no public-model training without consent, no identity sharing) / your controls (export, delete evidence, delete case, clear saved location, manage AI memory, delete account — "Settings → Privacy" chip) / AI memory boundaries / contact (honest: no live channel in demo).
- terms.tsx: 5 anchored plain-language sections (as-is, no official status, evidence responsibility, AI informational not legal advice, acceptance) with anchor chip nav + scroll-mt-24, demo-draft stamp.
- QA via agent-browser on :3000: all 6 views render with correct headings/aria sections; landing has 9 sections, 1 h1, no horizontal overflow (scrollWidth <= innerWidth); command bar + example prompts open the palette.
- HOTFIX (out-of-scope, 1 line, documented): src/components/sasi/command-palette.tsx — `new Map()` inside the grouped useMemo resolved to the lucide-react `Map` ICON under Turbopack's concatenated module scope ("Map is not a constructor" on every mount, killing the globally-mounted palette and the landing hero's main interaction). Changed to `new globalThis.Map<string, ResultRow[]>()` with an explanatory comment. Verified in fresh browser session: zero page errors, palette opens cleanly from the landing command bar.
- Re-ran bun run lint (clean) and tsc --noEmit (zero errors in src/components/sasi; only pre-existing errors in sandbox examples/ and skills/ folders remain).

Stage Summary:
- Files written: src/components/sasi/views/{landing,about,how-it-works,security,privacy,terms}.tsx (+1-line hotfix in src/components/sasi/command-palette.tsx).
- Key decisions: deterministic demo clock (timeAgo uses DEMO_NOW) for landing rows; latest-3 incidents derived by updatedAt desc; landing keeps 90-95% monochrome with national light only in dots/glows/badges; animations are opacity/transform only, 0.35-0.45s, whileInView once; all sections aria-labelledby with matching heading ids; demo-derived data always carries DemoBadge; no fabricated stats anywhere.
- Contract gaps noticed: (1) the command-palette Map-icon shadowing crash (fixed, see above) — foundations task assumed palette worked; (2) palette NAVIGATION group has no "Landing/Home" row, so app-shell users can't reach public pages via ⌘K (suggest adding to navItems); (3) IncidentCard's internal timeAgo uses real Date.now(), which will drift from the demo clock — cosmetic only.

---
Task ID: 2-c
Agent: full-stack-developer (investigate + report)
Task: Build flagship views — investigate.tsx (Investigation Workspace), report.tsx (7-step report wizard), start-investigation.tsx (quick starter).

Work Log:
- investigate.tsx (flagship): DESKTOP 3-COLUMN grid-cols-12 (left sticky case-context card: mono ref, service icon+tint, title, location+ward, status/priority/AIState chips, DemoBadge, impact quote, "Sources X · Evidence Y" counts, "View full case" → openCase; center = AI timeline; right = Findings/Sources/Evidence scroll panels (max-h-96, sasi-scroll) + animated Overall-confidence mini-card from store findings). MOBILE: compact header (ref + AIStateChip) + segmented control Timeline/Findings/Sources/Evidence with live counts.
- Stage tracker: horizontal chips UNDERSTANDING→RESEARCHING→CORRELATING→VERIFYING→PREPARING FINDINGS with connecting hairlines; active = white chip + sasi-breathe dot, completed = check + dimmed, derived from case.aiState (WAITING_FOR_APPROVAL/ACTING/VERIFYING_RESULT/COMPLETE → all done; PAUSED keeps last scripted stage via trackerStage).
- Simulated agentic run (core experience): module-level SCRIPT of 7 idempotent steps (label-deduped events) — t0 UNDERSTANDING "Understanding the report" (Investigation Agent) → +3.5s RESEARCHING "Searching official and public sources" (Research Agent) → +7s "3 sources found" (source event) → +10.5s CORRELATING "Comparing sources against your evidence" (Evidence Agent) → +14s VERIFYING "Checking source consistency · 2 sources agree; 1 pending" → +17.5s PREPARING_FINDINGS + addFinding (only if case has none; id F-{ref}-new-{ts}, INFERRED/MEDIUM) → +21s WAITING_FOR_APPROVAL "Prepared a service report for your approval".
- Engine: all timer ids in refs, cleared on unmount/case-switch; every timer callback re-checks the case still exists via getState(); queueRef holds {stepIndex, fireAt}; Pause (clearTimeout + remaining saved + AIStateChip PAUSED) and Resume (re-schedule remaining, restore next step state) fully supported; run(fromStage) slices the script — auto-run on mount when aiState=UNDERSTANDING (deferred via setTimeout 0 → also survives strict-mode double-mount without duplicate events), "Run investigation" GhostButton for IDLE/RESEARCHING/PAUSED/mid-states, RESEARCHING (case-123) jumps straight to CORRELATING→onward (~10.5s).
- Approval gate: ActionApprovalCard rendered inline in the center column when proposedAction exists (wired to store approveAction/rejectAction); generic fallback ProposedAction derived at render (no effect) for cases without a stored action — approve still triggers the full sequence. Post-approval watcher effect (guarded by postKickRef) fires: ACTING/IN_PROGRESS → +2.5s "Action submitted through official public channel" (action event) + setActionState COMPLETED + setVerification IN_PROGRESS + VERIFYING_RESULT → +3s VERIFIED ("Demo verification: status page updated (simulated)") + "Verification completed" (verification event) + COMPLETE. VerificationCard shown from VERIFYING_RESULT onward.
- Live timeline: TimelineRail split-render — all but newest event static, newest wrapped in framer-motion (opacity+y, 0.25s) keyed by event id; live=true (breathe dot) while running. Findings animate in per-card.
- report.tsx: 7-step wizard (What/Where/When/Impact/Evidence/Review/Done) with animated stepper chips + white-on-white/10 progress bar. Draft persisted to store.reportDraft on every change (doneRef guard stops re-persist after submit). Step 1: 8 service icon chips + SERVICE_REPORT_OPTIONS water list (generic fallback for other services, "Other" reveals text input); Step 2: location input defaulting to saved location + quick chips incl "Use my saved location"; Step 3: radio cards Today/Yesterday/More than 2 days ago + native date input (color-scheme dark); Step 4: optional impact textarea; Step 5: honest dropzone ("File upload is coming soon. For the demo, add a note or link.") + note/link inputs → removable chips serialized into reportDraft.evidenceNote; Step 6: review dl with per-row Edit jumps + gold "SASI will not contact any authority without your explicit approval" card; Step 7: Submit to SASI (800ms spinner) → submitReport() → clearReportDraft() → Done screen (CheckCircle2, mono ref, "NOT yet filed with any government authority", Start investigation now → startInvestigationFor(newId), View case → openCase(ref), Back to dashboard). Validation blocks What/Where with inline #fda4a0 errors; Enter advances; min-h-11 touch targets.
- start-investigation.tsx: compact screen with Existing-case / Describe-new toggle; eligible radio list (OPEN/ACTION_REQUIRED/INVESTIGATING, ref+title+status tag); new-issue mode = description textarea + 8 service chips + location chips; Start investigation → existing: startInvestigationFor(id); new: setReportDraft→submitReport→startInvestigationFor(newId) (store auto-navigates). Explanation card with mono flow chips UNDERSTAND→…→ASK APPROVAL + independence note + link to full report flow.

Stage Summary:
- Files written: src/components/sasi/views/investigate.tsx, src/components/sasi/views/report.tsx, src/components/sasi/views/start-investigation.tsx. No other files touched.
- All simulation events dedupe by label → pausing/resuming/navigating away and back can never duplicate history; timers always cleared (no leaks).
- No fabricated official responses: all copy is demo-labelled or explicitly marked simulated; approval copy reuses data.ts/content semantics.
- bun run lint: PASS (fixed 2 react-hooks/set-state-in-effect errors by deferring auto-run kickoff and deriving the generic action during render). tsc --noEmit: only pre-existing errors in examples/ and skills/ (not mine). Dev server: GET / 200, no runtime errors in dev.log.

---
Task ID: 2-d
Agent: full-stack-developer (incidents + map + evidence) — completed via main-agent QA after context timeout
Task: Incidents explorer, incident detail, civic map, evidence vault.

Work Log:
- Files were fully written before the agent's context deadline: incidents.tsx (384 ln: stat tiles, search, list/dense toggle, service/status/severity/municipality filters, IncidentCard grid), incident-detail.tsx (351 ln: summary, details, update timeline, sources, GautengMiniMap side panel, related actions/cases), map.tsx (617 ln: stylized dark SVG Gauteng, exported reusable GautengMiniMap + MARKER_COLOR, city labels, status-colored markers w/ pulse ring, desktop floating filter/detail panels, mobile filter sheet + bottom card), evidence.tsx (720 ln: stats, search, type/case/verification filters, grid/list, preview dialog, add note/link wired to store.addEvidence).
- Main-agent QA fixes: (1) map markers were unclickable — transparent hit circle r=6.5 overlapped neighbouring markers (INC-0048 covered INC-0042); reduced capture radius to r=4 and set pointerEvents="none" on decorative halo/ring/dot circles. Verified clickable via elementFromPoint.

Stage Summary:
- All 4 views functional. Journey 6 (map → select incident → open detail) verified in browser at 390px.

---
Task ID: 2-e
Agent: full-stack-developer (services + settings + auth) — completed via main-agent QA after context timeout
Task: Services directory, service detail (water flagship), settings, profile, admin, login, signup.

Work Log:
- Files fully written before deadline: services.tsx (228 ln: hero, search, popular chips, 11 service cards with demo incident counts), service-detail.tsx (396 ln: common tasks, civic info stats, current incidents, evidence-that-helps checklist, official pathways, related cases), settings.tsx (693 ln: 9 sections incl. functional Accessibility controls and the AI permissions trust UI), profile.tsx (194 ln), admin.tsx (402 ln: moderation, case table, users, honest analytics "NO LIVE METRICS", source mgmt, AI run monitor, audit), login.tsx + signup.tsx (190/193 ln with demo hint + coming-soon SA ID).

Stage Summary:
- All 7 views functional, verified in browser at 1440px and 390px.

---
Task ID: 3
Agent: main
Task: Integration, cross-cutting fixes, browser QA, asset completion.

Work Log:
- Generated 4 demo evidence photos via z-ai SDK (scripts/gen-demo-images.ts): dry-tap, leak-street, burst-pipe, reservoir → /public/demo/*.jpg; EvidenceThumb renders them with DEMO chip.
- Fixed demo-clock drift: data.ts NOW changed from fixed 2026-02-10 to Date.now() at module load (safe: splash gate means nothing time-relative SSRs); relative times now read fresh ("13m ago").
- Command palette: added "Home / Landing" to NAVIGATION group (gap found by 2-a).
- Map marker click fix (see 2-d).
- Verified end-to-end in agent-browser: landing (hero/command/services/live intelligence), ⌘K palette incl. "water" query, dashboard (greeting/stats/cases/next steps), investigation workspace full agentic loop (Run investigation → stages → findings → approval → ACTING → VERIFYING RESULT → VERIFIED + AI: COMPLETE), report wizard 7 steps → "Reported to SASI" CASE-000124 → Start investigation now, mobile dashboard/bottom nav/floating Ask SASI, map marker selection + bottom sheet, services + water detail, evidence vault with photos, case detail tabs, settings AI permissions, incidents, activity, notifications, admin. No console errors. No horizontal overflow observed.
- Sticky footer verified: footer bottom == document bottom on long pages; mt-auto keeps it pinned on short pages.

Stage Summary:
- PHASE 1 + PHASE 2 COMPLETE (frontend). All 25 views implemented on the single-route SPA architecture. Lint clean, tsc clean in src/. Known limitations: no real backend/auth/persistence (by design — demo), accessibility settings partially cosmetic outside text-size/reduce-motion, sharing is labeled coming soon.

---
Task ID: 4
Agent: main
Task: Handover + scheduled webDevReview.

Work Log:
- Created cron job "SASI web dev review" (webDevReview, every 15 min) to continue autonomous QA/fix/feature loops.
- This worklog is the handover document.

Stage Summary:
- Recommended next-phase priorities: (1) add micro-interactions + page polish pass (spec §74 Phase 2: spacing/typography/motion audit), (2) implement real search behind the palette via API route, (3) persistence via Prisma/SQLite for cases+evidence, (4) LLM-backed "Ask SASI" free-text flow using z-ai-web-dev-sdk backend, (5) PWA manifest + offline shell, (6) i18n scaffolding.

---
Task ID: 5
Agent: main (autonomous QA + feature round)
Task: Assess project status, browser QA via agent-browser, then fix bugs / advance next-phase development.

Work Log:
- STATUS ASSESSMENT: lint clean, tsc clean in src/, no runtime errors in dev.log, all 25 views stable. Verified via agent-browser: landing renders (9 sections, no overflow), sign-in → dashboard works, evidence vault photos all load (6/6 naturalWidth>0), investigation agentic loop runs to the approval gate (all 7 scripted steps fire, WAITING FOR APPROVAL + approval card shown). The /demo/*.jpg 404s in older dev.log entries predate file creation — images serve 200 now (false alarm). Conclusion: phase stable → moved to new development, no bug fixes required.
- FLAGSHIP FEATURE — LLM-backed "Ask SASI" chat (view #26 of 26):
  - New API route src/app/api/sasi/ask/route.ts: backend-only z-ai-web-dev-sdk chat completion. System prompt encodes SASI persona (SA civic context, 10111/10177 emergency rule, never-contacts-authorities-without-approval, no fabricated confirmations) and injects compact live context: user's cases (ref/status/action state), tracked incidents, service directory, saved location. Accepts ≤10 message history; returns {reply, refs[]} (refs = CASE-xxxxxx/INC-xxxx detected in reply). runtime nodejs, force-dynamic. Verified: POST 200 in ~3s with grounded answers.
  - Store additions: chatMessages/chatBusy/pendingAsk/askSasi()/clearChat(). askSasi POSTs history + savedLocation to the API, shows "sending" placeholder bubble, honest error bubble on failure (state "error" → red-tinted AlertTriangle styling). No fabricated offline fallback.
  - New view src/components/sasi/views/ask-sasi.tsx: fixed-height flex layout (dvh with vh fallback; topbar/clearance aware so composer never hides behind the mobile bottom nav), sticky-bottom composer with gradient fade, markdown-lite renderer (**bold**, "- " bullets w/ gold dots, refs → clickable mono chips → openCase/openIncident), typing dots (framer-motion), busy hint line, copy-to-clipboard on assistant replies, clear-conversation button, saved-location chip, 6 staggered suggestion cards on empty state (water, load-shedding, case-status explainer, report pathway, capabilities, municipality directory), trust footer ("SASI can be wrong… Nothing is submitted without your approval"), aria log/live regions.
  - Entry points wired: floating "Ask SASI anything" mobile button now navigates to the chat (and hides while on it); mobile topbar sparkle → chat; desktop sidebar "Ask SASI" (Bot icon) under OVERVIEW; command palette free-form "Ask SASI about …" rows + "Why is my water off?" + "What should I do next?" now set pendingAsk and open the chat (auto-send consumed on mount via guarded effect); palette Navigation gains "Ask SASI chat"; dashboard command card is now a split control (palette ⌘K | gold Ask SASI shortcut).
- STYLING DETAIL PASS:
  - StatTile: mount count-up animation (easeOutCubic 650ms, rAF-only state updates to satisfy react-hooks/set-state-in-effect, prefers-reduced-motion respected, hydration-safe first paint = target) + tone light bloom on hover + tabular-nums. Affects dashboard/cases/incidents/evidence tiles.
  - .sasi-card-interactive: hover lift (-1px translateY + soft drop shadow + inset top light), :active press-down, :focus-visible white ring — global tactile upgrade for CaseCard/IncidentCard/SourceCard/service cards.
  - New .sasi-glow-soft (gentle gold halo) for assistant surfaces.
- PWA: public/manifest.json (name/short_name, standalone, #050505 theme/background, categories) + generated sasi-icon-192/512 + maskable 512 via sharp from the SASI logo; layout.tsx metadata.manifest + typed icon set (192/512/apple).
- BROWSER QA (agent-browser, 1440px + 390px): sidebar → chat; suggestion card send; LLM reply with INC-0041/INC-0042/CASE-000123 rendered as clickable chips + bold labels + gold bullets; copy button; multi-turn follow-up via composer Enter; clear conversation → empty state; palette Ctrl+K → "Why is my water off?" → auto-send → reply; mobile composer above bottom nav, floating button hidden on chat; zero console errors, zero page errors, zero horizontal overflow at both sizes; /manifest.json + icons serve 200.
- bun run lint PASS; tsc --noEmit PASS in src/ (only pre-existing examples//skills/ noise).

Stage Summary:
- ASK SASI IS LIVE: end-to-end LLM chat grounded in the demo dataset, wired into palette/sidebar/floating button/dashboard. Project now has a real AI backend surface (1 API route) while remaining single-route SPA.
- Files: +src/app/api/sasi/ask/route.ts, +src/components/sasi/views/ask-sasi.tsx, +public/manifest.json, +public/sasi-icon-{192,512,maskable-512}.png; edited types.ts (ChatMessage + ask-sasi view), store.ts (chat slice), sasi-app.tsx (registration), app-shell.tsx (entries), command-palette.tsx (ask routing + nav row), dashboard.tsx (split command card), primitives.tsx (StatTile count-up), globals.css (interactive hover/focus, glow-soft), layout.tsx (manifest+icons).
- Unresolved/risks: (1) LLM latency ~2-4s per answer — busy indicator covers it, but no streaming; streaming via SSE would feel faster if adopted later. (2) Assistant message refs stored but not yet surfaced as post-message action chips (inline chips cover it). (3) Chat history is in-memory only (Zustand) — lost on reload; persistence via Prisma or localStorage is the natural next step. (4) LLM may still cite near-accurate public numbers (e.g. utility phone lines) from general knowledge — acceptable for demo, worth a stronger disclaimer in prod.
- Recommended next-phase priorities: (1) persist chat + cases via Prisma/SQLite (survive reload), (2) SSE streaming for Ask SASI replies, (3) "draft a report from this conversation" action chip when a reply proposes one (closes the loop chat→report wizard), (4) i18n scaffolding (EN + SA official languages), (5) notification digests from investigation completions.

---
Task ID: 6
Agent: main (autonomous QA + persistence/streaming feature round)
Task: Assess status, agent-browser QA, then advance: Prisma persistence, SSE streaming, chat→report chips, toasts, styling details.

Work Log:
- STATUS: lint/tsc clean, all 26 views stable, zero console errors → phase was stable; moved straight to feature development (no bug fixes needed).
- PERSISTENCE LAYER (survives reload, keyed by anonymous browser sessionId in localStorage `sasi.sessionId`):
  - prisma/schema.prisma rewritten: CaseRecord (ref unique, payload=full SasiCase JSON), ChatMessage (sessionId+role+content+refs JSON), Profile (saved location JSON). Old User/Post demo models removed. `bun run db:push` OK (SQLite db/custom.db).
  - New API route src/app/api/sasi/state/route.ts: GET hydrate (cases+chat+location, corrupt rows skipped), POST (type:"case" upsert by ref | type:"location" upsert), DELETE ?scope=chat (clear). All best-effort — client never blocks on storage failure.
  - Store: getSessionId() helper in utils.ts; hydrate() action (double-invoke guarded, merges user cases by ref, restores chat only if in-memory chat empty, restores location); submitReport/startInvestigationFor/approveAction/rejectAction now persist case payloads fire-and-forget; new persistCaseById() called at investigation loop completion (investigate.tsx) so final VERIFIED/COMPLETE state survives reload; setSavedLocation debounced (600ms) persist; clearChat issues server DELETE.
  - Verified end-to-end: report CASE-000124 created → toast → full reload → case still on dashboard + case-detail renders + full agentic investigation loop ran on it; chat restored verbatim after reload (role-ordered, refs intact); clear → DB rows 0.
- SSE STREAMING for Ask SASI:
  - Confirmed z-ai-web-dev-sdk supports stream:true (returns ReadableStream of OpenAI-compatible SSE chunks; ends data:[DONE]).
  - /api/sasi/ask rewritten: stream mode (SSE: {type:"delta"} per token → {type:"done",refs,suggestReport} / {type:"error"}), upstreamDeltas() parser, user msg persisted at request start + assistant reply persisted after stream; non-stream JSON fallback preserved (stream:false); suggestReport heuristic broadened (reportish + actionable words like "want to / would you like / next step").
  - Store askSasi(): reads response body stream, patches assistant bubble incrementally (state sending→streaming→done/error), JSON fallback path kept; chat view: blinking gold streaming caret (.sasi-caret), auto-scroll follows stream growth (lastAssistantLen dep), busy hint switches to "answering live" once text arrives.
  - Verified: token-by-token rendering at 1440px and 390px, caret visible mid-stream, no console errors.
- CHAT→REPORT LOOP: new draftReportFromChat(messageId) — pre-fills report wizard (problem=last user message ≤160 chars, location from savedLocation) + navigates to report; assistant messages with actions.report render gold "Draft a report from this" chip (FilePlus2 icon); verified desktop + mobile (wizard step 1 shows the prefilled problem text).
- GLOBAL TOASTS (sonner): dark-styled Toaster mounted in sasi-app.tsx (bottom-right, zinc-950 panels, gold action button). Fired on: report created ("Report CASE-000124 created"), conversation cleared, action approved ("SASI is executing"), action rejected ("SASI paused"), chat→report prefill. Verified visually + in snapshots.
- STYLING DETAILS: .sasi-caret (blinking gold gradient block + glow, reduced-motion safe), .sasi-skeleton (shimmer sweep utility), .sasi-btn-sheen (diagonal light sweep on hover, skewX -18°) applied to PrimaryButton (all views), public shell CTA, landing hero CTA; green "Saved" sync chip (CloudCheck) in chat header (md+ only) signalling persistence; Prisma client log noise reduced to 'error'.
- QA: zero console/page errors on fresh loads (a stale console entry from a mid-edit state was verified stale and disappeared after reload); NO horizontal overflow at 1440px and 390px; DB row checks via node script (4 chat rows → 0 after clear; CASE-000124 with completion-state payload); dev.log healthy (only stale Fast Refresh warnings from mid-edit saves).
- bun run lint PASS; tsc --noEmit PASS in src/.

Stage Summary:
- SASI now has a REAL backend persistence layer: reports, full investigation state and chat history survive reloads; Ask SASI streams token-by-token with an honest error path; the chat→report wizard loop is closed with one click; all user actions get toast feedback.
- Files: prisma/schema.prisma (rewritten), +src/app/api/sasi/state/route.ts, src/app/api/sasi/ask/route.ts (SSE rewrite), src/lib/sasi/store.ts (hydrate/streaming/persist/toasts), src/lib/sasi/utils.ts (+getSessionId), src/lib/sasi/types.ts (streaming state + actions), src/components/sasi/sasi-app.tsx (hydrate + Toaster), views/ask-sasi.tsx (caret, chip, Saved, live hint, scroll-follow), views/investigate.tsx (persist on COMPLETE), views/landing.tsx + public-shell.tsx + primitives.tsx (sheen), globals.css (+caret/skeleton/sheen), src/lib/db.ts (log: error).
- Unresolved/risks: (1) DB grows unbounded (no TTL) — fine for demo, add pruning if reused; (2) sessionId is a privacy-lite key, not auth — anyone with the id can read that chat; documented, acceptable for demo; (3) suggestions heuristic may occasionally miss the report chip (heuristic, not classifier); (4) investigate mid-loop states (ACTING etc.) aren't persisted per-step — only completion; reload mid-loop restarts the loop.
- Recommended next-phase priorities: (1) i18n scaffolding (EN + an SA official language) — biggest untouched surface, (2) VLM: attach evidence photos in report wizard with real image understanding (z-ai SDK vision), (3) persist evidence items + findings like cases (same payload pattern), (4) notification digest view generated from completed investigations, (5) PWA offline shell via service worker.

---
Task ID: 7
Agent: main (autonomous QA + VLM evidence feature round)
Task: Assess status, agent-browser QA, then build: VLM photo analysis, evidence persistence, honesty/styling details.

Work Log:
- STATUS: lint/tsc clean, no browser errors, dev.log healthy → phase stable → feature round.
- FLAGSHIP — VLM EVIDENCE ANALYSIS ("SASI reads your photo", view flow #5 of the report wizard):
  - New API route src/app/api/sasi/vision/route.ts: POST {image: dataURL, context:{service,problem,location}} → z-ai-web-dev-sdk createVision (model intentionally omitted; backend assigns glm-5v-turbo — verified live). SA-civic system prompt returns STRICT JSON {what_i_see, service_guess, severity LOW| Medium|HIGH|CRITICAL, useful_for[], notable[], suggested_caption, quality_tip}; robust extraction (fence stripping, brace slicing), MIME allowlist (png/jpeg/webp), 5MB base64 cap, honest 502s. CLI-tested first with public/demo/burst-pipe.jpg — returned perfect structured JSON.
  - Client (report.tsx): the old "file upload coming soon" placeholder is now a REAL photo picker. Hidden file input → FileReader → canvas downscale (≤1280px, JPEG q0.82) → auto-POST to /api/sasi/vision. UI states: gold scanning skeleton while analysing ("SASI is reading the photo…"), analysis panel (severity pill colour-coded LOW zinc/MEDIUM gold/HIGH orange/CRITICAL red, "Reads like: water" with one-click "change report to X", useful_for gold bullets, notable line, "Better photo tip" callout, trust footer "AI-assisted read — not proof"), "Use as evidence note" (adds gold Photo chip to wizard + store.addEvidence → vault + toast "Photo filed as evidence", once-only guard) and "Re-analyse" + error retry path. EvidenceChip kind extended to "photo" (serialize/parse round-trip "Photo: caption").
  - EvidenceThumb honesty fix (domain.tsx): user photos (id EVD-user-*) previously fell back to a random demo street photo (misleading) — now render an elegant "YOUR PHOTO · AI-READ" camera placeholder on a national-light gradient; demo photos unchanged. EvidenceCard gets a gold AI-READ chip for user items.
- PERSISTENCE COMPLETION — evidence items survive reload:
  - Prisma EvidenceRecord model (evidenceId unique, payload JSON) + db:push.
  - /api/sasi/state: GET now returns evidence[] (corrupt rows skipped); POST type:"evidence" upserts by evidenceId.
  - Store: hydrate() merges user evidence (dedupe by id) + persists addEvidence fire-and-forget. Verified: evidence row in SQLite after "Use as evidence note"; AI-READ card still in vault after full reload.
- BUG FIXED — duplicate case refs after reload: caseCounter restarted at 124 each load, so a second report reused CASE-000124 (and the DB upsert silently overwrote the old row). hydrate() now bumps caseCounter past every restored ref/id (bumpCaseCounterFrom). Verified: reload → new report gets CASE-000125, both rows in DB.
- OPS: dev server had a STALE Prisma Client (evidenceRecord undefined → /api/sasi/state 500). Fixed by restarting dev server (supervisor did not auto-restart after kill; `setsid`/`nohup` children were reaped between tool calls — `(bun run dev &)` subshell survived). If the state route 500s with "Cannot read properties of undefined" after a schema change: restart the dev process.
- BROWSER QA (agent-browser, 1440px + 390px): full wizard run with real file upload (burst-pipe.jpg) → analysis panel accurate ("large black pipe discharging water… HIGH, reads like water"), filed as evidence, submitted → CASE-000125 toast → vault shows PHOTO/AI-READ/UNVERIFIED card with the VLM description → reload → card persists. Mobile: no horizontal overflow, zero console errors, chat history restored.
- bun run lint PASS; tsc --noEmit PASS (src/).

Stage Summary:
- SASI's report wizard now has real computer vision: attach a photo, SASI describes it, classifies the service, rates severity, suggests a caption and files it as AI-inferred evidence — with the photo itself never persisted (only the analysis). Evidence joins cases/chat/location in the reload-proof SQLite layer; ref-duplication after reload fixed.
- Files: +src/app/api/sasi/vision/route.ts, prisma/schema.prisma (+EvidenceRecord), src/app/api/sasi/state/route.ts (+evidence GET/POST), src/lib/sasi/store.ts (hydrate evidence + counter bump), src/lib/sasi/types.ts (+EvidenceAnalysis), views/report.tsx (photo picker + analysis panel + photo chips), domain.tsx (EvidenceThumb user placeholder + AI-READ chip).
- Unresolved/risks: (1) photo base64 goes to the vision backend for analysis — privacy note exists in UI copy ("never shared without approval" refers to authorities; worth a dedicated privacy line later); (2) analysis latency ~4-10s — skeleton covers it, no streaming for vision; (3) suggestReport/vision heuristics are prompt-based, no eval set; (4) dev server must be restarted after prisma generate (documented above).
- Recommended next-phase priorities: (1) i18n scaffolding (still the biggest untouched surface), (2) evidence vault "Analyse this photo" action reusing /api/sasi/vision on existing items, (3) notification digest from completed investigations, (4) PWA offline shell, (5) case-detail share/export (print-friendly case sheet).
---
Task ID: 8
Agent: main (autonomous QA + briefing/export/features round)
Task: Assess status, agent-browser QA, then advance: AI City briefing, evidence photo re-analysis, case sheet export (copy + print), styling details.

Work Log:
- STATUS ASSESSMENT: lint/tsc clean, dev.log healthy, zero console/page errors. agent-browser QA at 1440px: landing → sign-in → dashboard (persisted CASE-000124/125 restored), evidence vault (8 imgs, 0 broken), notifications, chat. Verdict: phase STABLE → feature round, no blocking bugs.
- FLAGSHIP 1 — AI "City briefing" (dashboard digest):
  - New API /api/sasi/briefing: LLM writes a STRICT-JSON daily digest {headline, risk CALM|ELEVATED|STRAINED|CRITICAL, sections[{title,body,refs}], watchlist[]} grounded ONLY in the dataset. First live run invented facts (property-tax amounts) → prompt hardened with "GROUNDING IS ABSOLUTE" rule; second issue: single assistant-role message → API error 1214 (messages invalid) → fixed by ending the message list with a user turn (same pattern as /api/sasi/ask).
  - Grounding upgrade: POST body now takes extraCases (user's live store cases) which the route merges over static CASES (dedupe by ref, 12 max) so user-created refs (CASE-000124/125) are real briefing inputs.
  - Store slice: briefing/briefingBusy/briefingError + generateBriefing({force}) + restoreBriefing(); sessionStorage cache `sasi.briefing` → verified 0 API calls on re-mount after reload, same headline restored.
  - New component briefing-card.tsx: gold-tinted card with national hairline, risk pill (Calm/Elevated/Strained/Critical + icon), headline, per-service sections, "Worth watching" box, trust footer ("Written by SASI's AI from your demo data… can be wrong… nothing shared"). Skeleton shimmer while writing, honest retryable error state. Ref chips clickable — with existence guard: unknown ref → sonner toast "Reference not in this demo record" instead of a dead "case not found" page. Mounted top of dashboard left column; auto-generates once per session.
- FLAGSHIP 2 — Evidence vault "Analyse this photo" (VLM on existing items):
  - New photo-analysis.tsx PhotoAnalysisPanel: fetches the bundled /demo/<imageKey>.jpg → canvas downscale ≤1280 → POST /api/sasi/vision → severity pill, what_i_see, useful_for bullets, notable, "File as evidence note" (addEvidence NOTE INFERRED + toast; once-only guard) + Re-analyse + quality tip. Idle/working(skeleton)/error states, resets on item switch.
  - HONESTY FIX: panel only mounts when item.type === "PHOTO" && imageKey — user photos (EVD-user-*, image never stored) previously fell back to analysing an unrelated demo image. Now they show no panel at all.
- FLAGSHIP 3 — Case sheet export (case-detail Share popover rebuilt):
  - "Copy case summary": plain-text brief (ref/status/location/dates/summary/impact/findings/sources+evidence counts) via clipboard + toast + 2.4s "Summary copied" state.
  - "Print / save as PDF": window.print() with a paper-styled print sheet — brand header w/ logo, CASE BRIEF · DEMO — NOT AN OFFICIAL DOCUMENT, meta table, summary/impact/findings(+confidence)/record timeline, honest footer. Rendered via createPortal(document.body); print CSS hides `body > *:not(.sasi-print-sheet)`. First attempt used the classic visibility technique → pages 2-3 leaked black blocks from app chrome; portal approach yields a clean SINGLE-page PDF (verified with agent-browser `pdf` export).
  - Share popover: Escape-to-close added (keyboard a11y); honest "Direct link sharing is not available in this demo" retained.
- SHARED RENDERER: extracted ask-sasi.tsx's markdown-lite into components/sasi/rich-text.tsx (Inline + RichText, optional onRef); chat + briefing + future AI surfaces share one renderer. Chat verified post-refactor (bold, bullets, INC chips, streaming, restored history).
- BUG FIXED — horizontal overflow on mobile evidence grid (390px): long single-line titles (e.g. the new "AI photo read — …" note) blew grid items to 598px because grid items lacked min-width:0, defeating `truncate`. Fixed with min-w-0 on EvidenceCard/CaseCard/IncidentCard roots (domain.tsx) + AnimatePresence motion.div wrappers (evidence.tsx, incidents.tsx). Re-verified: 12-view sweep at 390px all OK.
- STYLING DETAILS: .sasi-spin (reduced-motion safe) for the regenerate icon; print stylesheet + @page 14mm + paper types (.sasi-print-*); briefing card glow/hairline treatment; share popover menu styling.
- QA: /api/sasi/briefing curl-verified (grounded JSON), full wizard-free browser pass at 1440px + 390px: briefing generate/regenerate/cache-restore, evidence analyse→file→vault top card, copy summary (popover state), print PDF single page, chat RichText, 12-view mobile overflow sweep OK, zero console/page errors. bun run lint PASS; tsc --noEmit PASS (src/).

Stage Summary:
- Dashboard now opens with an AI-written, session-cached, ref-linked City briefing; the evidence vault can re-read any demo photo on demand; every case exports as a clean one-page PDF/copy brief. Project surface: 26 views + 4 API routes (state/ask/vision/briefing), all single-route SPA.
- Files: +src/app/api/sasi/briefing/route.ts, +src/components/sasi/briefing-card.tsx, +src/components/sasi/photo-analysis.tsx, +src/components/sasi/rich-text.tsx; edited src/app/globals.css (print system + spin), src/lib/sasi/store.ts (briefing slice + extraCases), src/lib/sasi/types.ts (CityBriefing/BriefingRisk/BriefingSection), views/dashboard.tsx, views/evidence.tsx, views/case-detail.tsx (share actions + portal print sheet), views/incidents.tsx (min-w-0), views/ask-sasi.tsx (shared renderer), domain.tsx (min-w-0 card roots).
- Unresolved/risks: (1) briefing refs can still cite refs outside the merged dataset — chips are guarded with an honest toast, but body text may mention them (prompt-level risk only); (2) briefing is per-session cached — location changes don't invalidate the cache (regenerate button covers it); (3) print sheet is only rendered while case-detail is mounted (by design); (4) vision/briefing latency unchanged (4-10s / 2-6s) — skeletons cover, no streaming for JSON endpoints.
- Recommended next-phase priorities: (1) i18n scaffolding (still untouched), (2) briefing auto-refresh + persisted history (DB) with "past briefings" list, (3) incident-detail share/print reusing the print-sheet system, (4) chat message → briefing cross-links ("ask SASI about this briefing section"), (5) PWA offline shell.

---
Task ID: 9
Agent: main (autonomous QA + briefing-history/auth/incident-export round)
Task: Assess status, agent-browser QA, then advance: auth shell continuity fix, briefing history (DB) + past briefings UI + staleness, briefing→chat cross-links, incident share/print, styling details.

Work Log:
- STATUS ASSESSMENT (in progress): lint PASS, dev.log healthy, agent-browser sweep 1440px: landing → sign-in → dashboard → cases/incidents/evidence/notifications/map/activity/settings/services all render, zero console errors, zero horizontal overflow. ONE UX BUG found: `services`/`service-detail` are in PUBLIC_VIEWS, so opening Services from the authenticated sidebar swaps AppShell → PublicShell (sidebar/notifications vanish, header shows "Sign in" to a signed-in user — feels like a logout). Phase otherwise stable → feature round.
- PLAN: (A) authed flag + shell continuity + sign out; (B) BriefingRecord in SQLite + Past-briefings UI + 6h staleness auto-refresh; (C) per-section "Ask SASI about this" chips; (D) incident-detail share/copy/print sheet; (E) styling details (popover entrance anim, history rows); (F) full QA + this handover.

Work Log (continued — completion):
- BUG FIXED — auth shell continuity: `services`/`service-detail` previously rendered in PublicShell even for a signed-in user (sidebar vanished, header showed "Sign in" — felt like a logout). Added `authed` flag to the store (persisted in localStorage `sasi.authed`, restored in hydrate), `signIn()` called by login+signup success paths, and APP_ELIGIBLE_PUBLIC set in sasi-app.tsx so those two views render inside AppShell when authed. PublicShell header/footer/mobile-menu now show "Go to dashboard" instead of "Sign in" when authed (verified with a restored session — no re-sign-in needed). Profile view gains a SESSION section with a proper danger-styled "Sign out" button (signOut → toast + navigate landing; saved data stays per the copy).
- FLAGSHIP 1 — Briefing history persisted in SQLite + "Past briefings" UI + staleness refresh:
  - Prisma BriefingRecord (sessionId, risk, headline, payload JSON; index [sessionId, createdAt]) + db:push + dev server restart (documented lesson: running process needs restart after prisma generate).
  - /api/sasi/state: GET returns briefings[] (8 newest, corrupt rows skipped); POST type:"briefing" appends (append-only history) and returns {ok,id}.
  - Store: briefingHistory[] (newest-first, deduped by generatedAt, capped 8) merged from GET and from every generate; every generated briefing fire-and-forget persists. Staleness: cached briefing older than 6h (BRIEFING_TTL_MS) no longer short-circuits — the card quietly rewrites on mount; on generation failure a stale cached briefing stays visible instead of an error panel.
  - briefing-card.tsx rebuilt: BriefingBody shared by today + reopened briefings; per-section "Ask SASI about this" chip; "Past briefings" collapsible (count badge, chevron rotate) with rows (risk dot, truncated headline, mono datetime · risk label) that reopen a past briefing; gold "Viewing a past briefing · {datetime}" banner with "Back to today".
- FLAGSHIP 2 — briefing → chat cross-link verified end-to-end: chip sets pendingAsk "About the \"Water\" part of my city briefing for {locationLabel}: what does this mean for me and what should I do next?" → chat auto-sends → grounded LLM reply citing CASE-000123. One chip per section, whisper-quiet until hover (sasi-chip-ask + .group/sec hover reveal).
- FLAGSHIP 3 — incident share/print: Share popover on incident-detail (case-brief pattern): "Copy incident summary" (clipboard plain-text brief + sonner toast + 2.4s "Summary copied" state), "Print / save as PDF" (portalled print sheet reusing the case print system: INCIDENT BRIEF · {ref} · DEMO — NOT AN OFFICIAL DOCUMENT, meta table incl. affected area, summary, recent updates, public sources list, honest footer). Escape closes; popover resets on incident switch (render-time adjustment pattern to satisfy react-hooks/set-state-in-effect). Verified via agent-browser pdf export: clean SINGLE-page PDF, no app chrome leak.
- STYLING DETAILS: .sasi-pop popover entrance animation (160ms scale-and-settle, reduced-motion safe) applied to share popover, briefing history list and topbar notifications drawer; .sasi-chip-ask gold hover treatment with section-hover reveal; history rows with risk dots + hover surface; danger-styled sign-out button; "Viewing past" banner in national gold light.
- QA: lint PASS; tsc --noEmit PASS (src/); zero console/page errors across the whole sweep; zero horizontal overflow at 1440px and 390px (dashboard, incident detail, profile); DB checks via node script (briefingRecord rows created per generation, keyed by sessionId; verified hydrate restores a DB briefing into history on a fresh load with empty sessionStorage — dedupe by generatedAt works); verified: services/service-detail stay in AppShell when authed (sidebar intact, no "Sign in" button), "Go to dashboard" from public shell with restored session, sign-out → landing + toast, past briefing open/back, ask chip → chat reply, incident copy + print PDF.
- QA note: clearing localStorage rotates sasi.sessionId (the storage key) — that is BY DESIGN; "no history" after wiping localStorage is correct behaviour, not a bug.

Stage Summary:
- Shipped this round: (1) auth shell continuity — the app no longer "logs you out" when browsing Services, plus a real sign-out; (2) AI briefings are now a durable history in SQLite with an in-card "Past briefings" browser, 6h staleness auto-refresh and stale-visible-on-error behaviour; (3) every briefing section cross-links into Ask SASI with a grounded pre-filled question; (4) incidents export as copy/PDF like cases.
- Files: prisma/schema.prisma (+BriefingRecord), src/app/api/sasi/state/route.ts (briefings GET/POST), src/lib/sasi/store.ts (authed slice, briefingHistory, BRIEFING_TTL_MS, persistBriefing, hydrate authed+briefings), src/components/sasi/sasi-app.tsx (APP_ELIGIBLE_PUBLIC), src/components/sasi/public-shell.tsx (authed header/footer/mobile), src/components/sasi/views/login.tsx + signup.tsx (signIn), views/profile.tsx (Session + sign out), views/incident-detail.tsx (share popover + print sheet), src/components/sasi/briefing-card.tsx (rewritten: history + ask chips + past view), src/components/sasi/app-shell.tsx (sasi-pop on notifications), src/app/globals.css (.sasi-pop, .sasi-chip-ask, reduced-motion).
- Unresolved/risks: (1) briefing history grows one row per generation (append-only, GET caps at 8, client caps at 8) — a TTL/pruning job is future work; (2) staleness check runs only on dashboard mount — a long-lived open tab won't self-refresh until remount (regenerate button covers it); (3) incident print sheet renders only while incident-detail is mounted (same by-design constraint as the case sheet); (4) authed is a demo-grade flag in localStorage, not real auth (documented); (5) "Ask SASI about this" question mentions section title only — if the LLM regenerated a different picture the reply may diverge from the exact past briefing being viewed (acceptable: chat is grounded in live context).
- Recommended next-phase priorities: (1) i18n scaffolding (EN + isiZulu/Afrikaans) — still the biggest untouched surface; (2) notification digest generated from completed investigations/briefings (notifications view is still static demo data); (3) chat message → briefing cross-link (reverse direction: "summarise this chat as a briefing"); (4) PWA offline shell via service worker; (5) map view: click-through from briefing watchlist pins to incident-detail.

---
Task ID: 10
Agent: main (autonomous QA + i18n/live-digest/map-focus round)
Task: Assess status, agent-browser QA, then advance: i18n scaffolding, live notification digest, briefing watchlist→map click-through, styling details; fix any bugs found.

Work Log:
- STATUS ASSESSMENT: lint/tsc clean, dev.log healthy, agent-browser sweep at 1440px (landing → sign-in-restore → all sidebar views) found ZERO console errors, ZERO overflow, session persistence intact → phase stable → feature round, no pre-existing bugs needing priority.
- FLAGSHIP 1 — i18n SCAFFOLDING (EN + isiZulu + Afrikaans, no more "coming soon" stub):
  - New src/lib/sasi/i18n.ts: flat key dictionary (~55 keys × 3 languages, en fallback), LANGUAGES + PLANNED_LANGUAGES meta with honest coverage notes, translate(lang,key) + useT() hook bound to store lang. Lang type lives in types.ts (avoids store↔i18n runtime cycle; i18n imports the store for the hook only).
  - Store: lang/setLang (persists localStorage `sasi.lang`, sets document.documentElement.lang; restored in hydrate).
  - Wired: app-shell (sidebar sections+items, admin row, topbar search placeholder + saved-location + notifications popover chrome, mobile bottom nav, floating Ask button), public-shell (nav links, header CTA, mobile menu, footer link columns), landing (hero headline a/b + sub, both CTAs, command-bar placeholder, 3 quick-prompt chips), settings (real working Select bound to the store: English/isiZulu/Afrikaans with per-language coverage notes; remaining SA languages listed honestly as disabled "coming soon"; "Honest coverage" explainer card + "Back to English" shortcut), command palette (Language group: "Switch to {lang}" rows for the two non-active languages).
  - VERIFIED live in browser: palette switch to isiZulu → landing hero "Ukwazi komphakathi eNingizimu Afrika", full sidebar (UHLOLOJIKELELE/Idashibodi/Buza i-SASI/UPHENYO/Phenya/Amacala/Izigameko/Imephu/UBUFAKAZI/Umsebenzi/ISISTIMU/Izaziso/Izilungiselelo/IZINSIZAKALO), html[lang]=zu; Afrikaans via palette ("Burgerlike intelligensie vir Suid-Afrika", Dashbord/Vra SASI/Ondersoek/Sake/Voorvalle/Kaart/Bewys/Aktiwiteit/STELSEL/Kennisgewings/Instellings/Dienste); switch back via Settings select (real UI path); RELOAD → language persists.
- FLAGSHIP 2 — LIVE NOTIFICATION DIGEST (notifications view is no longer static demo data):
  - Prisma NotificationRecord (notificationId unique, sessionId, read column, payload JSON) + db:push + dev-server restart (documented lesson applied).
  - /api/sasi/state: GET returns notifications[] (40 newest, read flag from column wins); POST type:"notification" (idempotent upsert by notificationId) and type:"notification-read" ({notificationId}|{all:true}); DELETE scope=notifications.
  - Store: pushNotification() (prepends via mergeNotifications — newest-first, dedupe by id, cap 30 — persists fire-and-forget), notifyInvestigationComplete(caseId). Events wired: report submitted (CASE), investigation COMPLETE (INVESTIGATION, fired from investigate.tsx after persistCaseById), action approved (ACTION, replaced the old inline set), briefing generated (UPDATE with risk label). markNotificationRead/markAllRead now sync read-state to the DB for live items.
  - Seed read memory: demo seed notifications' read flags persist via localStorage `sasi.ntfRead` (applied in hydrate) so "Mark all read" is sticky across reloads; live notifications persist read state in SQLite.
  - Notifications view: new "Today at a glance" digest strip at the top — LIVE EVENTS (count + latest timeAgo), YOUR REPORTS (user-created case count), CITY BRIEFING (risk pill + age, colour-coded), and an "Ask SASI · What changed today?" action cell that opens the chat with a grounded digest question. Live rows carry a mono gold LIVE tag (sasi-live-tag).
  - Briefing warm-up: hydrate() now calls generateBriefing() (TTL + sessionStorage guarded) so the digest has data on any view, not just after a dashboard visit. Verified.
  - VERIFIED end-to-end: regenerate briefing → badge 3→4 + "City briefing ready · Strained [LIVE]"; wizard report → "Report CASE-000124 received [LIVE]"; full agentic loop on CASE-000125 (start → WAITING_FOR_APPROVAL → approve → COMPLETE) → "Investigation complete · CASE-000125 [LIVE]"; RELOAD → all 4 live notifications restored from DB with correct unread state; mark-all-read → reload → 0 unread.
- FLAGSHIP 3 — BRIEFING WATCHLIST → MAP CLICK-THROUGH:
  - Store: mapFocusRef + focusOnMap(ref) (guards unknown refs with an honest "Not on the demo map" toast) + clearMapFocus().
  - briefing-card: watchlist rows that cite an INC ref (regex) render a quiet "View on map" chip (sasi-chip-map, blue accent, MapPin icon).
  - map view: derives focusIncident from mapFocusRef — focused marker is always visible (bypasses filters) and pre-selected; gold "INC-xxxx · From your city briefing [Clear]" banner (sasi-map-focus, breathe dot, backdrop blur) with aria-labelled Clear that returns to the user's filters.
  - VERIFIED in browser: focusOnMap('INC-0041') → banner + marker pre-selected + incident panel populated; unknown INC-9999 → guard toast; Clear → focus null + banner gone.
- BUG FIXED — cross-session case-ref hijacking: CaseRecord.ref was GLOBALLY unique while sessions are per-browser. A fresh browser session (empty DB for its sessionId) started its counter at 124 and the ref-upsert silently OVERWROTE the other session's CASE-000124 row. Fix: `@@unique([sessionId, ref])` + upsert by `sessionId_ref` compound key. Two browsers can now both own CASE-000124 independently. Verified: fresh session report creates its own row; old session's rows untouched; reload restores own-session cases and the counter bumps past them (second report = CASE-000125).
- Dev/QA hook: store exports itself on window.__sasiStore in non-production builds (stripped in prod) — lets agent-browser drive the store directly (used for map-focus + notification QA; kept for future rounds).
- STYLING DETAILS: sasi-live-tag (mono gold LIVE chip), sasi-chip-map (quiet→blue hover chip), sasi-map-focus (gold-tinted backdrop-blur pill with soft glow + breathing dot), notifications digest strip (hairline-gap 4-cell grid, collapses cleanly on mobile), briefing watchlist chips, settings language section layout.
- QA: bun run lint PASS; tsc --noEmit PASS (src/); zero console/page errors across the sweep; NO horizontal overflow at 1440px AND 390px across dashboard/cases/incidents/evidence/activity/map/settings/profile/services/ask-sasi/investigate/case-detail/incident-detail/landing/notifications; screenshots verified desktop dashboard + notifications digest + mobile notifications; dev.log healthy (all 200s).

Stage Summary:
- SASI now speaks three languages (EN/isiZulu/Afrikaans) across the shell, landing and shared controls with instant switching from the palette or Settings; the notification centre is a live, persisted digest of real app events (reports, investigation completions, approvals, briefings) with sticky read-state; and briefing watchlist items click through to a focused map marker. Plus a real cross-session data-integrity fix on case refs.
- Files: +src/lib/sasi/i18n.ts, prisma/schema.prisma (CaseRecord per-session uniqueness + NotificationRecord), src/app/api/sasi/state/route.ts (notifications GET/POST/DELETE + compound upsert), src/lib/sasi/store.ts (lang slice, pushNotification/notifyInvestigationComplete, mapFocusRef, seed-read memory, hydrate merges + briefing warm-up, __sasiStore dev hook), src/lib/sasi/types.ts (Lang, AppNotification.live), src/components/sasi/app-shell.tsx (i18n + t), public-shell.tsx (i18n), views/landing.tsx (i18n), views/settings.tsx (real language section), views/notifications.tsx (digest strip), views/map.tsx (focus banner + effective selection), views/investigate.tsx (completion notification), briefing-card.tsx (watchlist map chips), command-palette.tsx (Language group), domain.tsx (LIVE tag), globals.css (live-tag/chip-map/map-focus).
- Unresolved/risks: (1) i18n coverage is shell/landing/shared-controls only — individual views remain English (documented honestly in Settings); isiZulu/Afrikaans strings are hand-written, not proofread by a native speaker. (2) Briefing warm-up at hydrate fires an LLM call when the session cache is stale/empty (2-6s, off the critical path) — a first-load cost worth watching. (3) The DB grows unbounded for notifications (GET caps at 40, client at 30; no TTL job). (4) Briefing watchlist map chips only appear when the LLM cites an INC ref — CASE-only watchlists get none (honest; refs inside RichText remain clickable). (5) window.__sasiStore exists in dev builds only — QA convenience, stripped in prod.
- Recommended next-phase priorities: (1) extend i18n coverage to dashboard + cases + notifications views (dictionary is ready — views just need t() calls); (2) chat → briefing reverse link ("summarise this chat as a briefing" chip) — closes the loop both ways; (3) notification preferences from Settings actually filtering which events push (ntfCase/ntfInvestigation/ntfAction toggles are still visual-only); (4) PWA offline shell via service worker; (5) evidence items in the map/skip-to-case flows (user cases have no mapX/mapY — could place at saved location with a jitter).

---
Task ID: next-phase (post-i18n/digest/map-focus round)
Agent: main
Task: Assess project status + agent-browser QA → project was stable, so advance the previous round's recommended priorities: (A) notification preferences become real, (B) chat → briefing reverse link, (C) i18n coverage for dashboard/cases/notifications, plus a mandatory styling-details pass.

Work Log:
- STATUS ASSESSMENT: read worklog tail + dev.log (all 200s), swept landing/dashboard + 11 views with agent-browser — zero console errors, zero horizontal overflow at 1280px and 390px. No bugs found; proceeded to feature work per the handover's recommended priorities.
- FEATURE A — REAL NOTIFICATION PREFERENCES:
  - types.ts: `NtfPrefs` { case, investigation, action, service }; store: `ntfPrefs` + `setNtfPref()` persisted to localStorage `sasi.ntfPrefs`, restored in hydrate.
  - `pushNotification` now GATES on prefs by kind: CASE→case, INVESTIGATION→investigation, ACTION→action, SYSTEM→service; UPDATE (briefing heartbeat) always passes — documented honestly in the Settings footnote.
  - NEW EVENT TYPE — service alerts: URGENT/CONFIRMED demo incidents near the saved location raise ONE quiet SYSTEM digest notification. Seen-set in localStorage `sasi.svcSeen` prevents re-nagging across reloads. Because demo data is static, toggling service ON also surfaces pending incidents immediately (instant, honest feedback instead of waiting for a future incident that can never come).
  - Settings → Notifications: the four toggles were previously visual-only useState — now bound to the store; added per-row identity icons (FileText/FolderSearch/Stamp/Siren) in gold-tinted tiles, a live "N OF 4 CHANNELS ON" counter (aria-live, breathing dot), "NEW" tag on Service alerts, "Recommended: keep on" subnote on approvals, and a gold confirm-flash micro-interaction on every flip (event-driven, no setState-in-effect — lint-clean).
  - VERIFIED end-to-end: case=off → real submitReport → NO CASE notification (count unchanged); case=on → submit → "Report CASE-000125 received" pushed; service=on → "Service alert · 3 confirmed incidents near Johannesburg" pushed instantly; reload → prefs restored, live notifications + user cases restored from SQLite.
- FEATURE B — CHAT → BRIEFING REVERSE LINK:
  - /api/sasi/briefing accepts `origin:"chat"` + `transcript[]`; new CHAT_SYSTEM_PROMPT distils the Ask SASI conversation into the same strict briefing JSON (grounding rules preserved: no invented facts, only real refs, 1-2 focused sections, unarmed voice).
  - store: `briefingFromChat()` — requires ≥1 user ask + 1 done assistant reply, POSTs last 12 done messages, replaces the dashboard briefing (state + sessionStorage + SQLite history), pushes "Briefing written from your conversation" UPDATE notification, navigates to dashboard with toast; honest guard-toast when there's nothing to summarise.
  - ask-sasi.tsx: two entry points — a header chip "Summarise as briefing" (spinner while busy, disabled with explanatory title before any answer exists) and a quiet pill above the composer "Summarise this chat as a briefing ⌃".
  - briefing-card.tsx: blue `FROM YOUR CHAT` origin tag (sasi-origin-chat) replaces "DAILY · DEMO" when origin==="chat"; new risk meter bar (sasi-risk-track: recessed track, per-risk gradient fill 25/50/75/100%, animated sheen, reduced-motion safe, role="img" label).
  - VERIFIED end-to-end with the real LLM: asked about the water outage → answer streamed with CASE-000123/INC-0041 refs → clicked distil → auto-navigated to dashboard → card shows FROM YOUR CHAT + gold Elevated meter + headline "Your water outage report (CASE-000123) is being investigated." with 1 focused section (prompt correctly refused to pad). dev.log: POST /api/sasi/briefing 200 in 2.8s.
- FEATURE C — i18n: ~50 new dictionary keys (dash.*, cases.*, ntf.*) EN/isiZulu/Afrikaans; wired t() through dashboard (greeting key switch, stat tiles, section labels, next-steps variants incl. all three action states, TIPS as keys), cases (title/subtitle/CTA/status tabs/filter options/empty state), notifications (title/subtitle/unread/mark-all/digest strip cells/filters/empty/caught-up). LANGUAGES coverage notes updated honestly. VERIFIED in isiZulu via screenshots: "Sawubona (ebusuku), Thabo", "Amacala/Konke/Kuvuliwe/Kuyaphenanywa…", "Izaziso/IZEHLAKALO EZIPHILAYO/Kwaguquka ngani namuhla?" all render.
- STYLING DETAILS: sasi-chip-brief (blue glow hover), sasi-risk-track + sasi-risk-sheen keyframes, sasi-origin-chat inner glow, sasi-pref-flash row confirm animation, gold icon tiles in Settings, hover lift on toggle rows — all reduced-motion guarded.
- QA: bun run lint PASS; tsc --noEmit PASS for src/ (examples/+skills/ pre-existing errors unchanged); zero console errors; zero horizontal overflow at 390px across dashboard/cases/notifications/settings/ask-sasi/map/incidents; dev.log healthy. One mid-round crash caught and fixed: dashboard h1 briefly referenced renamed greeting fn during a partially-applied MultiEdit — resolved before QA.

Stage Summary:
- The three visual-only notification toggles are now a real preference system (gating + persistence + a brand-new service-alert event family), Ask SASI conversations can be pinned to the dashboard as grounded briefings (closing the chat↔briefing loop in both directions), and the three highest-traffic views speak isiZulu and Afrikaans. Plus a detail pass: risk meter, chat-origin tag, per-row icons, confirm-flash, channel counter.
- Files: src/lib/sasi/types.ts (NtfPrefs, CityBriefing.origin), src/lib/sasi/store.ts (ntfPrefs slice + gating, service-alert generator in setNtfPref + hydrate, briefingFromChat), src/lib/sasi/i18n.ts (+~50 keys, coverage notes), src/app/api/sasi/briefing/route.ts (transcript/chat mode), src/components/sasi/views/settings.tsx (store-bound toggles, icons, counter, flash), src/components/sasi/views/ask-sasi.tsx (distil entries), src/components/sasi/views/dashboard.tsx + cases.tsx + notifications.tsx (i18n), src/components/sasi/briefing-card.tsx (origin tag, risk meter), src/app/globals.css (4 new classes + keyframes).
- Unresolved/risks: (1) i18n: case-detail/report/incidents/map view bodies remain English (honest in Settings); isiZulu/Afrikaans strings hand-written, not native-proofread; the cases stats line ("4 cases · 2 investigating…") is intentionally untranslated mono metadata. (2) Service alerts fire once per incident per browser (seen-set) — a forced re-fire requires clearing `sasi.svcSeen`; acceptable for demo but a TTL would be nicer. (3) briefingFromChat replaces "today's" briefing silently for users who don't read toasts — the FROM YOUR CHAT tag mitigates; a "back to city mode" shortcut could help. (4) Notification POSTs are fire-and-forget: an immediate reload can cancel the persist fetch (observed once during QA; in-memory state stayed correct, DB missed one row — cosmetic, self-heals on next event). (5) DB notification growth still unbounded (GET caps 40, client 30).
- Recommended next-phase priorities: (1) notification preferences UI for the digest heartbeat (let users quiet UPDATE notifications too, default on); (2) extend i18n to incidents + map views (largest remaining surfaces); (3) "back to city briefing" shortcut when viewing a chat-distilled briefing; (4) PWA offline shell via service worker (carried over); (5) user cases on the map (place at saved location with jitter — carried over).

---
Task ID: next-phase+1 (quiet-digest / city-mode / user-reports-on-map round)
Agent: main (autonomous QA + feature round)
Task: Assess status + agent-browser QA → project stable, so advance the handover's recommended priorities: (A) briefing-digest notification preference becomes real, (B) "City mode" back-to-city-briefing shortcut, (C) user cases on the map, (D) i18n for incidents + map views — plus the mandatory styling-details pass.

Work Log:
- STATUS ASSESSMENT: worklog tail read; lint PASS; tsc --noEmit PASS (src/); dev.log all 200s; agent-browser sweep (landing + 10 views) — ZERO console errors, zero horizontal overflow at 1280px & 390px → stable, feature round.
- FEATURE A — BRIEFING DIGEST IS NOW A REAL PREFERENCE (5th channel):
  - NtfPrefs + `digest` (default true); prefGatesKind UPDATE → prefs.digest (previously hard-wired `return true`); setNtfPref label map extended.
  - Settings → Notifications: 5th ToggleRow "Briefing digest" (Newspaper icon) with honest subnote "the briefing itself still lands on the dashboard, only the alert is quieted"; counter now "N OF 5 CHANNELS ON"; footnote rewritten (every family can be quieted without losing dashboard data).
  - VERIFIED via store: digest off → pushNotification(UPDATE) is gated (count unchanged); toggle back on restores. Counter + row + subnote render (screenshot).
- FEATURE B — "CITY MODE" BACK-TO-CITY SHORTCUT (closes the chat↔briefing loop both ways):
  - store: `cityBriefingBackup` + `restoreCityBriefing()` — briefingFromChat now stashes the displaced daily briefing (only when origin≠chat); restore swaps it back for FREE (state + sessionStorage), toast confirms; fallback when no backup (e.g. reload): honest "Rewriting your city briefing…" toast + generateBriefing({force:true}).
  - briefing-card: gold `City mode` chip (sasi-chip-city) next to the risk pill whenever briefing.origin==="chat" && !viewingPast; chat toast copy now points at City mode.
  - VERIFIED: simulated chat-origin briefing + backup → chip shows (screenshot: FROM YOUR CHAT + City mode) → click → daily briefing restored, origin tag gone, DAILY·DEMO back. Fallback path tested against the real LLM: fresh grounded briefing written (2.1s, dev.log 200).
- FEATURE C — USER REPORTS ON THE MAP (carried-over priority, now done):
  - map.tsx: `CITY_COORDS` (derived from the label table), deterministic per-ref jitter (`jitterFrom`), `placeAtCity(city, seed)` — user cases (id `case-new-*`) render as gold markers near their saved city (unknown city → Johannesburg fallback); always visible, filter-independent.
  - New `SelectedCaseSummary` panel: gold service tile, YOUR REPORT live-tag, CaseStatusBadge + PriorityBadge, location + "Filed {timeAgo}", honest "placed near your saved location" note, Open case button; wired into both desktop panel and mobile bottom card.
  - `focusOnMap` now accepts CASE refs (checks the user's cases); map focus resolution handles incident OR case; banner suffix reads "Your report" for cases vs "From your city briefing" for incidents. Unknown CASE-9999 → guard toast (verified).
  - "You are here" home marker: dashed gold halo at the saved location (sasi-home-ring, 36s rotation, reduced-motion safe, pointer-events none) + label; legend gains a gold "Your reports" entry in all three render sites.
  - VERIFIED: submitted report via wizard path (CASE-000126) → gold marker appears near Johannesburg; focusOnMap('CASE-000126') → banner + pre-selected marker + case panel (screenshot); Open case navigates to case-detail.
- FEATURE D — i18n: incidents + map views fully wired (~80 new keys: inc.*, map.*, shared status.*, sev.*) in EN/isiZulu/Afrikaans; {n}/{shown}/{total}/{t} placeholder interpolation via .replace; legend labels now keyed too. VERIFIED live in isiZulu: incidents ("Izigameko zomphakathi", chips "Kuqinisekisiwe/Okuphuthumayo", count "Izingameko ezingu-9") and map ("IZIHLUNGI ZEMEPHU", "Sesha izigameko…", "Ulapha", "Imibiko yakho") — screenshots. LANGUAGES coverage notes + header comment updated honestly.
- STYLING DETAILS: sasi-chip-city (quiet gold → warm glow hover, focus ring), sasi-home-ring slow-rotating dashed halo (reduced-motion guarded), gold inner-core on user-case markers (reads distinct from incidents at a glance), gold legend accents, YOUR REPORT live-tag reuse — all consistent with the existing national-light token system.
- QA: bun run lint PASS; tsc --noEmit PASS; zero console errors; zero horizontal overflow at 1280px (13 views) AND 390px (10 views); digest gate verified end-to-end; dev.log healthy (POST /api/sasi/briefing 200 in 2.1s).

Stage Summary:
- All five notification channels are now user-controlled (the digest heartbeat can be quieted, honestly documented); a chat-distilled briefing can be swapped back to the daily digest in one click (free restore, honest LLM fallback); the user's own reports finally appear on the stylised Gauteng map as gold markers near their saved location with click-through focus from briefing/chat refs; and the two largest remaining English views now speak isiZulu and Afrikaans.
- Files: src/lib/sasi/types.ts (NtfPrefs.digest), src/lib/sasi/store.ts (digest gate + labels, cityBriefingBackup/restoreCityBriefing, CASE-aware focusOnMap), src/lib/sasi/i18n.ts (+~80 keys, coverage notes), src/components/sasi/views/settings.tsx (5th toggle, counter, footnote), src/components/sasi/briefing-card.tsx (City mode chip), src/components/sasi/views/map.tsx (user-case markers, home marker, SelectedCaseSummary, CASE focus, full i18n), src/components/sasi/views/incidents.tsx (full i18n), src/app/globals.css (sasi-chip-city, sasi-home-ring).
- Unresolved/risks: (1) isiZulu/Afrikaans strings remain hand-written (not native-proofread); case-detail/report/incident-detail view bodies still English (documented in Settings). (2) User-case map placement uses city-level coords + jitter — suburb-level precision is impossible on a stylised map (the panel says so honestly). (3) City-mode fallback without a backup costs one LLM call (unavoidable; pre-announced via toast). (4) Notification POSTs remain fire-and-forget (one-row loss possible on instant reload; self-heals). (5) QA-session test data (CASE-000126) lives in the dev DB for that browser session only — harmless.
- Recommended next-phase priorities: (1) PWA offline shell via service worker (carried over twice — biggest untouched surface); (2) notification → map click-through (caseRef notifications could offer "View on map" now that CASE refs focus); (3) i18n for case-detail + incident-detail (the remaining high-traffic surfaces); (4) briefing watchlist chips for CASE refs (backend already grounds them; only the regex in briefing-card keys on INC-); (5) per-view i18n coverage self-check in Settings (show per-view translated % honestly).

---
Task ID: next-phase+2 (PWA offline shell / notification→map / i18n detail views / CASE watchlist chips round)
Agent: main (autonomous QA + feature round)
Task: Assess status + agent-browser QA → stable, so advance the handover's carried-over priorities: (A) PWA offline shell via service worker [carried over 3×], (B) notification → map click-through, (C) i18n for case-detail + incident-detail, (D) briefing watchlist chips for CASE refs — plus the mandatory styling-details pass.

Work Log:
- STATUS ASSESSMENT: worklog tail read (395 lines); lint PASS; tsc PASS; dev.log all 200s; agent-browser sweep (landing + dashboard/map/cases/incidents) — zero console errors, zero overflow at 1280px. One alarm investigated and cleared: console showed a store.ts:318 parse error that proved STALE (mid-edit HMR artifact from a previous session; file on disk was already correct; fresh reload compiled clean). → stable, feature round.
- FEATURE A — PWA OFFLINE SHELL (the biggest untouched surface, now done):
  - public/sw.js (new): versioned caches (sasi-shell-v3 / sasi-static-v3); install precaches "/" + manifest + icons individually (one 404 can't fail install) and skipWaiting; activate purges old versions + clients.claim(); fetch strategy: /api/* NEVER cached (data must be live or honestly failed; POSTs always hit network), navigations network-first→cache→offline-fallback HTML page, /_next/static + icons cache-first in prod, EVERYTHING network-first in dev (registered as /sw.js?mode=dev so HMR never serves stale chunks).
  - src/lib/sasi/pwa-store.ts (new): tiny standalone Zustand store { online, swPhase(unsupported/registering/ready/failed), installable, installed, updateReady } + dev-only window.__sasiPwa hook (same pattern as __sasiStore, stripped in prod).
  - src/components/sasi/pwa.tsx (new): PwaRuntime mounts once in SasiApp — registers the SW, mirrors navigator.onLine, captures beforeinstallprompt (browser mini-infobar suppressed in favour of SASI's own affordance), detects appinstalled + standalone display-mode, detects waiting worker → updateReady; exports promptPwaInstall() / applyPwaUpdate(); renders the quiet bottom-left OFFLINE chip (amber, backdrop-blur, safe-area aware) via AnimatePresence.
  - sasi-app.tsx: mounts <PwaRuntime/>; updateReady → sticky toast "A new version of SASI is ready" with Reload action (skipWaiting + reload).
  - Settings → NEW "App & offline" section (Smartphone nav item between Security and Data): live status rows — Connection (Online/Offline with breathing amber dot when offline), Installed as an app, Offline copy (service worker) with honest per-phase copy, Install SASI button (installable only; honest guidance toast when the browser offers no prompt), Update ready + Reload row (updateReady only); plus a "What works offline — and what waits" green/amber two-card explainer (AI/report/submissions need the network — stated honestly).
  - manifest.json: +launch_handler navigate-existing, +3 shortcuts (Report an issue / City briefing / Ask SASI) with 192px icons.
  - Deep links: /?view=<name> honoured once at hydrate (validated against a new ALL_VIEWS list in store; unknown → ignored; app view while signed out → lands on login honestly; query string cleaned via history.replaceState so refresh doesn't re-trigger).
  - VERIFIED: SW registered and controlling page; caches populated (sasi-shell-v3: 6, sasi-static-v3: 39); /?view=report jumped to report view and cleaned the URL; offline chip appears/disappears on store toggle (screenshot); Settings row flips live Online↔Offline; "App & offline" section renders fully (screenshot).
- FEATURE B — NOTIFICATION → MAP CLICK-THROUGH:
  - domain.tsx NotificationRow: new optional onViewOnMap prop → gold "View on map" chip (sasi-chip-map-gold) in a hover/focus-revealed action strip (sasi-ntf-actions; always visible on touch via @media(hover:none); stopPropagation + keyboard operable so it never triggers the row's open-case click).
  - notifications.tsx: chip appears when the notification's caseRef resolves against the user's cases; click marks read + focusOnMap(ref) (which already guards unknown refs honestly).
  - VERIFIED end-to-end: 7 gold chips rendered for case notifications; clicked "Show CASE-000126 on the civic map" → map view, focus banner "CASE-000126 · Your report", gold marker pre-selected with YOUR REPORT panel + Open case button; notification marked read (6→5 unread).
- FEATURE C — i18n FOR CASE-DETAIL + INCIDENT-DETAIL (~90 new keys cd.*/id.*, EN/isiZulu/Afrikaans):
  - case-detail: back link + aria, all 6 tabs, all 6 PROGRESS_HINT status lines, CAN_DO/CANNOT_DO lists (key arrays), share menu (copy/copied/print/note), "Open investigation", Summary/Impact/Details section labels, all 6 fact labels, case-progress + aria + hint, 4 empty states (findings/evidence/sources/action) + descriptions, evidence/sources count lines ({n}/{off} interpolation), "Add evidence", why-approval card (title/body/3 bullets/note), full case activity + {n} events. Print sheet + toasts stay English (document export).
  - incident-detail: back + aria, not-found state, share menu + export hint, Summary/Details/Recent-updates/Public-sources/Location/Related-actions/Related-cases labels, 8 detail-row labels + "None attached yet", {n} attached, sources-empty body, coords caption, Investigate/Report-similar CTAs + approval note.
  - i18n.ts header coverage + LANGUAGES notes updated honestly.
  - VERIFIED live in isiZulu via screenshots: case-detail ("Wonke amacala", tabs "Uhlolojikelele/Uphenyo/Ubufakazi/Imithombo/Izenzo/Umsebenzi", "ISIFINGQO/IMININGWANE", facts "Insizakalo/Indawo/Idaliwe/Ibuyekezisiwe/Okubalulekile/Isimo", "Ukuqhubeka secala 8%") and incident-detail ("Buyela ezigamekweni", "ISIFUNDAZWE/UMASIPALA/INDAWO ETHINTEKILE/KUBIKIWE/ISIBUYEKEZO SOKUGCINA", "IZENZO EZIHLOBENE", "Phenya lesi sigameko").
- FEATURE D — BRIEFING WATCHLIST CASE CHIPS:
  - briefing-card.tsx watchlist now extracts CASE-\d{4,6} as well; INC keeps the blue chip, CASE gets the gold variant (consistent with the user's gold map markers); still only one chip per row (INC wins if both, which can't happen in practice).
- COMMAND PALETTE — NEW "App" GROUP (discoverability for the PWA):
  - "Install SASI as an app" (when installable, with honest fallback toast), "Reload to update SASI" (when updateReady), "App & offline settings" (jumps straight to the new section).
  - settingsSection added to the main store; Settings derives its active section from the store (no local state, no setState-in-effect — lint-clean) so palette/shortcuts/deep links can land on a specific section.
- STYLING DETAILS: sasi-offline-chip (amber hairline radial, blur, safe-area, print-hidden), sasi-chip-map-gold (gold glow hover, focus ring), sasi-ntf-actions hover-reveal strip + focus-within row tint + touch always-visible, Settings gold icon tiles + status tone system (good/warn/muted with breathing waiting dots), palette App group inherits existing group styling.
- QA: bun run lint PASS; tsc --noEmit PASS (src/); fresh reload zero console errors (two transient HMR artifacts investigated and confirmed stale); zero horizontal overflow at 1280px (12 views) AND 390px (6 views); SW caches verified; dev.log healthy (all 200s).

Stage Summary:
- SASI is now a real installable PWA: an offline shell reloads the app with no network (API honesty preserved — nothing cached, nothing fake), the offline state is visible everywhere (chip + Settings + palette), and updates arrive via a polite in-app toast. Notifications carrying case refs jump straight to the map (gold), briefing watchlists finally link user cases to the map, and the two most important record views now speak isiZulu and Afrikaans.
- Files: +public/sw.js, +src/lib/sasi/pwa-store.ts, +src/components/sasi/pwa.tsx; src/components/sasi/sasi-app.tsx (PwaRuntime + update toast), public/manifest.json (shortcuts, launch_handler), src/lib/sasi/store.ts (settingsSection, ALL_VIEWS, ?view= deep link in hydrate), src/components/sasi/views/settings.tsx (App & offline section, store-derived section), src/components/sasi/command-palette.tsx (App group), src/components/sasi/domain.tsx (NotificationRow map chip), src/components/sasi/views/notifications.tsx (map click-through), src/components/sasi/briefing-card.tsx (CASE watchlist chips), src/components/sasi/views/case-detail.tsx + incident-detail.tsx (full i18n), src/lib/sasi/i18n.ts (+~90 keys), src/app/globals.css (offline chip, gold chip, ntf actions, print guard, focus tint).
- Unresolved/risks: (1) isiZulu/Afrikaans strings remain hand-written, not native-proofread; report wizard/activity/evidence/admin views still English (documented in Settings). (2) The offline shell caches the HTML shell but NOT API data — an offline reload shows the app with demo/in-memory data; anything the user created that lived only in SQLite (their cases, chat, notifications) will NOT be visible offline after a hard reload (in-session navigation keeps it). An IndexedDB snapshot of the user's data would be the next level. (3) beforeinstallprompt is desktop-Chrome/Edge/Android only — on iOS Safari the Settings copy says "not available right now" with manual Add-to-Home-Screen guidance (honest, platform reality). (4) Service-worker update flow is exercised only via the dev-mode registration in this sandbox (prod versioning logic reviewed but not live-tested). (5) Notification POSTs remain fire-and-forget; DB growth still unbounded (carried over).
- Recommended next-phase priorities: (1) offline data layer — persist the user's cases/chat/notifications to IndexedDB on write and hydrate from it when the network is down (completes the offline story); (2) i18n for the report wizard + activity/evidence (remaining high-traffic surfaces); (3) iOS install guidance card in Settings with platform detection (Safari share-sheet steps with screenshots); (4) briefing watchlist chips already cover INC+CASE — extend the same focusOnMap chip into notification SYSTEM bodies by parsing INC- refs there too; (5) per-view i18n coverage meter in Settings (carried over).

---
Task ID: 4-b
Agent: i18n-expert (report/activity/evidence translation round)
Task: Extend real EN/isiZulu/Afrikaans i18n to the report wizard, activity and evidence views — audit, correct, and live-verify.

Work Log:
- DISCOVERY on arrival: the wiring for all three views was already in the working tree (uncommitted: report/activity/evidence import useT with TKey-typed label maps, and i18n.ts already carried the rp.*/act.*/ev.* sections plus the updated header-coverage comment and LANGUAGES notes) — apparently an earlier 4-b pass that never logged. So this round became: exhaustive audit + translation quality pass + one honesty fix + full live verification, rather than fresh wiring.
- AUDIT: scripted a key-usage extraction (all t("…") calls + dynamic `rp.kind.*` + constant-map keys in the three views) against the DICT — 169 distinct keys checked, all present, every one with en+zu+af (zero missing, zero zu/af fallbacks). Scanned the three views for hardcoded English JSX/labels — only intentional leftovers: StatusBadge/DemoBadge mono badges (shared primitives, English-by-convention across ALL views), canonical stored values (problem option values, evidence-note serialisation "Photo:/Link:/Note:"), service meta labels (untranslated everywhere) and domain.tsx ActivityRow event text (other agent's file, demo-data strings).
- HONESTY FIX — rp.evidence.photo-hint claimed photo analysis happens "on-device"; the app actually POSTs to /api/sasi/vision (server-side vision model), and photo-analysis.tsx's own comment says "the photo itself is never persisted". Rewrote en/zu/af to the honest framing: "read by SASI's AI — the photo itself is never stored." / "iyafundwa i-AI ye-SASI — isithombe ngokwaso asigciniwe." / "gelees deur SASI se KI — die foto self word nooit gestoor nie."
- TRANSLATION CORRECTIONS (23 keys, en untouched except the claim above): fixed genuine isiZulu grammar/typos — "Kengeziwe"→"Kwengeziwe" (rp.review.items-one/-many), "Awabelwana nawe"→"Awabelwana ngawo" (rp.impact.hint, was "not shared with you"), "Abubhalwanga"→"Ayibhalwanga" (ev.row-not-recorded, class-9 agreement for indawo), "owengezile"→"olwengezile"/"esengezile" (ev.fallback-note/-link class-11/5 relatives), object concords "izoyifunda/iyifunda"→"izosifunda/iyisifunda" (rp.evidence.photo-title/-reading, isithombe is cl.7), "ngi-AI"→"ngo-AI" (rp.evidence.ai-note), "zela elingaphansi"→"zama esinye esingaphansi" (rp.err.size), "obungatshontshi" (= "not stealing"!)→"Irekodi ecace" (act.subtitle; "irekhodi" is the word the dict already uses elsewhere), "UQOSHO LWEDEMO"→"IREKHODI YEDEMO" (act.count, same alignment), "mongo wedemo"→"le demo" ×4 (ev.demo-note/-add-desc/-nothing-leaves/-toast.desc; "mongo" means benefit, not environment), "Isiphethu"→"Isigodi" (rp.where.placeholder, suburb), "erheferensi"→"kureferensi" (ev.stat-linked-hint, matches dict's "kurekhodi" pattern), "le mibiko"→"lo mbiko" (rp.review.submit-note, singular cl.3), "kulesi sihlungi"→"kule hlungi" (act.empty.title, matches the dict's own "ihlungi" class). Afrikaans: "KI-gestutte"→"KI-gesteunde" (ai-note), "van onder ongeveer 10 MB"→"van ongeveer 10 MB of kleiner" (err.size), "Fotoplaaai"→"Foto-oplaai" (coming-photo-aria), "filters te maak"→"filters te skrap" (ev.empty.description).
- REPORT WIZARD GAP: the review step rendered the stored canonical problem value raw ("No water") while every other surface maps it through PROBLEM_KEYS — added the same lookup so the review row shows the translated label (falls back to the raw value for the user's own free-text "Other").
- Checked and left alone deliberately: act.count/act.subtitle "DEMO RECORD" mono line stays keyed (translated), severity pill on AI analysis stays raw uppercase mono (same convention as PriorityBadge on incident cards), service-name tiles stay SERVICES meta labels (untranslated in every view).
- QA: bun run lint PASS; tsc --noEmit shows zero errors for src/ EXCEPT one pre-existing/concurrent error in notifications.tsx:284 (mapRef null-vs-undefined — belongs to the concurrent 4-a agent's file, untouched by this round); dev.log healthy (all 200s).
- VERIFIED live in isiZulu via agent-browser (store-driven): report wizard end-to-end — hero "Tshela i-SASI okwenzakalayo", stepper Yini/Kuphi/Nini/Umthelela/Ubufakazi/Bheka kabusha/Kwenziwe, "ISINYATHELO n SE-7", option cards ("Amanzi awatholakali"…), where/when/impact hints, evidence step with the corrected photo-hint, note+link chips with "Engeza"-arias, review table ("Kwengeziwe into eyodwa", "Amanzi awatholakali" after the fix, "Uma uthumela… lo mbiko"), submit → done screen "Kubikiwe ku-SASI · Qala uphenyo manje/Bheka icala/Buyela kudashibodi" (CASE ref mono untranslated, as intended); activity — "Umsebenzi", "IZEHLAKALO EZINGU-14 · IREKHODI YEDEMO", corrected subtitle, chips Konke…Ukuqinisekiswa, buckets NAMUHLA/IZOLO/OKWEDLULE with keyed aria; evidence — "Indawo yokugcina ubufakazi", stats + "Kuxhunywe kureferensi yecala", search/filters, "Wonke amacala" select, type chips, preview dialog (UHLOBO/KULAYISHWE/INDAWO/UKUQINISEKISWA, "Kunikezwe ngumsebenzisi", "Vula icala"), add dialog ("Ukukulayisha…" arias, "Akukho nokuphuma kule demo", "Gcina endaweni yokugcina"), gibberish-search empty state ("Abukho bufakazi…", "Susa zonke izihlungi"). Afrikaans spot-checked on all three (incl. the corrected KI photo-hint) via the same flow. No raw "rp./act./ev." keys anywhere. Finished with setLang('en') (html[lang]=en) and a fresh reload: agent-browser errors shows exactly the 4 known-stale entries, no new ones; transient mid-edit parse errors in store.ts/settings.tsx console output belong to the concurrent agent's HMR saves (files on disk fine, Fast Refresh recovered).

Stage Summary:
- The three remaining high-traffic views are confirmed trilingual: every user-facing string in report/activity/evidence resolves through the dictionary (167 own rp./act./ev. keys + ~20 reused shared keys), a dishonest "analysed on-device" claim is gone, ~23 translated strings had genuine isiZulu/Afrikaans errors corrected, and the review step now displays the translated problem label. Settings coverage notes and the i18n header already listed these views — now honestly true and verified.
- Files: src/lib/sasi/i18n.ts (23 keys corrected + honest photo-hint rewrite), src/components/sasi/views/report.tsx (review-step problem label through PROBLEM_KEYS), src/components/sasi/views/activity.tsx + evidence.tsx (verified, no code changes needed this round).
- Unresolved/risks: (1) isiZulu/Afrikaans strings remain hand-written, not native-proofread — corrections were dictionary-consistent but a speaker should still review; "Isigodi"/"usesha"/"ihlungi" word choices are defensible, not certified. (2) Concurrent 4-a agent's notifications.tsx currently has a tsc error (mapRef null-vs-undefined) — flagged, not mine to fix; re-run tsc after their round lands. (3) Store-level toasts ("Report CASE-000128 created", offline copy) and shared badges (StatusBadge/DemoBadge/CaseStatusBadge) stay English — that is app-wide convention, noted honestly in Settings; making badges i18n-aware needs primitives.tsx + domain.tsx (other agent's files). (4) ActivityRow demo-event descriptions stay English (domain.tsx data strings, same reason). (5) Submit wizard left QA data (CASE-000128) in the dev DB for this browser session — harmless per prior rounds' precedent.
- Recommended next-phase priorities: (1) i18n for the remaining English surfaces — admin view, store-level toasts, and shared primitives (StatusBadge/DemoBadge labels via optional labelKey) once domain.tsx/primitives.tsx ownership allows; (2) native-speaker proofread pass over zu/af strings (a single "translate all keys" review, ideally with a glossary for service names); (3) the concurrent round's notifications tsc error must be resolved before merge (one-line `?? undefined` / null-check).

---
Task ID: next-phase+3 (offline data layer / notification→map INC chips / iOS guidance / coverage meter round)
Agent: main (autonomous QA + flagship feature round) + i18n-expert subagent (Task 4-b)
Task: Assess status + agent-browser QA → stable, so advance the handover's top priorities: (A) offline data layer — IndexedDB snapshot of the user's data [carried over 3×], (B) i18n for report/activity/evidence, (C) iOS install guidance, (D) SYSTEM notification INC→map chips + per-view i18n coverage meter — plus the mandatory styling-details pass.

Work Log:
- STATUS ASSESSMENT: worklog tail read (434 lines); lint PASS; tsc PASS (src/); dev.log all 200s; agent-browser sweep of landing + 11 views — zero NEW console errors. Investigated 4 "usePwaStore is not defined" ReferenceErrors in the session error log: proved STALE (import/export verified matching on disk, palette's App group renders live, __sasiPwa store live with swPhase "ready", error count never grew across two fresh reloads + palette open) → stable, feature round.
- BUG FIXES found during QA: (1) hydrate's `if (!res.ok) return;` silently skipped deep-link handling AND the service-alert block on any non-OK server response — restructure now always runs them. (2) Settings copy bug "Snapshot last written 1m ago ago" (timeAgo already includes "ago"). (3) A real race in the NEW offline mirror (see below): per-operation IDB open/close churn silently lost writes under hydrate-time concurrency (briefing + notifications mirrored simultaneously) — rewritten to a single cached connection + serialized write queue; dev-only console.warn on write failure so this class of bug is never silent again.
- FLAGSHIP — OFFLINE DATA LAYER (src/lib/sasi/offline.ts, new):
  - IndexedDB "sasi-offline" (single kv store): one cached connection, write queue serializes every mutation, reads concurrent, broken connections self-heal (onclose → re-open).
  - Write path: every persistence point in store.ts mirrors to the snapshot — persistCase → snapshotCase, addEvidence → snapshotEvidence, pushNotification/markRead/markAll → mirrorNotifications, askSasi (all 3 terminal paths) → mirrorChat, persistBriefing → snapshotBriefing, persistLocation → snapshotLocation, clearChat → clearSnapshotChat. All fire-and-forget; caps (40 cases/60 chat/30 ntfs/24 evidence).
  - Read path: hydrate now merges the SNAPSHOT FIRST (works with no network), then the server copy over it (source of truth when reachable) — both through one shared mergeRemote() helper (same dedupe/prepend rules). Snapshot briefing seeds state + sessionStorage so City mode works offline.
  - Offline honesty: `restoredOffline` flag + one quiet toast ("Restored from this device"); briefing LLM warm-up SKIPPED while offline (snapshot covers the card); service alerts still fire offline (static demo data).
  - Settings → App & offline: new "On this device" card — live counts (reports/chat/notifications/evidence/briefing-cached) via refreshDeviceSnapshot + Refresh button, "Snapshot last written …", amber sasi-restored-note when the visit started offline, honest "never leaves this device" copy; empty state invites first write.
  - VERIFIED END-TO-END: report → snapshot cases=1 (browser-wide: grows to 2 with prior QA case ✓); chat ask → snapshot chat mirrors (restores across sessions in same browser ✓); briefing mirror commits after the queue fix ✓; REAL server-unreachable test via `agent-browser network route **/api/sasi/state* --abort` + reload → restoredOffline=true, 2 cases + 6 chat + briefing + 14 notifications ALL restored from device; unroute + reload → clean recovery (restoredOffline=false).
- FEATURE B — i18n REPORT/ACTIVITY/EVIDENCE (Task 4-b subagent): discovered the wiring already existed in-tree from an unlogged earlier pass; became an audit pass: 169 keys checked, 0 missing, 23 zu/af corrections (grammar, concords, "KI-gestutte"→"KI-gesteunde" etc.) + honesty fix (photo hint no longer claims "analysed on-device" — it names the vision API); report review step now renders the translated problem label. Verified live in isiZulu across all three views (wizard stepper → done screen, activity buckets, evidence dialogs). LANGUAGES notes updated.
- FEATURE C — iOS INSTALL GUIDANCE: platform detection (iPad|iPhone|iPod UA + MacIntel-touchpoint heuristic) in AppOfflineSection; when iOS && !installed && !installable → honest StatusRow + a 2-step "Install on iPhone or iPad" card (Share → Add to Home Screen), explicitly explaining Apple allows no one-tap prompt.
- FEATURE D — SYSTEM NOTIFICATION → MAP: service-alert bodies embed INC refs; notifications.tsx parses the first INC-\d{4,6} from body when no caseRef, validates against demo INCIDENTS, and offers the same gold chip; domain.tsx NotificationRow gains mapRef prop for correct a11y label ("Show INC-0041 on the civic map"). VERIFIED: alert chip → click → map view, mapFocusRef=INC-0041, banner "INC-0041 · From your city briefing", incident panel opens.
- FEATURE E — PER-VIEW i18n COVERAGE METER: Settings → Language "Honest coverage" paragraph replaced with a real table: 17 surfaces × EN/ZU/AF dots (green full / amber chrome-only / grey English-only), legend + "isiZulu 10/17 · Afrikaans 10/17" tally, hand-maintained I18N_COVERAGE map kept honest alongside i18n.ts notes.
- STYLING DETAILS: sasi-snapshot-grid staggered tile entrance (5-tile rise, reduced-motion guarded), sasi-restored-note breathing gold hairline, sasi-ios-step hover glow + nudge, sasi-coverage-list soft staggered fade — all consistent with the national-light token system and reduced-motion safe.
- QA: bun run lint PASS; tsc --noEmit PASS (src/); zero new console errors (only the 4 known-stale session entries); isiZulu smoke test clean after all changes; dev.log healthy (all 200s). New Settings cards use the same fluid patterns that passed prior 390px rounds; CLI viewport emulation unavailable this session (viewport/device subcommands not supported) — noted as residual risk.

Stage Summary:
- SASI's offline story is now complete: the service worker reloads the shell with no network AND the user's cases, chat, notifications, evidence, location and briefing come back from an on-device IndexedDB snapshot — with honest signals (toast, Settings note, offline chip) and zero fabricated data. Service alerts gained the same map jump the case notifications already had, iOS users get real install steps instead of a dead-end toast, and Settings now shows the true per-view language coverage.
- Files: +src/lib/sasi/offline.ts; src/lib/sasi/store.ts (mergeRemote, snapshot mirrors at every persistence point, restoredOffline + deviceSnapshot + refreshDeviceSnapshot, hydrate rewrite: snapshot-first → server-over → always-run deep links); src/components/sasi/views/settings.tsx (On-this-device card, SnapshotTile, iOS guidance + detection, CoverageTable + I18N_COVERAGE, timeAgo copy fix); src/components/sasi/views/notifications.tsx (INC-ref map chips); src/components/sasi/domain.tsx (NotificationRow mapRef); src/components/sasi/views/report.tsx + src/lib/sasi/i18n.ts (Task 4-b corrections); src/app/globals.css (4 new class families + keyframes).
- Unresolved/risks: (1) Browser-viewport emulation commands unavailable in this agent-browser build — 390px QA for the NEW Settings cards reasoned from pattern-equivalence, not measured; next round should re-run the standard 390px sweep. (2) IDB snapshot is per-origin (shared across sessionIds in one browser) while SQLite is per-sessionId — deliberate (it's the same device-owner), but multi-person shared-browser scenarios would mix data; acceptable for demo. (3) restoredOffline describes the load-time state only; reconnecting does not re-sync until next reload (documented copy says so). (4) Coverage meter is hand-maintained — could drift from the dictionary if views change without updating I18N_COVERAGE. (5) isiZulu/Afrikaans remain hand-written, not native-proofread (carried over); store-level toasts + shared badges still English (carried over).
- Recommended next-phase priorities: (1) standard 390px mobile sweep of the new Settings cards + notifications INC chips (restore viewport tooling or use CDP directly); (2) reconnect re-sync: when `online` flips back true, quietly re-run the server merge + briefing warm-up instead of waiting for reload; (3) i18n for the remaining English surfaces (services/about/security views, store toasts, shared badges via optional labelKey); (4) evidence photos in the offline snapshot are stored as full dataURLs — consider capping total snapshot bytes and evicting oldest evidence first; (5) native-speaker proofread pass over zu/af (carried over).

---
Task ID: 11 (rebrand round — "Service Intelligence")
Agent: main
Task: User instruction — "load latest save and please change name to Service Intelligence instead of Civic". Rename the platform brand from "South African Civic Intelligence Platform" to "South African Service Intelligence" everywhere the NAME appears.

Work Log:
- Loaded latest save: prior state = next-phase+3 offline data layer round (IndexedDB snapshot system complete, lint/tsc passing, zero console errors).
- Audited ALL "Civic" occurrences project-wide (src/, public/, docs) and split them into three classes: (a) BRAND strings → rename; (b) capitalized surface labels ("Civic map/assistant/directory/incidents/information") → rename to "Service …"; (c) generic lowercase civic prose ("civic technology", "civic service issues", "civic problems") → KEEP — honest category vocabulary per §0 (independent civic-tech), untouched.
- BRAND renames: layout.tsx metadata (title.default / openGraph.title / twitter.title / keywords "service intelligence" / twitter description), public/manifest.json (name + description), globals.css header comment, all 4 API system prompts (ask / vision / briefing ×2 — AI now self-identifies as "South African Service Intelligence"), public-shell brand line, about brand sentence, case-detail + incident-detail "Generated by SASI — South African Service Intelligence (demo)", splash tagline in sasi-app.tsx, map aria-label, types.ts philosophy comment, worklog header.
- Surface labels: landing "Live service intelligence" + "Latest service information" (+ section comments), dashboard section key RENAMED dash.civic-intelligence → dash.service-intelligence (store-side usage updated in dashboard.tsx), command palette "Service map" label + "Service assistant" hint (+ added "service" to match keywords), ask-sasi "Service directory" hint + "Service assistant · grounded…" line, service-detail "Service information" section, incidents title "Service incidents" (+ subtitle already generic), settings I18N_COVERAGE label "Service map", aria labels "on the civic map" → "on the SASI map" (briefing-card ×2, domain.tsx).
- i18n zu/af rewritten for renamed keys (NOT machine-literal, checked for grammar): landing.hero.a → en "Service intelligence for" / zu "Ukwazi ngezinsizakusebenza" / af "Diensintelligensie vir"; dash.service-intelligence → zu "Ukwazi ngezinsizakusebenza" / af "Diensintelligensie"; inc.title → en "Service incidents" / zu "Izigameko zezinsizakusebenza" / af "Diensvoorvalle". Grammar verified live in hero composition: zu "Ukwazi ngezinsizakusebenza eNingizimu Afrika." · af "Diensintelligensie vir Suid-Afrika." — both flow correctly with landing.hero.b.
- QA via agent-browser: document.title = "SASI — South African Service Intelligence" ✓; splash + hero "Service intelligence for South Africa." ✓; public shell "South African Service Intelligence" ✓; about brand sentence ✓; dashboard eyebrow "SERVICE INTELLIGENCE" ✓; palette "Service map" ✓; incidents "Service incidents" ✓; zu/af/en hero all rendered ✓; console error/warning sweep EMPTY ✓; dev.log all 200s.
- bun run lint PASS; tsc --noEmit clean for src/ (only pre-existing excluded examples/ + skills/ errors).
- Verification sweep: `rg "Civic Intelligence|Civic intelligence|Civic map|Civic assistant|Civic directory|Civic incidents|Civic information|civic-intelligence"` over src/ + public/ → ZERO matches.

Stage Summary:
- The platform is now named South African Service Intelligence end-to-end: metadata, PWA manifest, splash, hero, shells, dashboard, palette, incidents, exports/disclaimers, AI system prompts, and the zu/af translations of every renamed string. Generic lowercase "civic" domain vocabulary deliberately retained (civic technology / civic problems / civic service issues) — it describes the sector, not the brand, and keeps the §0 honest-identity language intact.
- Files touched: src/app/layout.tsx, src/app/globals.css, public/manifest.json, src/app/api/sasi/{ask,vision,briefing}/route.ts, src/components/sasi/{sasi-app,public-shell,domain,briefing-card,command-palette}.tsx, src/components/sasi/views/{landing,dashboard,about,incidents,service-detail,ask-sasi,map,settings,incident-detail,case-detail}.tsx, src/lib/sasi/{i18n,types}.ts, worklog.md header.
- Unresolved/risks: (1) carried over from next-phase+3: 390px mobile sweep of new Settings cards, reconnect re-sync, i18n for services/about/security surfaces, snapshot byte caps, native zu/af proofread. (2) The renamed zu phrase "Ukwazi ngezinsizakusebenza" is hand-written (not native-proofread) — flagged for the existing proofread carry-over. (3) Round 9's unfinished hero "SASI background text + circulating glow outline" and Round 10 items (split-screen auth, sidebar collapse toggle, map icons polish, fake-data removal audit) remain the standing backlog.
- Recommended next-phase priorities: (1) Round 10 backlog — split-screen auth layouts, sidebar collapse toggle, dashboard declutter pass; (2) hero background SASI text with circulating glow (Round 9 leftover); (3) map icon/interaction polish; (4) the carried-over offline/mobile/i18n priorities from next-phase+3.

---
Task ID: 12-a
Agent: auth-split-screen (subagent)
Task: Round 10 backlog item — split-screen fashion-minimal auth. (1) login/signup render with NO top nav and NO footer; (2) both auth pages redesigned as split screens (dark brand panel left / minimal form right) using the existing "circulating glow" design language from globals.css; (3) all existing form logic/state preserved.

Work Log:
- Read worklog (Round 10/11 backlog: "split-screen auth layouts" was the standing #1 priority), sasi-app.tsx (mounting: login/signup are PUBLIC_VIEWS → PublicShell), public-shell.tsx, existing login.tsx + signup.tsx, primitives (PrimaryButton/GhostButton/DemoBadge/SasiLogo), Input, and the globals.css design classes (sasi-search-glow / sasi-btn-white-glass / sasi-btn-glass / sasi-serif / sasi-eyebrow / sasi-line / sasi-ambient / sasi-hero-word / sasi-breathe) + landing hero usage patterns.
- public-shell.tsx: added `isAuthView = view === "login" || view === "signup"`; header and footer are conditionally not rendered for auth views (`{isAuthView ? null : (<header/>)}`). The `min-h-screen flex flex-col` + `main flex-1` + footer `mt-auto` skeleton is untouched, so all other public views (landing/about/how-it-works/services/security/privacy/terms) keep normal chrome and the footer still sticks to the viewport bottom.
- login.tsx (layout+style refactor, logic byte-preserved: email/password/loading/success/forgot state, timers + handleContinue, signIn → dashboard flow, SA ID ghost button + COMING SOON badge, terms/privacy navigation, demo hint):
  - LEFT (lg+ only, hidden on mobile): dark `#070708` panel with `sasi-hero-word` "SASI", 2 `sasi-ambient` breathing lights (red top-left / blue bottom-right, inline radial-gradient styles), `sasi-eyebrow` "Service intelligence" with breathing national-gradient dot, `sasi-serif` headline "Service intelligence for South Africa.", honest sub-line "Independent civic technology. Not a government website.", national accent dot + wordmark line at the bottom. Panel is `overflow-hidden border-r border-white/6`.
  - RIGHT: card-less minimal form on near-black — serif "Sign in" heading, Email + Password shadcn Inputs wrapped in a `GlowField` div (`sasi-search-glow rounded-lg border border-white/10` → circulating national conic glow ring on focus-within, computed ::before opacity verified = 1 on focus; inputs are borderless h-11 = 44px), `PrimaryButton` + `sasi-btn-white-glass` h-11 submit, "or" hairline divider, glassy disabled SA ID button, "Create account" toggle to signup, "Forgot password" (state preserved), Terms/Privacy note, subtle "Back to home" link, demo hint as plain line (no card).
  - Mobile: single column — compact SasiLogo header + `sasi-line` on top (lg:hidden), form below; all interactive targets ≥44px (inputs h-11, submit h-11, link rows h-11, eye toggle 44×44).
- signup.tsx: same split-screen treatment; name/email/password/showPassword/loading/success state + handleCreate + success/toggle/terms/demo copy all preserved. Password eye toggle now a 44×44 hit area inside the glow wrapper; gold/green ambient variant on the brand panel; "Already have an account? Sign in" toggle + "Back to home" link; NOT SENT ANYWHERE badge kept. Submit uses `PrimaryButton` + `sasi-btn-white-glass` (consistent with login).
- No i18n.ts edits; copy is plain inline English exactly as the files already were (login/signup never used useT()).
- QA (agent-browser): navigate('login') / navigate('signup') → `document.querySelectorAll('header').length === 0 && footer.length === 0` on both ✓; aside + sasi-hero-word + 2/3 sasi-search-glow fields + white-glass submit present ✓; page fills viewport exactly (390×844 scrollHeight = 844, 700 = viewport on desktop) ✓; typed into all fields (state holds, get value ✓); glow ring computed opacity 1 while focused ✓; eye toggle flips type password↔text ✓; submit login AND signup → authed:true → dashboard ✓; landing + about → header(4 nav buttons)+footer present, footer bottom == innerHeight ✓; VLM visual inspection of screenshots confirmed split screen / no chrome / no glitches (desktop login, mobile 390px login, filled signup) ✓; console sweep: zero error messages (only Fast Refresh/HMR info); the 6 EMPTY pageerror entries in `agent-browser errors` reproduce on a brand-new tab before any navigation → pre-existing dev artifact, not from these views ✓; dev.log all 200s, compiles clean ✓.
- bun run lint PASS (exit 0); tsc --noEmit clean for src/ (only pre-existing examples/ + skills/ errors).

Stage Summary:
- Auth is now a chrome-free, fashion-minimal split screen: giant outlined SASI word with circulating national light on the left, a quiet card-less form with the circulating glow ring on focus and a white glass submit on the right — mobile collapses to a single column with a compact logo header. All previous auth behavior (demo sign-in, success states, forgot-password note, SA ID coming-soon, terms/privacy links, demo badges) is intact. Landing/about/public chrome untouched and footer still sticks.
- Files touched: src/components/sasi/public-shell.tsx, src/components/sasi/views/login.tsx, src/components/sasi/views/signup.tsx (ownership scope respected — no other files edited).
- Risks/notes: (1) auth pages are exactly min-h-screen; if a future language adds much taller copy on 320px-wide screens the form scrolls naturally (by design). (2) The bare render is driven by view in PublicShell — if a new auth-like public view is added, it must be added to the isAuthView condition to go chrome-less. (3) sasi-hero-word font-size uses 30vw, clipped by the panel's overflow-hidden — verified no horizontal overflow. (4) Round 10 leftovers that remain: sidebar collapse toggle, dashboard declutter, map icons polish, fake-data removal audit.

---
Task ID: 12-b
Agent: sidebar-collapse + dashboard-minimal (Z.ai Code subagent)
Task: (A) app-shell.tsx — collapsible sidebar (persistent, animated, 44px collapsed rail with floating labels); (B) dashboard.tsx — minimal honest rebuild for the "remove clutter / all content user-generated / effortless" round.

Work Log:
- TASK A — app-shell.tsx: AppShell now owns `sidebarCollapsed` (useState initializer reads localStorage key `sasi-sidebar-collapsed`, useEffect writes "1"/"0" on change — zustand store untouched, try/catch guards private mode). Sidebar takes `collapsed` + `onToggle` props; new header toggle (PanelLeftClose expanded / PanelLeftOpen collapsed, title + aria-label + aria-expanded). Aside animates `transition-[width] duration-300 ease-in-out motion-reduce:transition-none` between `w-[228px]` and `w-[60px]`; content wrapper animates matching `lg:pl-[228px]/[60px]`. Collapsed rail: header = centered 44px expand button; section labels replaced by hairline dividers; every link collapses to a centered 44×44 icon with `title` floating label + aria-label, active white indicator bar and gold unread dot kept (badge number → dot); profile footer collapses to initials tile (title = name); admin row collapses to 44px icon. Topbar gains the SASI mark (lg only) while the rail is collapsed so brand presence survives. Desktop-only sidebar (no mobile drawer exists → nothing to keep there); mobile keeps bottom nav. Sidebar footer decluttered in passing: "Demo account" suffix + "DEMO" admin chip removed per the no-demo round.
- TASK B — dashboard.tsx rebuilt (~360 → 290 lines): greeting header = real-clock `dash.greeting.*` (no DEMO_NOW) + DEMO_USER.firstName in sasi-serif, with one HONEST status line derived only from store cases ("Nothing on your plate yet." at zero; active/action-required counts otherwise — no fake metrics). Exactly two primary actions: "Report an issue" (sasi-btn-white-glass, h-11) + "Ask SASI" (sasi-btn-glass, h-11). Removed entirely: stat tiles (incl. nearby-demo-incidents), command-card duplicate, Next-steps flagship case-123 logic, tips, service shortcuts, demo ACTIVITY sections, DemoBadge, start-investigation ghost button. Max 3 data-gated sections: (1) briefing → CityBriefingCard only when `briefing` exists (card's own auto-refresh still applies; no auto-gen on an empty dashboard since the card doesn't mount), (2) "Service intelligence" = recent cases as quiet rows (service icon, title, ref · timeAgo, status badge) in a `max-h-96 sasi-scroll` list, cap 8, "View all cases" footer, (3) notifications → unread-only rows via NotificationRow in `max-h-80 sasi-scroll`, click marks read + opens the view. Whole-empty state (no cases && no briefing && no unread) renders one premium `NothingHereYet`: bordered icon tile (Inbox), sasi-serif "Nothing here yet", honest sub-line "Your reports, briefings and answers appear here once you create them — nothing is pre-filled.", quiet sasi-btn-glass CTA. Layout narrowed to max-w-3xl single column (calm focus), everything stacks cleanly at 390px.
- QA (agent-browser + VLM): dashboard DOM asserts — h1 serif greeting, 2 action buttons, exactly 3 sections with seed data (City briefing / Service intelligence / Notifications), 2 sasi-scroll lists, zero "demo" text in dashboard markup (remaining "DAILY · DEMO" strings come from briefing-card.tsx — not my file, parallel purge). Collapse: click real button → aside 60px, links 44×44, labels hidden, titles present on all 11 links, active bar still rgb(255,255,255), unread gold dot, localStorage="1"; full reload → 60px persists (screenshot); expand → 228px + labels restored + "0". Empty state verified live by runtime-only `__sasiStore.setState({cases:[],notifications:[],briefing:null})` probe (no file edits, reloaded after): serif line + sub-line + CTA + status "Nothing on your plate yet." — screenshots /tmp/dash-1..4. Natural zero-cases state appeared mid-QA when the parallel data.ts purge landed (cases:0 → no cases section, honest status line, briefing+unread sections still render). 390×844 viewport: no horizontal scroll, sidebar hidden (bottom nav), both actions visible. VLM visual pass on expanded/collapsed/empty screenshots: clean, no glitches. bun run lint PASS; tsc clean for src/ (only pre-existing examples/+skills/ errors). Console: no errors from my files (LandingView `DemoBadge is not defined` HMR errors belong to the parallel purge agent's in-flight landing.tsx edit).

Stage Summary:
- The app shell now tucks away: a persistent (localStorage), animated (250–300ms, reduced-motion safe) collapse turns the 228px sidebar into a 60px icon rail with 44px targets, native hover labels, kept active/unread indicators, and a topbar brand mark while collapsed. The dashboard is a calm, honest home: serif greeting + truthful one-line status, two dominant actions, at most three sections that only render real user data (briefing / cases / unread notifications) with hover-reveal scrollbars, and a single premium empty state that says exactly what will (and will NOT) appear. Zero demo chrome, zero simulated metrics, no stored state beyond one localStorage flag; store/i18n/css untouched.
- Files: src/components/sasi/app-shell.tsx; src/components/sasi/views/dashboard.tsx.
- Risks: (1) collapsed-rail hover labels use native `title` (spec-sanctioned) — a custom CSS tooltip was skipped because the scrollable nav clips overflowing children; (2) CityBriefingCard still carries its own "DEMO" chips/copy — owned by the parallel demo-purge agent, not dashboard.tsx; (3) `hasData` treats unread>0 as data, so a fresh user with seeded/persisted notifications sees the notifications section rather than the empty state (honest either way); (4) collapsed width (60px) and paddings are hand-tuned — future nav additions just work via the same SidebarLink path; (5) mobile has no sidebar drawer by design (bottom nav), so the "obvious close button" clause was N/A.
---
Task ID: 12-e2
Agent: Z.ai Code (views honesty/empty-state agent)
Task: Honesty sweep + empty-state robustness across the remaining views (cases, activity, notifications, ask-sasi, services, service-detail, incident-detail, case-detail, command-palette) after the dataset purge; ask-sasi composer glow + honesty line; palette empty-dataset safety; fashion polish (sasi-eyebrow / sasi-line / sasi-search-glow).

Work Log:
- HONESTY SWEEP (rg-mapped every demo/sample occurrence in my 9 files first):
  - REMOVED: DemoBadge import+usage in cases.tsx, activity.tsx, notifications.tsx, ask-sasi.tsx, service-detail.tsx (incl. the `DEMO · N INCIDENTS TRACKED` header badge → plain mono "N incidents tracked" span), incident-detail.tsx (`DEMO DATA` chip), case-detail.tsx. All "(Demo data.)" timeline suffixes in incident-detail. services.tsx "{n} demo incidents tracked"/"No demo incidents tracked" → "{n} incidents tracked"/"Nothing tracked yet". service-detail "demo dataset" empty copy → honest "tracked yet / SASI never invents data" copy. Palette footer "SASI command center — demo environment" → "— nothing is sent without your approval". ask-sasi header sub-line → "Answers are AI-assisted — unverified. Grounded only in what you provide." (inline EN per spec); SUGGESTIONS fake case ref "CASE-000123" → "What is the status of my case?"; palette idle row "Investigate my water outage" (hardcoded startInvestigationFor("case-123") — fake seed ref) → "How does an SASI investigation work?" via the ask path; startInvestigationFor branch/selector/import + unused CASES import removed. Clipboard/print footers in case-detail + incident-detail: "(demo)" → "(not a government submission)"; print doc headers now "…· {ref} — NOT AN OFFICIAL DOCUMENT" (DEMO token dropped). case-detail toasts reworded ("in this demo"/"(simulated for demo)" → "follows automatically."/"(simulated)"); "demo environment does not support…" → "…not supported yet."
  - i18n keys that still say demo but are used in MY files (i18n.ts not mine — inlined honest English at call sites): cd.notfound.desc ("No case with this reference is in your record…"), id.notfound.desc ("No incident with this reference is currently tracked…"), cd/id.share.note ("Direct link sharing is not available yet."), id.updates.note ("Status history as tracked by SASI — not a live event feed."), id.coords ("STYLISED POSITION · INDICATIVE COORDINATES"), act.count (inline "{n} EVENTS · YOUR RECORD").
  - KEPT (required honest disclaimers): ask-sasi bottom "…Nothing is submitted without your approval."; service-detail "SASI is independent and not an official channel"; incident/case-detail print footers' full "not a government document / nothing submitted / never contacts an authority" text; id.actions.note.
- EMPTY-STATE ROBUSTNESS: cases.tsx — new zero-cases state (bordered Inbox tile, sasi-serif "No cases yet", "…appear here only once you report an issue — SASI never invents data.", sasi-btn-glass CTA → report); existing filter EmptyState now only shows when cases exist but filters match none. activity.tsx — new zero-activity state (same tile pattern, "No activity yet", "…appears only once you create a case — SASI never invents data."); filter EmptyState kept for the (currently unreachable) filtered-empty branch. service-detail water flagship — featuredIncidents grid had NO empty branch (empty grid) → added "No water incidents tracked right now" card matching the non-water branch (whose demo copy was rewritten). Verified no `[0]` indexing / `.find(...)!` / unguarded maps in my files (INCIDENTS-empty flows all route through not-found EmptyStates).
- FASHION: ask-sasi composer container sasi-pulse → sasi-search-glow (both are ::before conic rings; keeps a single circulating national glow on focus, matches login GlowField). services search wrapper += sasi-search-glow (alongside sasi-command-focus, rules identical). Palette search row += sasi-search-glow + rounded-t-xl. sasi-eyebrow on all SectionLabels in services (Popular), service-detail (5), incident-detail (7), case-detail (7). sasi-line divider above the Recommended section (services) and Official pathways (service-detail).
- QA (agent-browser): fresh load → navigate cases/activity/notifications/ask-sasi/services one by one — every view renders a real h1, zero /demo/i matches in main, no dead grids. cases: serif "No cases yet" + honest line + glass CTA ✓; activity: serif "No activity yet" + "0 EVENTS · YOUR RECORD" ✓; notifications: "You're all caught up." + digest strip zeros ✓; ask-sasi: honesty line, bottom approval line, 1 .sasi-search-glow wrapper ✓; services: "Nothing tracked yet", 1 eyebrow, glow, sasi-line ✓. service-detail water (5 eyebrows + water empty card) and healthcare (4 eyebrows + incidents/cases empty cards) ✓. Palette (empty datasets): groups = Ask SASI/Services/Navigation/Language/App only — Cases/Incidents/Evidence headers correctly absent; garbage query → 3 always-matching ask rows, no crash, cmdk-empty correctly absent; footer demo-free ✓. case-detail not-found via openCase('CASE-0000') → honest desc, no demo ✓. Screenshot /tmp/e2-ask.png VLM-verified (honesty line ✓, no DEMO ✓, empty state ✓, no glitches ✓). agent-browser console/errors: nothing beyond Fast Refresh/HMR info. dev.log 200s, compiles clean. localStorage NOT cleared.
- bun run lint PASS (0 errors); tsc --noEmit clean for src/ (only pre-existing examples/+skills/ noise).
Stage Summary:
- Every remaining view now tells the truth: zero demo badges/copy anywhere in the 9 owned files (remaining "DEMO_NOW" matches are a data.ts export name used for time math — not user-facing), all list views render calm intentional empty states built on the bordered-tile + sasi-serif + "SASI never invents data" pattern, the ask composer and both search surfaces carry the circulating national glow, detail views got the eyebrow/line fashion treatment, and the command palette is provably empty-dataset-safe with honest groups and footer. Files touched: the 9 files in my ownership scope only.
- Risks/notes: (1) inlined-English honesty strings bypass useT() by design — if i18n.ts is ever purged of demo copy, those call sites can switch back to keys; (2) ACTIVITY is a permanently-empty static array, so activity.tsx always shows the zero state (honest; the view has no user-event source yet); (3) activity/case-detail still import DEMO_NOW for deterministic time bucketing — module-load-time "now", harmless for user-generated records; (4) palette ask rows use label-matching branches — new idle suggestions must be added to the explicit ask-condition list or they fall through to ask(query).

---
Task ID: 12 (main integration — fashion layer restore + full Round-10 execution)
Agent: main
Task: User report: "the UI is wrong — what happened to all the content we implemented such as circulating glows… bring back that UI" + full Round-10 list (rename confirmation, split-screen auth, declutter, map improvements, remove ALL fake results, bug-free, closable sidebars, advanced navigation).

Work Log:
- DIAGNOSIS: the Round-7 fashion layer (circulating glow system, glass buttons, serif/eyebrow/hairline language, hero background word) was LOST in a later globals.css rewrite and never git-committed (verified: zero `sasi-btn-glow`/`sasi-search-glow` refs in any of the 11 historical commits). UI rendered plain. Rebuilt from scratch rather than restored.
- FASHION LAYER REBUILT (globals.css, +260 lines): `@property --sasi-glow-angle` + `sasi-glow-rotate` keyframes driving ALL conic effects; `.sasi-search-glow` (+ `-live` variant) circulating national-light ring on focus-within, also upgraded onto existing `.sasi-command-focus`; `.sasi-btn-glass` / `.sasi-btn-white-glass` glass buttons with tri-color hover glow; `.sasi-serif`, `.sasi-eyebrow` (hairline-flanked), `.sasi-line`; `.sasi-hero-word` — giant outlined serif SASI with a conic national light circulating THROUGH the letterforms (Round 9's unfinished ask, now done); `.sasi-ambient`; fully transparent scrollbars that appear on hover; custom `[data-slot=select-*]` + popover/command panel glow styling; all reduced-motion guarded.
- LANDING (landing.tsx, 622→490 lines): hero gains the background SASI word + eyebrow/serif/glass treatments + always-on live glow on the command bar; REMOVED all three fake-data sections (Live service intelligence demo cards, sample-investigation DEMO_TIMELINE workflow preview, Latest service information demo list); added conditional "Your recent cases" section — renders ONLY real user cases ("Created by you on this device — nothing is sampled, simulated or pre-filled"); fixed a StatusBadge→CaseStatusBadge crash found by browser QA.
- FAKE DATA PURGED AT SOURCE (Task 12-d, survived an infra crash mid-flight — state verified complete on disk): data.ts INCIDENTS/SOURCES/CASES/NOTIFICATIONS seeds → []; store hydrate seeds empty; fresh sessions verified 0/0/none; briefing system prompt grounds ONLY in the resident's own reports/chat; API state route no longer seeds demo content. Contract held: every export/signature identical.
- AUTH (12-a): public-shell renders login/signup BARE (no top nav, no footer); both auth views rebuilt as split screens — left brand panel (sasi-hero-word, ambients, eyebrow, serif headline, "Not a government website" honesty), right minimal form with sasi-search-glow field shells + white-glass submit; all prior auth logic preserved; mobile single-column.
- DASHBOARD + SIDEBAR (12-b): dashboard rebuilt minimal — real-clock serif greeting, honest one-line status, exactly 2 dominant actions (Report / Ask), max 3 data-gated sections, one premium "Nothing here yet" empty state, max-w-3xl calm column; stat tiles/demo sections/DemoBadge removed. Sidebar gained a persistent collapse toggle (228↔60px animated, localStorage "sasi-sidebar-collapsed", icon rail with 44px targets + title labels, unread gold dot, "Demo account"/"DEMO" chips removed).
- MAP (12-c partial, verified working): honest empty-state overlay "Nothing plotted yet — SASI never invents map data" + Report CTA over dark Google tiles; improved marker plumbing (its scratch file tool-results/map_new.tsx removed to keep lint/tsc green).
- VIEWS HONESTY (12-e2 after first 12-e agent died on infra): cases/activity/notifications/services/service-detail/incident-detail/case-detail/ask-sasi/command-palette — every DemoBadge + demo/sample copy removed or reworded; print/clipboard footers now "(not a government submission)"; ask-sasi honesty line "AI-assisted — unverified. Grounded only in what you provide."; fake CASE-000123 suggestion + hardcoded case-123 palette row removed; premium zero-states for cases/activity/service-detail-grid; palette hides empty groups; sasi-search-glow on ask composer/services/palette searches; eyebrow/line polish.
- INCIDENTS (main): DemoBadge ×2 + "0 demo" counter removed; i18n keys updated honestly (stat hint "Resolved and closed"; empty state "No incidents to show — appear only once reported through SASI — nothing is pre-filled" with fresh zu/af).
- QA: bun run lint 0 errors 0 warnings; tsc clean for src/; fresh-session sweep navigated incidents/cases/activity/notifications/services/ask-sasi/map/dashboard — ZERO console errors; verified fresh session = 0 cases/0 notifications/no briefing; screenshots confirm map overlay, minimal dashboard, split-screen auth, hero glow word; dev.log all 200s.

Stage Summary:
- The full Round-10 brief is now implemented: brand = South African Service Intelligence everywhere; circulating-glow fashion language restored and applied site-wide; auth = chrome-free split screens; dashboard + site decluttered to a calm user-generated-only tool; map honest + improved; ALL fake/simulated results removed at the data source with premium honest empty states everywhere; sidebar collapses and persists; lint/tsc/console all clean.
- Files: globals.css, landing.tsx, incidents.tsx, i18n.ts (inc.* keys), public/manifest.json+layout (prior), + via agents: public-shell, login, signup, app-shell, dashboard, map, data.ts, store.ts, briefing-card, domain, briefing route, cases, activity, notifications, ask-sasi, services, service-detail, incident-detail, case-detail, command-palette.
- Unresolved/risks: (1) 12-c died mid-task — marker icon refinement may be partial (current markers functional; visual upgrade of per-service glyph tiles NOT confirmed in-browser — next round should finish and visually verify); (2) legacy sessions (old browser sessions with persisted demo rows) may still render old cases — by design, new sessions are clean; (3) briefing auto-gen behavior on empty dashboards changed — verify the "no material yet" path once a real report exists; (4) 12-a/12-b QA'd their own views (self-reported pass); (5) system serif stack used instead of a loaded display face (network-safe choice) — revisit if a brand serif is wanted.
- Recommended next-phase priorities: (1) finish map marker icon craft + popup redesign + verify focus jumps in-browser; (2) end-to-end golden path test with REAL user data: report → case → chat → pin briefing → map marker; (3) reconnect re-sync + 390px sweep (carried); (4) consider loading a true display serif via next/font when network allows.

---
Task ID: 13 (rebranded phase 3 restore)
Agent: main
Task: User instruction — "load the rebranded phase 3 not this one… custom UI elements, custom top navigation bar, transparent scroll bar, dot background not grid, liquid glass buttons, rounded UI interface, and much more."

Work Log:
- DIAGNOSIS: the "rebranded phase 3" state survived in the session snapshot /tmp/my-project: 11 files lost from the working tree (never git-committed, matching the Task-12 loss pattern) — gsap-runtime.tsx (GSAP 3.15 scroll layer: reading-progress bar, card rise-ins, ambient parallax, word reveals, hairline growth), views/gov.tsx (honest Government pitch page), lib/auth.ts (NextAuth v4 credentials + JWT, bcrypt cost 12, timing-safe dummy compare), lib/sasi/api-auth.ts (requireUserId + rate limiter), lib/sasi/submission.ts (honest PREPARATION-ONLY submission seam, empty adapter registry), lib/sasi/explainer.ts (paste-ready honest explainer), lib/sasi/mobile.ts (haptics/share), api/auth/[...nextauth], api/sasi/auth/signup (zod + bcrypt + audit), api/sasi/audit (GET/POST allow-listed), api/sasi/submission (GET capabilities / POST preparation record).
- RESTORED all 11 files; deps installed (gsap@3.15.0, bcryptjs, @types/bcryptjs; next-auth already present); .env + NEXTAUTH_SECRET + NEXTAUTH_URL.
- PRISMA: +User (bcrypt passwordHash, unique email), +AuditEvent (server-stamped, allow-listed client events), +SubmissionRecord (NOT_CONNECTED-only), CaseRecord.userId nullable + index; db:push OK; dev server RESTARTED (documented lesson).
- WIRED: sasi-app mounts <SasiGsapRuntime/> (global) + registers gov (PUBLIC_VIEWS, VIEW_COMPONENTS); types View + "gov"; store ALL_VIEWS + "gov"; public-shell NAV gains Government (i18n landing.nav.gov en/zu/af); app-shell sidebar gains Government link (Landmark icon, collapse-aware); palette Navigation gains Government; profile sign-out now ALSO clears the NextAuth JWT session (signOut redirect:false).
- REAL AUTH: login submit → signIn("credentials", {redirect:false}) with honest inline error states (wrong password verified live); signup submit → POST /api/sasi/auth/signup then auto credentials sign-in; both set store.accountName (NEW: persisted localStorage sasi.accountName, restored in hydrate) from the real account; dashboard greeting uses accountName ?? neutral "You" — verified live: "Good afternoon, Thabo Mokoena." after real login.
- UI (the user's described phase-3 look): .sasi-grid-bg converted to a DOT matrix (22px radial dots — dots, not grid, zero view edits needed); .sasi-btn-glass/.sasi-btn-white-glass upgraded to LIQUID glass (blur(18px)+saturate(160%), dual-layer gradients, inset specular top highlight, elliptical ::after sheen, tri-color national halo hover); public-shell header rebuilt as CUSTOM GLASS TOP NAV (blur(24px)+saturate(160%), hairline, transparent-over-landing, nav links inside a glass pill with active inset state, white-glass pill CTA); scrollbars TRANSPARENT-until-hover page-wide (html hairline thumb + .sasi-scroll fully transparent until hover/focus); .sasi-card radius bumped to 1rem (rounded UI); + GSAP support CSS (.sasi-scroll-progress 2px national gradient bar, .sasi-wmask/.sasi-word masked word reveals, reduced-motion guards); footer honesty line "DEMO ENVIRONMENT" → "HONEST BY DESIGN — NOTHING IS SUBMITTED WITHOUT YOUR APPROVAL".
- QA: lint PASS (0/0); tsc clean for src/; agent-browser: landing shows dots + glass pill nav (5 buttons) + liquid glass CTAs + hero word glow; gov view renders with active nav pill; REAL signup created User + server-stamped ACCOUNT_CREATED AuditEvent (verified in SQLite) and auto-signed-in; wrong password → honest inline error; correct password → dashboard with real name; GSAP progress bar mounted; no horizontal overflow at 1280px (dashboard+map); dev.log all 200s incl. /api/auth/* (csrf → callback/credentials → session).
- COMMITTED TO GIT (lesson from the phase-3 loss: uncommitted work dies on session restore) — commit "Task 13: restore rebranded phase 3 …".

Stage Summary:
- The rebranded phase 3 is live again: GSAP motion layer, honest Government page, REAL email+password accounts (bcrypt/JWT, server-stamped audit trail, honest submission seam, haptics, explainer) and the described custom UI — dot background, liquid glass buttons, custom glass top navigation, transparent scrollbars, rounded surfaces. Everything committed to git this time.
- Files: +src/components/sasi/gsap-runtime.tsx, +src/components/sasi/views/gov.tsx, +src/lib/auth.ts, +src/lib/sasi/{api-auth,explainer,mobile,submission}.ts, +src/app/api/auth/[...nextauth]/route.ts, +src/app/api/sasi/{auth/signup,audit,submission}/route.ts; prisma/schema.prisma (+3 models, CaseRecord.userId); package.json (gsap, bcryptjs); .env (NEXTAUTH_*); src/app/globals.css (dots, liquid glass, scrollbars, scroll-progress, wmask/word, radius); src/components/sasi/{sasi-app,public-shell,app-shell,command-palette}.tsx; views/{login,signup,dashboard,profile}.tsx; lib/sasi/{store,types,i18n}.ts.
- Unresolved/risks: (1) profile view still renders the neutral DEMO_USER identity (name/email "You") — wiring real account email/name into Profile + cases attribution is the natural next step; (2) data-layer routes (state/ask/vision/briefing) remain session-keyed by design — migrating them to userId ownership is phase-4 scope (the submission/audit routes already enforce real sessions); (3) password reset not built (explainer says so honestly); (4) gov view is English-only inline (consistent with other detail views); (5) next-auth v4 on Next 16 — providers/session endpoints verified live, but the library is in maintenance mode; consider Auth.js v5 in a future round.
- Recommended next-phase priorities: (1) profile + identity: show real account name/email, attribute cases to the signed-in user; (2) migrate /api/sasi/state to userId ownership with session fallback; (3) finish map marker glyph tiles (12-c leftover); (4) golden-path test: signup → report → investigate → briefing → map with the real account; (5) 390px mobile sweep of the new top nav + gov view.

---
Task ID: 14
Agent: main
Task: User instruction — "IMPROVE SPLASH SCREEN AND AUTH PAGES" (continue phase-3 fashion language: dots, liquid glass, rounded UI, custom top elements).

Work Log:
- CSS (+~200 lines, globals.css new "SPLASH + AUTH FASHION LAYER (Task 14)" section): .sasi-dot-veil (22px dot matrix with radial edge melt), .sasi-boot-ring (rotating conic national-light orbit on @property --sasi-glow-angle) + .sasi-boot-orbit (faint static track), .sasi-boot-plate (liquid glass slab, blur(24px)+saturate(160%), specular top), .sasi-boot-bar (indeterminate national-gradient sweep, sasi-boot-sweep keyframes), .sasi-auth-card (liquid glass form card, radius 24px), .sasi-auth-nav (floating blurred top bar scrim), .sasi-principle-tile (glass icon square); all reduced-motion guarded.
- SPLASH REBUILT (sasi-app.tsx): fixed z-80 boot layer — dot veil, red/blue/gold breathing ambients, glass boot plate holding the mark inside the circulating ring orbit, SASI letters staggered rise-in, hairline scaleX reveal, "SERVICE INTELLIGENCE" eyebrow, national-gradient boot bar + honest line "Preparing your workspace — everything stays on this device.", pinned "NOT A GOVERNMENT WEBSITE" floor line; boot window 650ms→950ms; app now mounts BENEATH the splash and AnimatePresence exits the splash with blur+scale fade so boot hands over gracefully (role=status aria-label).
- AUTH SHARED SHELL (NEW src/components/sasi/auth-brand.tsx): AuthTopBar (floating glass bar across both panels — logo→landing, "NOT A GOVERNMENT WEBSITE" chip, sasi-btn-glass "Back to home" pill), AuthBrandPanel (accent red|gold; dot veil, sasi-hero-word, ambients, eyebrow, serif headline, NEW liquid-glass principles plate: Report/Investigate/Approve with glass icon tiles — honest copy, no fake data, bottom national-dot caption), shared AuthGlowField (rounded-xl) + AUTH_FIELD_INPUT.
- LOGIN REBUILT (views/login.tsx): form now lives in a sasi-auth-card glass card; password show/hide toggle added; error/success chips rounded-xl inside the card; links + terms + honesty moved under the card; STALE COPY FIXED — "Demo environment — any credentials work." was false after Task 13 real auth → now "Real accounts — passwords are verified server-side, never stored in plain text."; forgot-password copy cleaned; duplicate mobile logo header + bottom back-to-home removed (AuthTopBar covers both).
- SIGNUP REBUILT (views/signup.tsx): gold accent panel; REAL LOCAL PASSWORD STRENGTH METER (4 segments, national colors, labels Too short→Excellent, aria-labelled, display-only honesty note — server zod+bcrypt remains the enforcement); STALE COPY FIXED — removed "Accounts are local to this demo / NOT SENT ANYWHERE" (false: accounts persist server-side) → "Passwords are hashed with bcrypt — SASI never sees or stores the plain text." + "Your reports stay on this device until you choose to act."; all NextAuth/signup logic untouched.
- QA (agent-browser): splash captured mid-boot — dots + glass plate + rotating national ring + staggered wordmark all visible; post-splash landing intact; login — floating top bar, brand panel, glass card render, WRONG-PASSWORD inline error verified live (NextAuth 401 → honest chip); signup — strength meter shows STRONG (3/4 green) for "Str0ng!Pass", focus glow ring visible; 390px mobile login — top bar adapts, no horizontal overflow (scrollWidth check passed); console errors: ZERO; dev.log all 200s (only intentional 401 from the wrong-password test); bun run lint PASS.
- COMMITTED TO GIT per Task-13 lesson (work must survive session restores).

Stage Summary:
- Splash + auth now fully speak the rebranded phase-3 language: dot backgrounds (not grid), liquid glass plates/buttons, custom floating top bar, circulating national light, rounded surfaces — plus two honesty upgrades (stale demo-auth copy replaced with real-auth truth on both pages) and a new real password strength meter.
- Files: src/app/globals.css, src/components/sasi/sasi-app.tsx, +src/components/sasi/auth-brand.tsx, src/components/sasi/views/login.tsx, src/components/sasi/views/signup.tsx.
- Unresolved/risks: (1) at very short desktop viewports (<~600px height) the brand panel's principles plate can clip (overflow-hidden) — acceptable, mobile unaffected; (2) boot window is now 950ms — deliberate branding tradeoff, PWA repeats are cached; (3) carried items from Task 13 remain: profile real-identity wiring, state-route userId migration, map marker glyph tiles, golden-path test with real account, 390px sweep of gov/top nav.
- Recommended next-phase priorities: (1) wire real account name/email into Profile view + case attribution; (2) finish map marker per-service glyph tiles (12-c leftover); (3) golden-path: signup → report → investigate → briefing → map under the new auth UI; (4) apply sasi-auth-card language to other modal/flat forms (report wizard header, settings sections) for fashion consistency.

---
Task ID: 15-c
Agent: full-stack-developer
Task: Map geolocation — locate control, pulsing accuracy marker, follow mode.

Work Log:
- Read worklog (Tasks 12/13/14) + map.tsx in full (1525 lines) — mirrored existing patterns: refs (mapRef/LRef), Effect A's cancelled-flag StrictMode guard, imperative marker updates (Effect B style), scoped <style> injection inside the component JSX.
- LOCATE CONTROL: added a real-device GPS button (Lucide LocateFixed, sasi-btn-glass, h-11 w-11 = 44px target, rounded-full) as the 4th control in the right-side cluster, which is now a unified glass pill (border-white/12, bg-gradient white/[0.06]→[0.02], backdrop-blur-md, shadow, rounded-full, role="group" aria-label="Map view controls"); zoom/recenter buttons became pill ghosts (same 44px targets, hover:bg-white/10). Placement unchanged: right-4 under the mobile filter chip / bottom-right desktop, clear of filters/legend/panels.
- STATE MACHINE: GeoPhase idle → locating → fixed → following. Click 1: navigator.geolocation.getCurrentPosition (enableHighAccuracy, 15s timeout) → store real fix in geoFixRef → L.divIcon pulsing dot + L.circle accuracy ring → map.flyTo([lat,lng], 16, {duration:1.2}). Click 2 (from fixed): start follow. Click 3 (from following): stop — clearWatch + remove marker+circle (back to idle, per spec).
- MARKER: sasi-geo-wrap 14px white core (2.5px #64b5f6 border) + sasi-geo-pulse expanding blue ring (@keyframes sasi-geo-ping 2.1s), non-interactive, zIndexOffset 500; accuracy ring exactly #64b5f6 / weight 1 / opacity 0.35 / fillOpacity 0.08 (8m display floor). All styles in a NEW SASI_GEO_CSS const injected via a second <style> tag in map.tsx — globals.css NOT touched (zero conflict with concurrent editor). prefers-reduced-motion: pulse disabled (static faint ring), locating breathe disabled.
- FOLLOW MODE: Effect H runs watchPosition only while phase === "following"; cleanup ALWAYS clearWatch (toggle-off, error, unmount, StrictMode re-run). Watch fixes update dot/ring imperatively + re-pan only after >30m movement (panTo 0.75s) to avoid camera jitter. Button gets sasi-geo-following live blue glow (#64b5f6, sasi-search-glive-style, two-class specificity beats .sasi-btn-glass), aria-pressed, title "Following — click to stop", plus a small role="status" "FOLLOWING" glass chip above the cluster.
- HONESTY: errors show a dismissible role="status" glass chip above the cluster with exact copy — denied → "Location permission was denied — SASI can't center on you until it's allowed in the browser."; unavailable/timeout/unsupported → "Your location is unavailable right now." Never a fake position (marker only ever renders from a real GeolocationPosition). Permanent microcopy next to the control (text-[10px] zinc-500, text-shadow for tile legibility): "Location is used only on this device."
- STRICTMODE: Effect F (mount/unmount guard) — geoDisposedRef mirrors Effect A's cancelled flag so late geolocation callbacks never touch a dead map; Effect F cleanup nulls layer refs; Effect G re-creates dot+ring keyed on [mapReady, geoPhase] so a remounted map re-anchors the LAST REAL fix; Effect H's clearWatch cleanup runs on every re-run. Denied mid-watch → stops following, drops to last honest fix + chip.
- QA: bun run lint → 0 errors 0 warnings; tsc --noEmit → 0 errors in src/ (4 pre-existing errors only in scaffold examples/skills folders, untouched); dev.log shows clean "✓ Compiled" + all 200s. Only map.tsx modified.

Stage Summary:
- The full map view now has real device geolocation: one honest GPS fix with pulsing dot + accuracy ring + flyTo(16), toggleable follow mode with live watch, blue glow + FOLLOWING tag, honest per-type error chips and on-device-only microcopy. All state transitions clean up after themselves (watch, marker, circle) and are StrictMode-safe.
- Files: src/components/sasi/views/map.tsx ONLY (+ this worklog). No new routes, no globals.css edits, no other views touched.
- Risks/notes: (1) main agent should QA visually — geolocation in headless/preview browsers may auto-deny; the honest "unavailable" chip is the expected path there; (2) accuracy circle can be large on wifi-only devices (raw honest meters, 8m min display floor); (3) follow-mode re-pan threshold is 30m — walking speed will pan gently, is deliberate; (4) if another agent restyles the map cluster, the pill classes live in the "map control cluster" JSX block ~line 1646 of map.tsx.

---
Task ID: 15
Agent: main (+full-stack-developer for 15-c)
Task: User instruction — "MAKE ALL BUTTONS HAVE CIRCULATING OUTLINE GLOW AND IMPROVE ALL CIRCULATING OUTLINE GLOWS TO MAKE THEM MORE VISIBLE. Improve all search bars 100x including all pop up menus. Improve left side of the auth pages. Make the map have geolocation to track real positioning. Improve all website features."

Work Log:
- UNIVERSAL BUTTON RINGS (globals.css Task 15 layer): shared rotating conic ::before ring (brighter stops up to alpha .95, 1.5px, inset -2px) applied to `.sasi-btn-glass`, `.sasi-btn-white-glass`, `.sasi-btn-ring` (new class added to PrimaryButton + GhostButton primitives) and ALL shadcn `[data-slot="button"]` (subtle 1px/0.28 rest). Always-on at rest, hover accelerates (2.4–3s) + brightens to ~1 with gold drop-shadow; focus-visible full; disabled dims to 0.14 and pauses. Glass button sheen moved from ::after into the background stack so ::before owns the ring; overflow now visible. Reduced-motion: ring frozen at 0.35.
- RING VISIBILITY BOOST everywhere: `.sasi-search-glow`/`.sasi-command-focus` ring thickened 1→1.5px, stops to ~0.9–0.98 alpha, focus rotation 4.5s→3.2s + gold drop-shadow; live variant opacity .3→.5, 9s→6.5s; focus adds national tri-color halo box-shadow.
- SEARCH BARS 100X: uniform liquid-glass shells on every search host (gradient bg, blur(14px)+saturate(150%), inset top highlight, hover border lift, focus national halo) — applies to hero command bar, palette trigger, services search, ask composer, auth fields automatically via the shared classes.
- POPUP MENUS 100X: select/popover/dropdown/command panels — radius 16px, deeper glass blur(28px)+saturate(170%), inset highlight + blue ambience shadow, sasi-menu-in pop animation (reduced-motion guarded); triggers got glass treatment; ALL items (select/command/dropdown) rounded 10px with a NATIONAL GRADIENT LEFT BAR that fades in on hover/highlight.
- AUTH LEFT PANEL (auth-brand.tsx): serif headline now ends in an ITALIC national-gradient "South Africa."; principles rows numbered 01/02/03 (serif) with per-row accent tiles (red/blue/gold + tinted glow) and hover lift; new TRUST CHIPS row (Local-first / Approval-gated / AI-assisted — unverified) under a hairline; corner orbit ornament (static track + slow 9s national ring) top-right.
- MAP GEOLOCATION (Task 15-c by subagent, map.tsx only): glass control cluster bottom-right (zoom +/−, recenter, NEW Locate-me, 44px targets); getCurrentPosition → pulsing blue divIcon dot + accuracy circle → flyTo 16; FOLLOW MODE state machine idle→locating→fixed→following (watchPosition, 30m re-pan threshold, aria-pressed, FOLLOWING live tag); StrictMode-safe guards; honest error chips verbatim ("Location permission was denied — SASI can't center on you until it's allowed in the browser." verified live); microcopy "Location is used only on this device."; styles injected via component <style> (globals.css untouched — zero conflict).
- INFRA LESSON: appending to globals.css via bash `cat >>` does NOT trigger the turbopack watcher (stale CSS served ~10min of debugging) — an Edit-tool write fires recompile. Recorded here so future agents never lose time on this.
- QA: computed-style proof — white-glass CTA ring {op .42, anim sasi-glow-rotate, conic live}; search ring {op .5, pad 1.5px}; palette dialog shows full glass + ring (screenshot); login left panel verified visually (gradient italic, numbered rows, chips, orbit, ringed top-bar pill + Continue); map locate click in headless → honest denied chip rendered, marker/circle logic lint+tsc clean; signup 390px — no horizontal overflow; console/page errors ZERO; bun run lint PASS.

Stage Summary:
- The national light now runs through every interactive surface: all buttons carry a rotating conic outline (dim at rest, alive on hover), every search field is a liquid-glass shell with a brighter/faster circulating ring, every popup menu is deep glass with national hover bars; auth left panel is a designed brand column; the map gained real device geolocation with follow mode and honest failure states.
- Files: src/app/globals.css, src/components/sasi/primitives.tsx, src/components/sasi/auth-brand.tsx, src/components/sasi/views/map.tsx (15-c).
- Unresolved/risks: (1) ring pseudo-elements double up if a future component stacks sasi-btn-glass INSIDE a data-slot button (avoid nesting buttons); (2) geolocation happy path untestable headless (denied by design there) — manual real-browser check recommended; (3) carried: profile real-identity wiring, state-route userId migration, map marker glyph tiles, golden-path run, zu/af proofing.
- Recommended next-phase priorities: (1) wire real account name/email into Profile + case attribution; (2) golden-path: signup → report → investigate → briefing → map under real account; (3) per-service marker glyph tiles (12-c); (4) optional: respect prefers-reduced-transparency for glass layers.

---
Task ID: 16
Agent: main
Task: User instruction — remove hero "SASI" text; one font everywhere + bolder; splash = icon logo only, big, centered, 7s; CGO on hover for buttons AND content; thicker/more noticeable CGO; smoother + easier navigation; mobile support; super secure + save data for low-data users; keep it simple for South Africans.

Work Log:
- HERO WORD REMOVED: giant outlined "SASI" background word deleted from landing.tsx hero AND auth-brand.tsx brand panel; `.sasi-hero-word` CSS block + its reduced-motion reference removed from globals.css (no dead code).
- ONE TYPE FOR EVERYTHING (globals.css Task 16 layer): `.sasi-serif` redefined → `font-family: inherit; font-weight: 700` (Georgia serif gone site-wide); `kbd/code/pre/samp/.font-mono` → inherit (Inter everywhere, verified computed: serif/mono/body all "Inter"); body default weight 400→500 ("more bold" for all plain text), b/strong→700, `.sasi-eyebrow`→600.
- SPLASH REBUILT (sasi-app.tsx): ONLY the logo mark, scaled BIG (132px tile mobile / 152px sm, 96px logo) dead-center inside the thicker circulating national ring + orbit + gold ambience; wordmark letters, boot plate text, eyebrow, hairline all removed; duration 950ms → 7000ms (BOOT_MS const); HONEST SKIP — tap anywhere or Escape/Enter/Space skips boot ("Tap anywhere to skip" hint); keydown listener cleaned up; exit upgraded to blur(8px)+scale(1.06).
- CGO v2 (globals.css): buttons' rings now HIDDEN AT REST (opacity 0, transition 0.28s) and SNAP ON hover/focus — user asked for hover-driven; thickness 1.5px@-2px → 2.5px@-3px; conic stops pushed to full alpha (red 1.0, gold 1.0, blue 0.95); hover rotation 2.2s + double drop-shadow halo (gold+blue, buttons) / (gold+red, shadcn buttons); disabled re-asserted to 0.12 after new rest state; reduced-motion = static ring on hover, none at rest.
- CGO ON CONTENT: `.sasi-card` gained the same conic ring via ::before (2px@-2px, rest 0) — hover 0.55, focus-within 0.85, interactive cards 0.9 + 3.2s spin; `.sasi-search-glow`/`.sasi-command-focus` hover whisper 0.4 (focus still 1.0); verified computed: card hover opacity 0.9, white-glass button hover opacity 1 @2.2s, rest 0 for both.
- SMOOTHER NAVIGATION: store.navigate() now window.scrollTo(0,0) on every view change (verified: scrolled 1200px → navigate → scrollY 0); no more landing mid-page in a new view.
- MOBILE SUPPORT: bottom-nav buttons → min-h-[56px] (44px+ target), icons 21px, labels 10px font-semibold, press-scale feedback; Topbar bell + avatar buttons → 40px on mobile (36px desktop via lg:).
- DATA SAVER (the "save data for users who use data" ask): new src/lib/sasi/data-saver.ts (read/write/apply/init, localStorage `sasi.data-saver`, toggles `sasi-data-saver` on <html>); SasiApp calls initDataSaver() before first paint; Settings → Data section gained a DataSaverCard — one simple Switch, plain-language copy ("Made for small data bundles… Your cases, evidence and settings stay exactly the same"), live "SAVING DATA" status, toast on change; CSS layer kills ALL backdrop-filter (!important), ambient/dot-veil/animation, every glow ring (display:none), scroll progress — content and data untouched; verified live: toggle → html class + rings display:none, reset after test.
- SECURITY HARDENING (next.config.ts): headers() on all routes — X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy camera=()/microphone=()/payment=()/geolocation=(self); dev server restarted, all 4 verified live via curl -I.
- SIMPLICITY: bigger touch targets, shorter honest labels, tap-to-skip boot, hover-only glow (calmer at rest = less visual noise), plain-language Data Saver copy.
- QA (agent-browser): splash @2.5s = only big logo + ring + skip hint + honesty line (screenshot); tap-skip → landing without hero word; hover CGO computed (btn 1.0/2.2s, card 0.9) + screenshots; fonts computed Inter/700/500; mobile 390px dashboard + bottom nav + Data Saver card screenshots, horizontal overflow 0px; Escape skips splash; login page renders clean post-edit; console errors ZERO; dev.log all 200s; bun run lint PASS (fixed one set-state-in-effect → lazy useState init).

Stage Summary:
- The site speaks with one bold Inter voice, boots on a single big centered mark for exactly 7s (skippable), shows the circulating national light only when you reach for it (buttons, cards, search) at a thicker 2.5px, starts every view at the top, works properly on phones (56px bottom nav, 40px topbar targets), and gained a real Data Saver + baseline security headers.
- Files: src/app/globals.css, src/components/sasi/sasi-app.tsx, src/components/sasi/views/landing.tsx, src/components/sasi/auth-brand.tsx, src/lib/sasi/store.ts, src/lib/sasi/data-saver.ts (new), src/components/sasi/views/settings.tsx, src/components/sasi/app-shell.tsx, next.config.ts.
- Unresolved/risks: (1) 7s boot is long for repeat visits — the tap/key skip mitigates; watch feedback; (2) blanket backdrop-filter removal under Data Saver may look flatter on some panels (by design, honest tradeoff); (3) ring ::before on .sasi-card could collide with a future component that needs card ::before (none today); (4) carried: profile real-identity wiring, state-route userId migration, map marker glyph tiles, golden-path run with real account, zu/af proofing.
- Recommended next-phase priorities: (1) golden-path: signup → report → investigate → briefing → map under real account; (2) wire real account name/email into Profile + case attribution; (3) per-service map marker glyph tiles; (4) consider "reduce transparency" respect for glass layers (prefers-reduced-transparency).

---
Task ID: 17
Agent: main
Task: User instruction — "start the server and also please improve AI chat interface". Dev server confirmed running (port 3000, all 200s). Full Ask SASI chat upgrade.

Work Log:
- STOP GENERATION (store.ts): new `stopSasi()` via module-level AbortController; abort → honest partial kept + italic "*(Stopped — this answer may be incomplete.)*" (or "Stopped before SASI started answering.") appended, state → done, snapshot mirrored. Verified live: stop clicked mid-stream, partial + note rendered, busy cleared.
- SERVER RACE FIX (api/sasi/ask/route.ts): `push()` now try/catches controller.enqueue and flips `closed` on failure — client aborts during streaming no longer throw "Invalid state: Controller is already closed" (was visible in dev.log).
- RICHTEXT v3 (rich-text.tsx, shared by chat + briefing + evidence): `*italic*` (zinc), `##`/`###` headings (bold white), `1.` numbered lists (gold number tiles), `> ` quote blocks (gold hairline) added alongside bold/bullets/ref-chips. Italic split regex is `**`-safe.
- COMPOSER REBUILT (ask-sasi.tsx): fixed input → auto-growing textarea (1→~5 lines, max-h 120px, thin scrollbar); Enter=send · Shift+Enter=newline (hint shown under composer); send button morphs into a STOP square while streaming; char counter appears only near the 500 limit (gold); h-10 44px+ targets kept.
- MESSAGE UPGRADES: timestamps (hh:mm, tabular) under both bubbles; error state now renders a "Try again" chip that re-asks the failed question (verified via injected error message); done answers get Copy + Helpful/Not-helpful thumbs (local-only, honest "Noted on this device" toast) — thumbs always visible on touch (hover-reveal on desktop only); user bubble upgraded to gradient glass.
- CONVERSATION: "TODAY" hairline divider; scroll-to-bottom "Latest" pill (sasi-btn-ring CGO, appears when >260px from bottom, verified appear→click→gone); empty state now uses the real SasiLogo mark in a glowing tile instead of a generic sparkles icon; suggestion cards rebuilt with national accent tiles (red/blue/green/gold/plain) on sasi-card (inherit hover CGO) + 52px min touch height.
- LAYOUT BUG HUNT (the deep one): the chat's fixed-height column NEVER worked — (a) Tailwind 4 silently dropped `h-[calc(100vh-3.5rem-1.25rem-2rem)]` (multi-term calc without spaces is invalid; single-term like map's `calc(100dvh-3.5rem)` gets auto-normalized — underscore form `h-[calc(100vh_-_3.5rem_-_1.25rem_-_2rem)]` generates correctly); (b) `flex-1` (basis 0) on the column made any height irrelevant (zero flex-shrink weight in the flex algorithm); (c) wrapper `h-full` resolved against an indefinite height (parent capped by max-h). FINAL STRUCTURE: column = definite `h-[calc(...)]` (no flex-1) with shrink-0 header + composer, wrapper = `relative min-h-0 flex-1`, log = `absolute inset-0 overflow-y-auto`. Verified: colH exactly viewport-derived (469 @ vh577), log internally scrollable, page scroll gone.
- HARDENING: empty-state `min-h-full` + pb-28 so cards always scroll clear of the composer on short viewports (agent-browser click-coverage issue fixed); globals.css chat layer (textarea scrollbar + data-saver kills caret).
- QA: agent-browser full pass — suggestion click → streamed answer (real LLM, 4 paragraphs, bullets render gold); stop mid-stream → honest partial; retry chip; jump pill; Enter-to-send; mobile 390×844 → 0 horizontal overflow, bottom nav clear, log internally scrolls; persisted history restored on reload (8 msgs server-side); console + page errors ZERO; bun run lint PASS; tsc src/ PASS.

Stage Summary:
- Ask SASI now feels like a real product chat: auto-growing composer with Enter/Shift+Enter, send-morphs-to-stop, honest partial answers, retry on error, per-message timestamps + local feedback thumbs, Today divider, Latest jump pill, brand-logo empty state with accent-coded starters — and the view finally has a true fixed-height internally-scrolling layout (three stacked CSS bugs found and fixed).
- Files: src/lib/sasi/store.ts (stopSasi + AbortController), src/app/api/sasi/ask/route.ts (enqueue race fix), src/components/sasi/rich-text.tsx (v3), src/components/sasi/views/ask-sasi.tsx (full rebuild), src/app/globals.css (chat layer).
- Unresolved/risks: (1) agent-browser innerHeight (577) < CSS vh (720) — desktop screenshots show composer overlap that real browsers won't; internal-scroll math verified at true values; (2) stop leaves the server reading upstream until completion (persistence intact, cost only); (3) suggestion truncation on 1-line cards by design (aria-label carries full text).
- Carried from 15/16: profile real-identity wiring, state-route userId migration, map marker glyph tiles, golden-path run under real account, zu/af proofing.
- Recommended next-phase priorities: (1) golden-path: signup → report → investigate → briefing → map under a real account; (2) wire real account name/email into Profile + case attribution; (3) per-service map marker glyph tiles; (4) follow-up quick-reply chips after answers.

---
Task ID: 18
Agent: main
Task: User instruction — "preview refused to connect" (transient; server verified healthy, binds *:3000) + "Refine and polish the website more and make search bars have liquid glass".

Work Log:
- LIQUID GLASS SEARCH v2 (globals.css Task 18 layer): `.sasi-search-glow` + `.sasi-command-focus` rebuilt — blur(22px) saturate(190%) brightness(1.05) refraction, 3-stop glass body + radial tint, dual specular edges (inset top 0.14 / bottom 0.04 / wide 22px inner glow), deep drop shadow; NEW ::after liquid sheen (elliptical specular + diagonal caustic streak, background-position sweep 120%→-20% over 0.9s on hover, rest 0.75 → hover 1.0); liquid lift (translateY(-1px)) + border 0.14→0.24 on hover; focus = deeper national tri-color halo. Applies to ALL 6 search hosts automatically: hero command bar, topbar palette trigger, palette dialog field, services search, chat composer, auth fields.
- ⚠️ CRITICAL BUILD DISCOVERY — Lightning CSS (turbopack minifier) silently DEDUPES dual `backdrop-filter` + `-webkit-backdrop-filter` declarations and keeps ONLY THE LAST ONE. Every previous dual pair was written unprefixed-first → the unprefixed declaration was stripped from the built CSS → computed backdrop-filter was "none" — REAL blur had been dead site-wide (buttons, menus, everything) despite code looking right. Also: minifier emits `::after` as `:after` (don't text-search for `::after`), and CSSStyleRule.cssRules is ALWAYS truthy (empty lists included) when probing CSSOM. FIX: declare `-webkit-` FIRST, unprefixed LAST — swept all 12 occurrences (buttons glass, menu panels, toasts, tab bar, task-18 search, data-saver kill rule). Verified in served CSS: 13 unprefixed + 13 webkit blur rules now coexist.
- Data Saver blur-kill now actually works again (was stripped too): computed `backdrop-filter` goes blur(22px)→none with the toggle. Sheen ::after display:none under saver; transform lift disabled under saver + prefers-reduced-motion.
- Site polish micro-pass: `.sasi-card` gains faint top specular (inset 0 1px 0 white/5%) so all surfaces read as one glass family; national gold `::selection` site-wide.
- INFRA: turbopack watcher went stale for globals.css mid-session (Edit-tool writes not picked up; touch no-op) — full dev-server restart forced recompile. Also agent-browser disk cache served stale CSS chunks after restart — `agent-browser close --all` + fresh session clears it (or fetch with cache:'reload').
- QA: computed hero = blur(22px) saturate(1.9) brightness(1.05) ✓; hover = transform matrix translateY(-1px) + sheen pos -20% + opacity 1 + border 0.24 ✓; data-saver = backdrop none + sheen display none ✓; screenshots: hero (live whisper ring), services search, palette dialog (glass + national top ring), chat composer (focused national ring), login fields (glass + specular) — all render liquid glass; mobile 390px 0 horizontal overflow; console + page errors ZERO; lint PASS; tsc src PASS. One transient CSS syntax error during the sweep (missing semicolon from the swap regex) — caught by dev.log within one request, fixed, server 200.
- Files: src/app/globals.css ONLY (Task 18 layer + 12 declaration-order fixes + data-saver reorder).

Stage Summary:
- Every search surface on the site is now genuine liquid glass — deep refraction blur, dual specular edges, a caustic sheen that sweeps on hover, liquid lift, national focus halo — and the whole glass family (buttons, menus, cards) finally ACTUALLY blurs again after discovering the Lightning CSS dual-declaration stripping bug that had silently disabled backdrop-filter site-wide since Task 15.
- Unresolved/risks: (1) remember the -webkit-first ordering rule for ANY future backdrop-filter declaration in this project; (2) dev.log grows unbounded (tee) — consider rotation; (3) preview gateway "refused to connect" was transient — if it recurs, check `curl localhost:3000` + `ss -tlnp | grep 3000` first.
- Carried: profile real-identity wiring, state-route userId migration, map marker glyph tiles, golden-path run under real account, zu/af proofing, follow-up quick-reply chips.
- Recommended next-phase priorities: (1) golden-path: signup → report → investigate → briefing → map under a real account; (2) wire real account name/email into Profile; (3) per-service map marker glyph tiles; (4) hover CGO audit on remaining interactive tiles.

---
Task ID: 18-b
Agent: main
Task: User follow-up — "preview still refusing to connect; inspect SSE server, wrapper CSS position:relative, build output". Continued Task 18 polish (follow-up quick-reply chips).

Work Log:
- PREVIEW DIAGNOSIS (all three user checkpoints verified healthy — nothing app-side to fix):
  (1) SSE server: POST /api/sasi/ask streams real LLM deltas instantly (curl -N verified token flow); closed-flag guards + cancel() intact; malformed POST returns 400 in <600ms — non-blocking by construction.
  (2) Wrapper CSS: chat log wrapper is `relative min-h-0 flex-1` → `absolute inset-0` child cannot collapse (position:relative confirmed in source).
  (3) Build output: .next/dev freshly compiled; server binds *:3000 AND external IP 21.0.7.38:3000 → 200; /, /manifest.json, /sw.js, /sasi-logo.png all 200; served HTML 32KB with full splash markup; agent-browser render clean (h1, no error dialog, no console errors).
  ROOT CAUSE: stale sandbox preview TUNNEL (server restart at 21:48 orphaned the gateway route) — fix is reloading the preview pane; app side healthy. An agent-browser "white screen" mid-session turned out to be about:blank (open call failed silently after close --all — always verify location.href, not just eval results).
- .env HARDENING: NEXTAUTH_SECRET (openssl rand) + NEXTAUTH_URL added — NextAuth v4 NO_SECRET warning gone; JWT sessions previously used an unstable dev-derived secret → every server restart silently signed everyone out. Server restarted cleanly to pick up env (attempt 1 → 200).
- GOLDEN PATH E2E (real account, agent-browser): signup (POST /api/sasi/auth/signup 201, bcrypt) → auto sign-in (NextAuth credentials 200) → dashboard authed:true + accountName wired → report wizard 7 steps (Water → No water → saved location → Today) → CASE-000001 created → Start investigation → full 7-event agentic script ran to WAITING_FOR_APPROVAL with finding (AI-INFERRED, Medium confidence) + live notification → Ask SASI real streamed answer (594 chars, grounded in Melrose, gold RichText bullets, "Draft a report from this" chip) → map with user's own report plotted as gold "Your reports" marker (real dark imagery, 11 markers).
  NOTE: new accounts now start with ZERO cases by design (data.ts CASES = [], user-generated only) — not a bug; "cases:1 after first report" is correct.
- FEATURE — follow-up quick replies (ask-sasi.tsx): FOLLOW_UP_POOL (6 topic regexes: water/electricity/cases/billing/roads/emergency × 2-4 questions each + 4 defaults); pickFollowUps() dedupes vs asked question, seeds rotation from answer id hash (consecutive answers rotate chips); renders under newest done answer when !chatBusy as "CONTINUE THE THREAD" group (national gradient dot, staggered chip entrance, min-h-9 touch targets, MessageCircleQuestion icon → gold on hover). Zero LLM cost — local heuristics only.
- BUGFIX: last follow-up chip had 9px overlap with floating "Summarise as briefing" pill at scroll bottom (measured via getBoundingClientRect) → messages container gets pb-14; re-verified: 68px clearance.
- QA: lint PASS, tsc src PASS (zero non-example errors), dev.log zero compile/runtime errors, desktop 1280 + mobile 390 no horizontal overflow, no error overlay, two-round chat thread verified (click chip → streams → chips rotate), console errors ZERO.
- Files: src/components/sasi/views/ask-sasi.tsx (follow-ups + pb-14), .env (NEXTAUTH_SECRET/NEXTAUTH_URL), worklog.md.

Stage Summary:
- Preview "refused to connect" conclusively diagnosed as a stale sandbox gateway tunnel, NOT an app defect — all three user-suspected areas (SSE/CSS/build) verified healthy with hard evidence; preview reload re-establishes the route.
- Golden path under a real account is now browser-verified end-to-end for the first time (signup → report → investigate → chat → map).
- Ask SASI closes the conversation loop: answer → contextual follow-up chips → one tap continues the thread, chips rotate per answer.
- Remember: set .value+dispatchEvent does NOT drive React controlled inputs — use agent-browser `type` for form QA.
- Carried: profile real-identity wiring (accountName already flows; email/avatar next), state-route userId migration, map marker glyph tiles, zu/af proofing.
- Next-phase priorities: (1) wire session user email/avatar into Profile + topbar; (2) per-service map marker glyph tiles; (3) hover CGO audit on remaining tiles; (4) dev.log rotation (tee grows unbounded).

---
Task ID: 19
Agent: main
Task: User — "improve closed side bars. make scroll bars transparent" — collapsed sidebar rail redesign + site-wide ghost scrollbars.

Work Log:
- SCROLLBARS — TRANSPARENT SITE-WIDE (globals.css scrollbars block rewritten):
  * NEW global ghost rule: `* { scrollbar-width: thin; scrollbar-color: rgba(255,255,255,0.07) transparent }` + `*::-webkit-scrollbar { 8px }`, track `transparent`, thumb = floating pill (rgba 0.07, border 2px transparent, background-clip content-box), thumb:hover 0.18. This finally covers the many plain `overflow-y-auto` areas (view panels, dropdown lists) that never had `.sasi-scroll`.
  * `.sasi-scroll` = fully invisible until the AREA is hovered/focused (thumb background-color 0 → 0.16, Firefox via scrollbar-color), 8px, floating pill; consolidated the two competing duplicate blocks (old block removed, later block canonical) so they stop fighting.
  * `html` page scrollbar: 0.08→0.18 hairline pill; `#sasi-ask-input` composer thumb also floated (background-clip), duplicate track rule removed; reduced-motion: scrollbar transitions none.
- COLLAPSED RAIL REDESIGN (app-shell.tsx + Task 19 CSS layer):
  * Rail body: new `.sasi-sidebar-rail` — gradient glass body (white 3% → transparent → 1.8%), inner edge lights (inset -1px hairline + top specular + wide soft inner glow), border-right transparent (inset edge replaces flat border); width 60→64px (topbar padding synced).
  * Active item = "lit key": `.sasi-sidebar-rail-active` radial white tile + inset specular ring + outer glow (left indicator bar intentionally dropped in collapsed mode — the glow tile is the indicator; expanded mode keeps its left bar).
  * GLASS PORTAL TOOLTIPS replace native `title` everywhere in the rail: `useRailTip()` hook (mouseenter/leave/focus/blur → getBoundingClientRect) + `RailTip` rendered via createPortal(document.body) so the nav's overflow-y-auto can NEVER clip them; `.sasi-rail-tip` glass pill (blur(12px) saturate(160%) — -webkit FIRST per the Lightning CSS rule, border, shadow, translateY(-50%) slide-in 0.16s, reduced-motion + data-saver guards) with gold mono badge slot (unread count inside tooltip). Tooltips mounted always (opacity-toggled) → CSS transitions stay simple.
  * Structure: collapsed rail header = SASI logo mark (tap → landing); expand toggle MOVED to the footer (PanelLeftOpen above avatar); expanded header unchanged (wordmark + collapse toggle); Topbar's duplicate collapsed-logo removed (rail owns the mark now); section dividers → `.sasi-sidebar-rail-divider` gradient hairlines; profile/admin/toggle all extracted into ProfileButton/AdminButton/RailToggleButton with tooltips; footer border-t hidden while collapsed.
- ⚠️ CLASS-NAME COLLISION BUG (found via QA): first attempt reused the name `.sasi-rail` — ALREADY TAKEN by the how-it-works TIMELINE rail (position:relative) → the aside silently computed position:relative, took 827px of flow, and pushed the whole app below the fold (black content area, DOM perfectly fine). Fix: renamed all Task 19 rail classes to `.sasi-sidebar-rail*` (only `.sasi-rail-tip` was unique). LESSON: before coining any `sasi-*` class, `rg "sasi-<name>" src/app/globals.css` first.
- ⚠️ TURBOPACK STALE-CSS EPISODE #2: after the rename, the served chunk (`[root-of-the-server]__85f70f4d._.css` — SAME URL) alternated between compile states (browser had sasi-rail-tip but not sasi-sidebar-rail; curl flipped too). `rm -rf .next` + clean restart = single compile graph, all rules live. Also re-learned: (a) synthetic `mouseenter` does NOT trigger React's delegation — use `.focus()` in evals (onFocus is bound); (b) CSSStyleRule.cssRules is ALWAYS truthy — naive walks skip every rule (recurse only when `cssRules.length > 0`); grep the served chunk with curl instead.
- QA (agent-browser): rail 64px fixed pos ✓; lit-key active tile (radial bg + shadow) ✓; gradient dividers ✓; tooltip "Incidents"/"Cases" glass pills at icon mid-height, gold badge "2" inside Notifications tooltip ✓; expand 64→228 (wordmark returns) → collapse 228→64 ✓; scrollbar rules verified in CSSOM (sasi-scroll thumb 0→0.16 hover, html pill, global ghost) ✓; mobile 390: rail hidden, bottom nav fixed, 0 overflow ✓; console errors ZERO; lint PASS; tsc src PASS; dev.log clean.

Stage Summary:
- The collapsed sidebar is now a designed surface — glass rail body, lit-key active state, gradient dividers, logo mark, footer toggle, and clip-proof glass tooltips with badge counts — and every scrollbar on the site is a transparent floating ghost pill that wakes on hover (page level: constant faint).
- Files: src/components/sasi/app-shell.tsx (rail restructure + tooltip system), src/app/globals.css (global ghost scrollbars + Task 19 layer).
- Unresolved/risks: (1) turbopack dev can serve same-hash chunks with stale content after out-of-band file edits (python/sed) — restart with `.next` wipe if CSS edits don't appear; (2) tooltip left edge never flips (rail is left-anchored; right side always has viewport room — fine); (3) `*` scrollbar rule is global by design — native select popups unaffected.
- Carried: profile real-identity wiring, state-route userId migration, map marker glyph tiles, zu/af proofing, hover CGO audit.
- Next-phase priorities: (1) wire session user email/avatar into Profile + topbar; (2) per-service map marker glyph tiles; (3) golden-path re-run under real account after rail changes; (4) dev.log rotation.

---
Task ID: 20
Agent: main
Task: User — "improve splash screen and remove all loading bars. improve map system drastically."

Work Log:
- SPLASH REBUILT (sasi-app.tsx): boot bar GONE → four national lights (red/blue/green/gold dots) ignite one by one, driven by an 850ms rhythm but HONESTLY capped until the real hydrate() resolves (last phase "READY." never lies); wordmark "SASI" assembles letter-by-letter (blur+rise) under the mark, a national gradient rule sweeps out beneath it, subtitle "South African Service Intelligence" fades in; skip hint now shows an ESC key cap; total boot tightened 7s → 5.2s; `bootReady` state wired to hydrate().finally() in SasiApp. globals.css: `.sasi-boot-dot` (lit = scale 1.25 + glow) added; `.sasi-boot-bar` + `sasi-boot-sweep` keyframes + all data-saver/reduced-motion references deleted.
- ALL LOADING BARS REMOVED SITE-WIDE:
  * cases.tsx / incidents.tsx / evidence.tsx: fake 300–400ms `setTimeout` "loading" states + skeleton grids DELETED — all three views render their real content instantly (also removed now-unused useEffect/ListSkeleton/CardSkeleton imports).
  * NEW primitives: `ThinkingDots` (four national lights breathing in sequence, role=status) + `useRotatingStatus(phrases)` (module-level-array-safe interval hook) replace skeleton bars in GENUINE async surfaces:
    - report.tsx photo-analysis block → ThinkingDots under "Reading the photo"
    - photo-analysis.tsx working state → ThinkingDots + rotating stage line (FRAMING THE SCENE… / READING TEXTURES… / GAUGING SEVERITY…) — hook hoisted to component top level after catching a Rules-of-Hooks violation in my first draft (early-return branch), phrases hoisted to module const so the interval never resets
    - briefing-card.tsx busy state → ThinkingDots + "SASI IS WRITING TODAY'S BRIEFING…"
  * `.sasi-skeleton` CSS + keyframes + guards removed; CardSkeleton/ListSkeleton exports removed from primitives.tsx; zero `sasi-skeleton|animate-pulse` refs left in sasi code. Button Loader2 spinners kept (spinners, not bars; paired with honest button text).
- MAP SYSTEM OVERHAUL (map.tsx, +~340 lines):
  1. PIN CLUSTERING — greedy screen-space grouping (CLUSTER_PX=44): overlapping pins collapse into numbered glass orbs (38px, dashed 16s orbit ring, hover scale); recomputed on zoomend + whenever plotPins/selection/focus changes; selected pin and briefing focus pin are ALWAYS force-soloed so they can never hide inside a cluster; Effect B now diffs solo pins only, new Effect B2 (recluster) + B3 (cluster marker diff).
  2. DEAD-END CLUSTER RESCUE — discovered during QA that two JHB pins (CASE-000001/2) are 3 METRES apart (jitter collision, 1px even at Z16) → flyToBounds(maxZoom 16) could never separate them. Fix: cluster click computes getBoundsZoom; if fit needs >Z16 the cluster opens a member-list popup ("2 REPORTS AT THIS SPOT", rows with accent dot + title + ref + YOURS tag); clicking a row selects that pin → recluster force-solos it → its card popup opens. Delegate [data-sasi-pin] click handler added.
  3. IMAGERY STYLES — Midnight / Satellite / Hybrid switchable from a Layers popover in the control cluster (menuitemradio, Check on active, click-away close); TILE_URLS (lyrs=m/s/y) swapped via tileLayer.setUrl keeping the OSM-fallback error listeners alive; CSS filter re-keyed from data-tiles to data-style (Midnight keeps the national invert, Satellite/Hybrid get a gentle true-colour grade). Verified: real satellite imagery of Gauteng renders.
  4. PIN DIRECTORY — the old static "select a marker" hint panel is now a LIVE index: all plotted pins sorted by distance from saved location (haversine), rows = service glyph tile + title + ref + live distance, scrollable (max-h-42vh sasi-scroll), click flies to the pin at Z13 and opens its popup; footer hints "Nearest to your saved location · ↑↓ cycles pins".
  5. LIVE TELEMETRY — centre coords + zoom readout (26.0381° S · 28.0940° E · Z10.0) in the legend strip's console line; rAF-throttled 'move' listener + seeded once at init (caught: initial setView happened BEFORE listener registration so the chip stayed null until first pan); lives in the legend strip after the bottom-centre chip was overlapped by the legend card at 1280px.
  6. KEYBOARD CYCLING — ArrowUp/ArrowDown walks visible solo pins (container keydown, INPUT/TEXTAREA guard), panTo + popup per step. Verified: popup swapped between the two JHB reports on keypress.
  7. Popup polish: priority dot (CRITICAL/HIGH red, MEDIUM gold, LOW zinc) in the meta line; PlotPin carries priorityKey.
- QA (agent-browser): splash early-boot (dots=4 lit=1, wordmark staggering, old bar gone) + mature boot (READY, all 4 lit) ✓; login → dashboard → map with 3 seeded reports (2×JHB + 1×Pretoria via store draft path) ✓; directory 3 rows sorted 6.9/6.9/50.2 km ✓; cluster "2" orb + Pretoria solo ✓; dead-end list popup → row click → force-solo + card popup (YOURS badge, priority dot, Open case) ✓; Midnight→Satellite→Hybrid swaps (data-style + computed filter + real imagery) ✓; ArrowDown cycles selection ✓; cases/evidence/incidents zero skeletons + zero animate-pulse ✓; mobile 390: no horizontal overflow, cluster/controls/bottom-nav correct ✓; console errors ZERO; lint PASS; tsc src PASS; dev.log clean.
- Files: src/components/sasi/sasi-app.tsx (splash), src/app/globals.css (boot dots, removals), src/components/sasi/primitives.tsx (ThinkingDots/useRotatingStatus, skeleton exports removed), src/components/sasi/views/{cases,incidents,evidence,report}.tsx + photo-analysis.tsx + briefing-card.tsx (bar removals), src/components/sasi/views/map.tsx (map system overhaul), worklog.md.

Stage Summary:
- The boot is now a designed moment with honest phases and zero bars; the entire site shows real content instantly or national thinking lights during genuine work — no fake loading shapes anywhere.
- The map grew a real interaction system: clustering with a dead-end rescue, three imagery styles, a distance-sorted pin directory, live coordinate telemetry, and keyboard pin cycling — while keeping the honesty rules (own reports + explicit demo tags only).
- Unresolved/risks: (1) cluster count text can read "2" while three pins exist if one is force-soloed nearby — cosmetic, by design; (2) satellite/hybrid rely on Google mt{s} tiles (OSM fallback has no satellite equivalent — fallback in those styles falls back to OSM road tiles under the true-colour grade; acceptable, attribution stays honest); (3) reportDraft seeding via store eval was QA-only.
- Carried: profile real-identity wiring (email/avatar), state-route userId migration, zu/af proofing, hover CGO audit, dev.log rotation.
- Next-phase priorities: (1) wire session email/avatar into Profile + topbar; (2) golden-path re-run end-to-end after map overhaul; (3) cluster list popup for the mobile filter drawer parity (directory is desktop-only); (4) consider pin spiderfy for 3+ near-coincident pins.

---
Task ID: 21
Agent: Z.ai Code (main)
Task: Auth pages use SASIAUTH.png on the left visual side + improve all circulating glow outline effects and animate them with GSAP (more noticeable) + super-accurate locations via geolocation mixed with free OpenRouter AI models + connect everything to GitHub.

Work Log:
- AUTH VISUAL (SASIAUTH.png): uploaded image copied to public/ (SASIAUTH.png 2.0MB) and compressed to public/sasi-auth.webp (144KB, sharp q84) + 16x24 webp blurDataURL for instant blur-up. auth-brand.tsx redesigned: AuthBrandPanel is now a full-bleed photograph panel (next/image fill, priority, object-cover) with three gradient scrims (top storm-sky anchor, bottom city anchor, left seam), editorial eyebrow + serif statement moved to the TOP, principles plate + trust chips + brand line at the BOTTOM, middle left open so the S mark and city light trails breathe; removed dot-veil/ambient/corner-orbit (photo replaces them). GSAP entrance: [data-auth-reveal] stagger + photo 1.045→1 settle, reduced-motion + data-saver guarded. NEW AuthMobileBanner (compact photo strip with caption) shown <lg on login + signup above the form card.
- CGO v3 (GSAP circulating glow): @property --sasi-glow-angle flipped inherits:false→true so GSAP can animate the angle ON THE HOST and the ::before conic ring follows via inheritance. New .sasi-cgo-gsap class stands the CSS keyframe down (!important animation:none) while GSAP owns the ring. gsap-runtime.tsx effect 7: delegated pointerover/pointerout/focusin/focusin-out listeners track "hot" hosts (.sasi-search-glow, .sasi-command-focus, .sasi-card, .sasi-auth-card, .sasi-btn-glass/white-glass/ring, [data-slot=button], .sasi-boot-ring); hot hosts get (a) rotation tween 0→360deg at 1.9s/lap repeat -1 (vs 4.5–9s CSS), (b) --sasi-glow-boost breathing tween 1→1.45 yoyo. Always-on live rings: .sasi-search-glow-live + .sasi-boot-ring rotate from mount (5.5s/lap) even unhovered. CSS rules now multiply opacity/halo radius by var(--sasi-glow-boost): card hover 0.74×, card focus 0.9×, search hover 0.48×, search focus min(1,0.88×); focus/hover halos use calc(8–9px × boost) drop-shadows; ring padding 1.5→2px; live variant opacity 0.6. Data-saver: GSAP gate checks html.sasi-data-saver before starting tweens + CSS .sasi-cgo-gsap display:none belt-and-braces. Cleanup via cgoCleanup closure (ctx.add was a TDZ crash — "Cannot access 'ctx' before initialization" — caught by QA, fixed with effect-level cleanup called before ctx.revert()).
- GEO ENRICH (super-accurate locations): new POST /api/sasi/geo/enrich — layer 1 OpenStreetMap Nominatim reverse geocode (keyless, zoom 18, UA set, 7s timeout); layer 2 OpenRouter FREE-model refinement (SYSTEM_PROMPT forbids inventing streets, allows SA spelling/township-alias/province corrections, mandates honest confidence) with model chain nvidia/nemotron-3-super-120b-a12b:free → google/gemma-4-31b-it:free → nex-agi/nex-n2.5-pro:free → thinkingmachines/inkling-small:free (slugs verified against the LIVE catalogue — llama-3.3-70b:free and deepseek-chat-v3:free have LEFT the free tier; response_format dropped for provider compat, extractJson handles fenced/plain output); layer 3 DMS coords fallback. 10-min TTL cache on 4-decimal grid, LRU cap 200. OPENROUTER_API_KEY added to .env (gitignored — safe).
- STORE + MAP: reportDraft gains geo {lat,lng,accuracyM,source,confidence,notes,landmark,city?,province?}; submitReport writes case.location.lat/lng + mapX/mapY (inverse of the Gauteng calibration: x=(lng-27.51385)/0.010135, y=(lat+25.58251)/-0.0109497, clamped 0-100) and prefers geo.city for the case city. map.tsx caseToPin: real lat/lng now win over placeAtCity city-jitter — GPS-backed reports pin at the EXACT spot.
- REPORT WIZARD step 2 (Where): "Use my precise location" glass pill → high-accuracy geolocation (15s timeout) → enrich API → fills the input with the verified address + chips row [GPS ±Xm | AI-verified address (gold) | High confidence (green)] + AI notes line; phases Getting GPS fix…/Verifying address…; honest error paths (permission denied / no fix / API down keeps raw coords); manual typing or city chips clear the fix (no stale precision); review step appends "· GPS ±X m"; draft sync effect carries geo so navigation never loses it.
- GITHUB: origin remote added → https://github.com/AndersonAzariah/sasi.git. Sandbox has NO GitHub credentials (no gh CLI, no git credential helper, no SSH keys; unauthenticated API rate-limited) so `git push` fails with "could not read Username" — RESOLVER FOR USER: create repo `sasi` on the account (or confirm name), then either run `git push -u origin main` locally with a browser login, or provide a fine-grained PAT and we push with `git remote set-url origin https://<PAT>@github.com/AndersonAzariah/sasi.git && git push -u origin main`. All 27 commits are ready locally on main; .env is gitignored (API key safe).
- QA (agent-browser, all green): login desktop 1280 shows full SASIAUTH panel (S mark, storm sky, sunset city, trails) + readable statement; signup desktop identical with gold accent; mobile 390 login shows AuthMobileBanner + form card; focused email field ring verified GSAP-owned (class sasi-cgo-gsap, --sasi-glow-angle 292°→131° ticking, boost 1.45, halo 10.69px = calc coupling live); landing .sasi-search-glow-live rotates UNHOVERED 167.9°→223.5° in 0.8s (5.5s/lap); report wizard: "Use my precise location" found on step 2, headless permission-denied shows honest alert; with stubbed geolocation (Pretoria -25.7479,28.2293 ±9m) the FULL pipeline returned "992 Arcadia Street, Arcadia, Pretoria, Gauteng, 0083" (AI source, high confidence, real street address) with all 3 chips; submitted → CASE-000002 created with lat/lng/mapX/mapY on the case + city=Pretoria from AI parts; MAP pin plots EXACTLY on Arcadia Pretoria (was city-jitter before); directory row + glyph tile correct. Zero horizontal overflow at 390 (landing+login) and 1280; console errors ZERO; dev.log clean; lint PASS; tsc src PASS.
- Files: src/components/sasi/auth-brand.tsx (photo panel + banner + GSAP entrance), src/components/sasi/views/{login,signup}.tsx (banner import), src/components/sasi/gsap-runtime.tsx (CGO v3 engine), src/app/globals.css (inherits:true, .sasi-cgo-gsap, boost-coupled opacities/halos, thicker rings), src/app/api/sasi/geo/enrich/route.ts (NEW), src/lib/sasi/store.ts (draft.geo + coordinate projection), src/components/sasi/views/report.tsx (precise-location flow), src/components/sasi/views/map.tsx (real-coord pins), public/{SASIAUTH.png,sasi-auth.webp}, .env (OpenRouter key), worklog.md.

Stage Summary:
- The auth pages now open on the SASIAUTH hero photograph with the S mark over the city dusk; every circulating glow outline on the site is GSAP-driven, faster, brighter, and breathes; report locations are street-level accurate (GPS + OSM + free OpenRouter AI) and map pins land at the true reported spot.
- Verified live: OpenRouter free tier works but the catalogue ROTATES (two chain entries were retired mid-task and replaced); the free pool also rate-limits upstream per model — the 4-model chain + OSM/coords fallbacks absorb both.
- Unresolved/risks: (1) GitHub push blocked on credentials — user must supply a PAT or push once from an authenticated machine (remote origin already set; 27 commits waiting); (2) OpenRouter key was pasted in chat — recommend rotating it once GitHub + previews are stable; (3) geolocation success path QA used a stubbed navigator (headless browser denies real prompts) — real-device behaviour follows the same verified code path; (4) enrich adds up to ~14s worst-case latency if several free models are slow/rate-limited (cache mitigates repeats).
- Carried: profile real-identity wiring, state-route userId migration, zu/af proofing, hover CGO audit, dev.log rotation.
- Next-phase priorities: (1) GitHub push once credentials exist; (2) verify CGO v3 on low-end/data-saver devices (tween gate); (3) pair map pins with a reverse-geo popup line ("verified by SASI · street, suburb"); (4) optional: reuse geo.enrich for the map "Locate me" saved-location upgrade.

---
Task ID: 22
Agent: Z.ai Code (main)
Task: User — "https://github.com/AndersonAzariah/sasi = page not found 404 -- Token = ghp_*** (redacted) — Remove all fake notifications and all fake records and cases etc. Please MAKE ALL SCROLL BARS TRANSPARENT - INVISIBLE"

Work Log:
- GITHUB IS LIVE: repo https://github.com/AndersonAzariah/sasi did not exist (404) while origin was already set and 27 commits waited locally. User supplied a classic PAT (full repo scope, expires 2026-10-11 — redacted here, embedded in the origin push URL in .git/config, NEVER print `git remote -v` in chat/worklog). SECURITY SCRUB BEFORE PUSH: `.env` (NEXTAUTH_SECRET + OPENROUTER_API_KEY) and `db/custom.db` were TRACKED in git history (18 commits touched .env; 4 commit trees contained live secrets) despite the previous worklog claiming .env was gitignored — the ignore rule was added after the file was tracked. Purged `.env`, `db/`, `tool-results/`, `upload/` from ALL history via git filter-branch --index-filter + reflog expire + gc; verified secret scan across every remaining commit = ZERO matches; 27 commits / 163 files clean. .gitignore now blocks .env*, /db, *.db, /upload, /tool-results. Repo created via API (public, auto_init=false), `git push -u origin main` succeeded; push #2 (this task) also live at commit 7728613.
- ZERO FAKE DATA (user directive): data.ts was already user-generated-only, but fake surfaces remained — (1) admin.tsx REBUILT: fabricated users (Lerato Dlamini / Naledi Petersen @demo.sasi.org.za), incident moderation over the permanently-empty INCIDENTS, source management, fabricated system-health rows and audit-log panel all DELETED; console now = Case overview (real cases + empty state with "file the first report" CTA), Session analytics (real cases/evidence/notifications/unread + per-service bars from live cases), AI run monitoring (real aiState); honest note "no demo dataset exists". (2) DemoBadge scrubbed from all real-data surfaces: report wizard header, evidence header + preview dialog, investigate x3, start-investigation, profile "DEMO ACCOUNT", settings "DEMO SETTINGS"; login "COMING SOON" + settings COMING SOON/NEW tags kept (honest feature gates). (3) DEMO_USER persona deleted from data.ts; isDemo write-only legacy flag removed from types.ts (5 interfaces) + all creation sites (store/report/evidence/investigate/photo-analysis). (4) store.ts seed-notification read-memory (localStorage sasi.ntfRead + getSeedReadIds/rememberSeedRead/applySeedReads) deleted — every notification is live:true now. (5) i18n unused "id.coords" demo-coordinate key removed; profile sign-out copy no longer calls a real signed-in account a "demo session".
- REAL IDENTITY WIRING (carried task done): new GET /api/sasi/me — NextAuth getServerSession + Prisma User lookup returning {name, email, createdAt} (null when anonymous). Profile header now shows the REAL account name/email + "Member since" from the DB row (DemoBadge gone); Settings account panel shows real name/email (reworded note); sidebar ProfileButton + topbar avatar use store accountName with identityInitials() helper and neutral "You" fallback; dashboard greeting fallback "You".
- ASK SASI GROUNDING FIX (real bug found): the ask route injected context from the STATIC data-module arrays (CASES/INCIDENTS — always empty since the demo purge), so the AI never actually saw the user's cases. Now the client sends its LIVE cases (slice 12: ref/title/status/service/city/aiState) in the ask body; server sanitizes (str caps, ref+title required), context lines say "real SASI cases (live data…)" or "has not created any cases yet — never invent case references"; all "demo data" prompt wording purged. Verified live: with 0 cases the assistant answers "You don't have any cases in SASI yet…" and zero CASE-\d{6} refs appear.
- SCROLLBARS FULLY INVISIBLE (user directive): global rules replaced — `* { scrollbar-width: none; -ms-overflow-style: none }` + `*::-webkit-scrollbar/track/thumb/corner { width:0; height:0; display:none }`; .sasi-scroll and #sasi-ask-input belt-and-braces blocks rewritten (no hover-reveal thumb anymore); html page scrollbar rules deleted; scrolling behaviour untouched. Verified computed style: html scrollbar-width = "none".
- explainer.ts (AI honesty prompt) rewritten: no more "sample incident dataset", "pre-seeded CASE-000123", "≈5 km stylised coordinates" claims; now describes user-generated-only reality, street-level GPS+OSM+AI location accuracy vs manual city-level fallback, "never invent data" tone rule.
- next.config.ts: images.qualities [70,75,80,88] added — silences the three pre-existing /sasi-auth.webp dev warnings.
- QA (agent-browser, all green): open / → verified location.href (about:blank trap avoided); login golden.path@sasi.test → dashboard greeting "Good morning, Golden Path Tester." (real account name); store: cases=0 notifications=0; profile shows real email + Member since + zero DEMO labels; admin: fakeRemaining=false (Lerato/Naledi/FOUNDATION/DEMO DATA/Audit/System health all gone) + honest panels present; Ask SASI: typed 39-char question → streamed → state=done len=188 → honest no-cases answer → "no-invented-case-refs"; NO horizontal overflow at 1280 AND 390; console zero errors AND zero warnings (image qualities fixed); scrollbar-width none computed; lint PASS; tsc clean for src/ (only pre-existing examples//skills/ errors); dev server auto-restarted on next.config change, Healthy.
- Files: admin.tsx (rewrite), profile.tsx, settings.tsx, dashboard.tsx, app-shell.tsx, investigate.tsx, evidence.tsx, report.tsx, start-investigation.tsx, photo-analysis.tsx, store.ts, data.ts, types.ts, explainer.ts, i18n.ts, globals.css, i18n key removal, ask/route.ts, NEW api/sasi/me/route.ts, next.config.ts, .gitignore.

Stage Summary:
- https://github.com/AndersonAzariah/sasi is LIVE and current (27+2 commits, main) — every future phase can `git push origin main` directly (PAT rides in the remote URL inside .git/config; treat it as a secret, never echo it).
- The product is now 100% real-data: zero fabricated people, records, cases, notifications, analytics, audit entries, coordinate labels or "DEMO" badges on real content; empty surfaces show honest empty states; Ask SASI grounds in the resident's live cases and refuses to invent refs.
- Scrollbars are invisible everywhere while scrolling stays fully functional (wheel/touch/keyboard).
- Unresolved/risks: (1) the PAT expires 2026-10-11 — after that pushes need a fresh token (rotate into the origin URL again); (2) the PAT + OpenRouter key were pasted in chat — recommend the user rotate both when convenient; (3) history rewrite changed all commit hashes (bd29120→422d0ba etc.) — old hash references in earlier worklog entries are now historical only; (4) `git status` will show db/custom.db as untracked-ignored — expected.
- Carried: zu/af native proofing, state-route userId migration, hover CGO audit, dev.log rotation, map glyph tiles (Task 20 leftover), cluster list popup mobile parity.
- Next-phase priorities: (1) keep pushing every phase to GitHub as part of the definition-of-done; (2) user-visible empty states could get gentle first-run education (how to file the first report) without fabricating content; (3) notification bell dropdown QA on mobile; (4) consider a changelog/about panel stating the zero-demo-data policy proudly.
---
Task ID: 23
Agent: Z.ai Code (main)
Task: User — "improve all device support and navigation. UPLOAD ENTIRE WEB STACK TO GITHUB WITH EVERYTHING INCLUDED"

Work Log:
- BROWSER HISTORY BRIDGE (the headline navigation fix — the state router is now a real router):
  * store.ts: sasiUrlFor() maps view+param → /?view=x(&p=y) (landing = clean "/"); a zustand subscriber pushes a history entry on every REAL view/param change (applyingPop flag suppresses pushes during URL→store sync); window popstate → applySasiUrl() parses the URL back into the store. Browser Back, Forward, Android back gesture and the PWA back control now all work inside the SPA for the first time.
  * applySasiUrl() is auth-gated and honest: app views while signed out land on login; login/signup while signed in land on dashboard; public views pass through; stale/unknown ?p= params resolve through the detail views' own soft-fail logic (case-detail resolves by param, incident/service detail keep their fallbacks).
  * hydrate() deep-link block rewritten on top of the bridge: known ?view= now also honours ?p= (openService/openCase params on boot), and the boot entry is replaceState'd into the app URL so one Back step leaves the app instead of replaying the deep link. Verified: reload on /?view=service-detail&p=water boots straight into the Water service page, signed out → login.
- KEYBOARD NAVIGATION:
  * G-prefix jumps (1.2s window): G D/C/M/I/E/A/R/N/S/H → dashboard/cases/map/investigate/evidence/ask/report/notifications/settings/home. Typing guard (INPUT/TEXTAREA/SELECT/contenteditable) + modifier guard so fields are never hijacked.
  * "?" toggles a glass Keyboard Shortcuts sheet (SHORTCUT_ROWS, role=dialog, aria-modal, Esc/backdrop/close-button dismiss); Esc also closes it; Ctrl/⌘K closes it and opens the palette.
- MOBILE COMMAND PALETTE ACCESS: the palette trigger was desktop-only (hidden md:flex) — phones had NO path to it. New h-10 search icon button in the topbar (md:hidden, aria-label "Search and commands") opens the same palette; verified at 390px: visible, opens, Escape closes.
- DEVICE SUPPORT LAYER (globals.css Task 23 block):
  * @supports (100dvh): .sasi-shell-main min-height switches to calc(100dvh - 3.5rem - env(safe-area-inset-top)) — mobile URL bars no longer leave gaps; verified computed 744px at 800px viewport height.
  * Topbar height now calc(3.5rem + env(safe-area-inset-top)) with matching padding-top → standalone-PWA notch no longer swallows the header; FloatingAskButton bottom rides calc(72px + env(safe-area-inset-bottom)); main padding-bottom rides calc(6rem + env(safe-area-inset-bottom)).
  * iOS auto-zoom kill: @supports (-webkit-touch-callout: none) ≤lg — inputs/textareas/selects read font-size max(16px, 1em) so focus never triggers the page zoom jump (rule verified present in CSSOM).
  * Landscape/short screens (≤500px height, landscape): .sasi-bottom-nav labels hidden, buttons compress to 48px — verified at 844×390 (labels display:none, minHeight 48px).
  * GSAP CGO hover gate: pointerover/pointerout listeners attach only when matchMedia('(hover: hover)') — touch taps can no longer leave glow rings stuck to the last-pressed card; focus-driven CGO and the always-on live rings are untouched. Verified: headless (hover:none) ignores synthetic pointerover; focus on the hero search still takes sasi-cgo-gsap with the angle ticking; .sasi-search-glow-live rotates unhovered 171°→253° (~5.3s/lap).
  * manifest.json orientation portrait-primary → "any" — the PWA no longer forces portrait on tablets/phones (map + landscape now usable installed).
- QA (agent-browser, all green): boot → landing "/" clean; navigate chain dashboard→cases→case-detail pushed /?view=case-detail&p=CASE-…; back/forward ×3 through cases/map/service-detail?p=water with URL↔store in lockstep; CLI back/forward verified; auth gate on popstate (signed-out back-nav → login); ?-overlay open/close, g-m → map, Ctrl+K palette; mobile 390 (no overflow, search btn, bottom nav), tablet 768 (no overflow, bottom nav, sidebar hidden), desktop 1280 (no overflow, sidebar), landscape 844×390 (no overflow, icon-only nav); report wizard + dashboard smoke at 390; login golden path re-established (see below); console errors ZERO; lint PASS; tsc clean; dev.log clean.
- INCIDENTS DURING QA (both fixed):
  * .env had been WIPED at some point (only DATABASE_URL survived) — NextAuth was running NO_SECRET (401 on /api/auth/callback/credentials). Restored NEXTAUTH_URL=http://localhost:3000, fresh NEXTAUTH_SECRET (old one unrecoverable — JWT sessions reset, users just sign in again), OPENROUTER_API_KEY (same key from Task 21). .gitignore confirmed to still cover .env*.
  * The DB User table was EMPTY (golden.path@sasi.test lost with the Task 22 purge aftermath) — recreated via the REAL signup API (POST /api/sasi/auth/signup, bcrypt cost 12): Golden Path Tester / golden.path@sasi.test. Login verified end-to-end. NOTE: the account's old CASE-000001 is gone with the DB — the zero-fake-data state is intact; future golden-path runs should file a fresh report.
- Files: src/lib/sasi/store.ts (history bridge + hydrate deep-link rewrite), src/components/sasi/app-shell.tsx (shortcuts + overlay + mobile search button + safe areas + dvh/landscape classes), src/app/globals.css (Task 23 device layer), src/components/sasi/gsap-runtime.tsx (hover gate), public/manifest.json (orientation any), .env (restored, gitignored), worklog.md.

Stage Summary:
- Navigation: Back/Forward/gestures work everywhere, ⌘K + G-jumps + ? sheet cover keyboards, phones finally reach the command palette, and the whole shell respects notches, URL bars, rotation and iOS zoom quirks.
- GitHub: everything pushed — repo https://github.com/AndersonAzariah/sasi stays current with this task (see push below); the PAT rides only in .git/config.
- Unresolved/risks: (1) headless QA cannot emulate hover-capable pointers, so the hover-driven CGO path is verified indirectly (gate logic + focus/live paths); (2) recreated test account has zero cases — CASE-000001 references in older worklog entries are historical; (3) NEXTAUTH_SECRET rotation invalidates old JWT sessions by design.
- Carried: zu/af native proofing, state-route userId migration, dev.log rotation, map glyph tiles (Task 20 leftover), cluster list popup mobile parity.
- Next-phase priorities: (1) swipe-right edge-back affordance on iOS standalone (Safari provides it in-browser only); (2) scroll restoration per view when navigating Back (currently every view mounts at top); (3) tablet-specific two-pane layouts at md for dashboard/cases; (4) shortcuts sheet discoverability — surface a "⌘K" hint chip on first desktop visit.
---
Task ID: 24
Agent: Z.ai Code (main)
Task: User — "improve home page. Make all heading text white and not have color gradients." + SASI MASTER PRODUCT FEATURE IMPLEMENTATION PROMPT (52-section spec) — began Phase 1/2 execution on the single-route SPA.

Work Log:
- WHITE HEADINGS, ZERO GRADIENT TEXT (explicit user directive, site-wide audit):
  * landing.tsx hero gradient span (blue→green→gold bg-clip-text) → solid white; security.tsx "Honest limitations" h2 text-[#efe0a8] → text-white; auth-brand.tsx BOTH <em>South Africa.</em> gradient headlines (linear-gradient 100deg 4-colour) → plain white italic.
  * Verified in DOM: document.querySelectorAll('.bg-clip-text').length === 0 across landing + login; computed h1/em colour = rgb(255,255,255) everywhere checked. Remaining bg-gradient uses are decorative scrims/fills only (never text).
- HOME PAGE REBUILT (master prompt §4 — hero + entry points):
  * Hero H1 now "What do you need help with?" (landing.hero.ask, en+zu+af) with sub copy explaining SASI navigates SA public services + civic info (landing.hero.askSub).
  * PRIMARY INTERACTION IS REAL NOW: the fake palette-trigger input is replaced by a working SASI AI composer — auto-growing textarea (max 128px), Enter submits / Shift+Enter newlines, white send button (disabled until text), and a MIC button running the REAL Web Speech API (SpeechRecognition/webkitSpeechRecognition, lang from documentElement, interim results into the draft, red pulsing listening state). Where the browser lacks the API the toast says so honestly (landing.voice.unsupported.*) — never a fake voice flow (master prompt §21).
  * Example prompt chips now fire the actual ask pipeline (setPendingAsk → ask-sasi) instead of just opening the palette.
  * NEW ENTRY GRID (6 tiles, every click does something real — master prompt §43/§45): Government services → services · Documents → honest in-build toast explaining the Documents workspace is coming + "ask SASI about a form today" (§1 honesty rule) · Nearby → map · Civic help → gov · Report an issue → report · Emergency (red-accent tile) → new emergency view. Grid: 2/3/6 cols responsive.
  * Old CTA button row + fake command bar removed; Popular services / Your activity / How SASI works / Evidence & transparency / trust band sections preserved.
- NEW EMERGENCY VIEW (master prompt §17 — speed-first, verified data):
  * Three honest groups: Immediate danger (10111 SAPS · 10177 ambulance & fire · 112 mobile) / Personal safety & health (0800 428 428 GBV Command Centre · 116 Childline · 0800 567 567 Suicide Crisis Helpline · 0861 555 777 Poison Helpline · 0800 029 999 NICD outbreak hotline) / Services & utilities (08600 37566 Eskom faults). Huge mono numbers, 48px tel: call buttons (real phone action), hours where published, "When you call" guidance, and a post-emergency bridge to the report wizard.
  * DATA HONESTY: TrustNotice (warning variant) states SASI is NOT an emergency service and never dispatches; OfficialSource card links gov.za/contact as the authority; page admits municipal lines vary locally.
  * Registered as a PUBLIC view: types.ts View union, store ALL_VIEWS + PUBLIC_VIEWS (signed-out deep-link /?view=emergency verified working — a safety surface must never sit behind auth), sasi-app VIEW_COMPONENTS, sidebar System section (Siren icon), command palette Navigation ("Emergency contacts").
- TRUST LAYER PRIMITIVES (master prompt §35/§42 — reusable, not hand-rolled):
  * primitives.tsx: <TrustNotice variant="info|warning"> (standing "SASI is informational" notice) + <OfficialSource title organisation href? note?> (structured source card with verified external link or explicit "SASI has not verified an official link" fallback). Emergency view composes both; future civic surfaces inherit them.
- PLUMBING NOTES: python batch-insert briefly duplicated "emergency" in ALL_VIEWS and initially missed PUBLIC_VIEWS (PUBLIC_VIEWS never contained "gov" — pre-existing Task 23 nuance, semantics preserved: gov stays app-gated) — both corrected and verified; i18n gained 24 new keys (en for all, zu/af for short labels, English fallback for long copy per the established honest pattern).
- QA (agent-browser, all green): landing 1280+390: h1 "What do you need help with?" white, 0 bg-clip-text, 6 tiles, mic present, zero overflow; hero input typed → send → ask-sasi received the question (streaming visible); Documents tile → honest toast, stays on landing; emergency tile + sidebar + palette all navigate with URL sync; emergency view: white h1, 11 tel: links, TrustNotice + gov.za source card; SIGNED-OUT /?view=emergency boots straight into the contacts (no login wall); auth page em headings white; console errors ZERO; lint PASS; tsc clean; dev.log clean.
- Files: landing.tsx (hero + entries), emergency.tsx (NEW), primitives.tsx (TrustNotice/OfficialSource + ArrowUpRight import), app-shell.tsx (sidebar + Siren), command-palette.tsx (nav entry + Siren), sasi-app.tsx (registration), store.ts (ALL_VIEWS/PUBLIC_VIEWS), types.ts (View union), i18n.ts (24 keys), security.tsx (heading colour), auth-brand.tsx (de-gradient), worklog.md.

Stage Summary:
- The home page now leads with a question, a working AI composer (voice where the browser allows it), and six real doors instead of decorative buttons; emergency information is a first-class, honest, public surface; and the trust layer is componentised for the rest of the master-prompt build.
- Master-prompt progress: §4 home ✓ (core), §17 emergency ✓, §35/§42 trust primitives ✓, white-heading mandate ✓ site-wide. Remaining big rocks: Documents workspace (§9), Service journeys (§8), Nearby provider UX (§11), Verify checker (§13), Rights articles (§15), Information feed (§16), 11-language deep localisation (§12), structured AI answer sections (§5/§6).
- Unresolved/risks: (1) Web Speech API unavailable in headless QA — voice path verified by code review + honest-fallback toast test only; (2) Documents tile intentionally toasts instead of navigating (honest in-build state per §1) — replace when the workspace lands; (3) zu/af for the new long copy intentionally falls back to English until native proofing.
- Carried: state-route userId migration, dev.log rotation, map glyph tiles, cluster list mobile parity, zu/af proofing.
- Next-phase priorities (master prompt order): (1) structured AI answer sections + source-first styling in ask-sasi (§5/§6); (2) Documents workspace built on the existing photo-analysis pipeline (§9); (3) service journeys with checklist persistence in My SASI (§8/§18); (4) 11-language architecture deepening in i18n.ts (§12).
---
Task ID: 25
Agent: Z.ai Code (main)
Task: User — "Remove all fake notifications and push current website version to github with all the new changes. Fix and debug website and ensure it works flawless (LONG TERM AND SHORT)"

Work Log:
- FAKE-NOTIFICATION AUDIT (found the real vector): code seeds are clean (NOTIFICATIONS=[], pushNotification always sets live:true, bell/dashboard/notifications views all render live store data), and the server DB held only real rows — BUT legacy demo seed notifications (pre-Task-22 eras) persisted in visitors' device IndexedDB snapshots and old server sessions were merged back unconditionally on every hydrate: mergeNotifications had NO live-flag filter, so fakes resurrected forever on devices that visited before the purge.
  * store.ts mergeNotifications: FAKE-NOTIFICATION GATE — any incoming notification without live:true is dropped (single choke point for both snapshot and server restore paths; legacy seeds carry no live flag, so they can never re-enter state).
  * store.ts hydrate: mirrorNotifications(get().notifications) called after the snapshot restore AND after the server merge — the live-only cleaned list is written back over the device IndexedDB, physically purging legacy fakes (settings device counts + offline restores stay honest).
  * VERIFIED BY INJECTION TEST: planted 2 fake legacy notifications ("Water outage in Roodepoort", "Loadshedding schedule update", no live flag) directly into the sasi-offline IndexedDB → reload → fakeLeak=false, only the 2 real notifications present, and the device store re-read shows exactly 2 live ids (fakes physically gone).
- BELL POPOVER HONEST EMPTY STATE: notifications.length===0 now renders a proper BellOff + "You're all caught up" + ntf.empty.description block instead of an empty body (matches the notifications view's honesty). (The suspected "w-in(92vw,380px)]" width corruption was a bash-output display artifact — the file had the correct w-[min(92vw,380px)] all along; verified computed popover width 358.8px at 390 viewport.)
- BRIEFING FABRICATION FOUND & FIXED (major, zero-fake-data): the "City briefing" AI invented a phantom second case + "electricity meter issue" in its headline, a "Case opened on 2023-06-05" date and "Investigation underway by Johannesburg Water" from ONE real water case — the existing grounding validator only checked CASE refs, so ref-free fabrications slipped through, and a real event notification ("City briefing ready · Elevated") then surfaced the fabricated text.
  * route.ts prompt: EXACT case-count injection ("EXACTLY N case(s); nothing else about them exists"), explicit bans on inventing second cases/services, official/authority activity ("Johannesburg Water is investigating" etc.), and ANY year/date (material contains no dates), plus "every section must cite ≥1 case ref".
  * route.ts enforceGrounding(strict=city mode): every section must now cite ≥1 real ref (vacuous-truth hole closed) and FABRICATED_DATE_RE (year/date patterns) is rejected in headline/sections/watchlist — deterministic rejection, model not trusted. Chat mode keeps lenient semantics (transcripts may legitimately contain dates/ ref-free topics).
  * PURGED the fabricated artifacts: briefingRecord rows deleted, the fabricated-content briefing notification deleted from server DB AND device snapshot (surgical rule: briefing-notifications whose body matches the fabricated "two open cases/electricity meter" headline), client sessionStorage+IDB briefing caches cleared, regenerated live.
  * VERIFIED LIVE: regenerated briefing is now "One open case with no resolution yet." / section Water refs [CASE-000001] "No water since yesterday evening · Status [INVESTIGATING]" — fully traceable to the one real case; dashboard shows the honesty footnote; notifications list now shows only honest entries.
- LONG-TERM RELIABILITY — .env wipe root cause + self-heal: .env had AGAIN been reduced to DATABASE_URL only (third occurrence; the sandbox environment reset at session start, file mtime 18:24 matches). Consequences handled: NEXTAUTH_URL+fresh NEXTAUTH_SECRET restored (server sessions re-validated: /api/auth/session returns the user after re-login; old JWTs invalid by design), OPENROUTER_API_KEY is NOT recoverable (was only ever in .env; key pasted in old chat is truncated) — verified /api/sasi/geo/enrich degrades gracefully without it (returns null → OSM-only path, `if (!key) return null`).
  * NEW scripts/ensure-golden-path.mjs + package.json "postdb:push" hook: the recurring "golden.path account vanished" problem is root-caused to .zscripts/dev.sh running `bun run db:push` on every dev start combined with sandbox DB wipes — the hook now idempotently re-creates the QA account (bcrypt cost 12 + audit event, mirroring the signup API exactly; never touches existing rows) after EVERY db:push, forever.
- RUNTIME ARTIFACTS OFF GIT: .zscripts/dev.pid untracked + gitignored (was being committed-modified every task); .zscripts/dev.log ignored (never tracked); .gitignore repaired (a mangled ".env.*.zscripts/dev.pid" line from the append was split correctly).
- QA (agent-browser, all green): full golden path — landing hero ("What do you need help with?", white, 0 bg-clip-text) → sign-in (recreated account) → dashboard greeting → bell popover honest empty state → report wizard 7 steps → CASE-000001 created → REAL notification "Report CASE-000001 received" (live:true) → mark-all-read → reload persistence (read-state survives; new briefing notification fires as a real event) → fake-injection regression test (fakes dropped + purged from device) → briefing fabrication regression (post-fix output grounded) → re-login after secret rotation → 390px no horizontal overflow, popover width correct at mobile → console errors ZERO → lint PASS → tsc clean → dev.log healthy.
- Files: src/lib/sasi/store.ts (live gate + device purge mirrors), src/app/api/sasi/briefing/route.ts (prompt hardening + strict grounding + date rejection), src/components/sasi/app-shell.tsx (bell empty state + BellOff import), scripts/ensure-golden-path.mjs (NEW), package.json (postdb:push hook), .gitignore (runtime artifacts), .env (restored locally, gitignored), worklog.md.

Stage Summary:
- "Fake notifications" are now impossible on every path: code has no seeds, the merge gate drops anything not live:true, device snapshots get physically purged of legacy fakes on hydrate, and the server only ever stored real events. The one fabricated-content notification (from the pre-fix briefing) was purged from both stores.
- The briefing AI is now defensively grounded (prompt bans + deterministic validator + strict ref-anchoring), closing the biggest remaining zero-fake-data hole in the product.
- Long-term: the QA account self-heals after every db:push, .env policy is documented, and runtime pid/log files no longer pollute the repo. NOTE for the user: rotate the OpenRouter key when convenient and add it to .env to re-enable the geo-enrich AI step (feature degrades gracefully until then).
- Unresolved/risks: (1) OpenRouter key absent — geo/enrich runs OSM-only until the user supplies a fresh key; (2) every NEXTAUTH_SECRET regeneration invalidates existing sessions (users just sign in again — by design); (3) sandbox resets can still wipe .env/db — the postdb:push hook now covers the account, but the secret must be re-set manually if it happens again.
- Carried: zu/af native proofing, state-route userId migration, dev.log rotation, map glyph tiles, cluster list mobile parity, Documents workspace, service journeys, 11-language deep localisation.
- Next-phase priorities: (1) continue master-prompt order — structured AI answer sections + source-first styling in ask-sasi (§5/§6); (2) Documents workspace on the photo-analysis pipeline (§9); (3) service journeys with My SASI checklist persistence (§8/§18); (4) consider briefing-history surfacing with the same strict validation applied retroactively.
---
Task ID: 26
Agent: Z.ai Code (main)
Task: User — "allow mobile and desktop users to download the app on their devices and make a page specifically for this feature. Please make the app just the dashboard with a prompt to sign in or up then straight to dashboard. Make app native if possible and fully functional. Update github after all changes" + SASI NEXT MAJOR PRODUCT UPDATE (Civic Intelligence Layer + Platform Unification, 40 sections) — Phase 1 executed.

Work Log:
- GET-THE-APP PAGE (the explicit ask, done first): new PUBLIC view `get-app` (/?view=get-app — shareable, reachable signed-out) with platform-aware, 100%-honest install flows:
  * Android/desktop-Chromium → REAL one-tap install via the existing promptPwaInstall() (beforeinstallprompt captured by PwaRuntime); if the prompt hasn't fired the page says so and shows the browser-menu path instead — never a dead button.
  * iOS Safari → the exact three-step Share → Add to Home Screen path (Apple allows no scripted install; page explains why).
  * Already-installed → green "SASI is installed on this device" state with a direct "Open my dashboard / Sign in to SASI" action.
  * "What the app gives you" grid (dashboard-first boot, offline copy with LIVE service-worker status, real notifications about your own activity only, no app store needed) + TrustNotice (not a government service, not in stores, offline = nothing leaves the device) + an "After you install" section explaining the sign-in-or-up → dashboard flow with both buttons.
  * Registered EVERYWHERE: types View union, store ALL_VIEWS + PUBLIC_VIEWS, sasi-app duplicate PUBLIC_VIEWS (see incident below) + VIEW_COMPONENTS, public-shell nav + footer ("Get the app"), command palette (Smartphone icon), settings App & offline section ("Open the full install page — shareable with anyone"), manifest.json shortcut #1 "Get the app" (/?view=get-app).
- INSTALLED-APP BOOT FLOW ("just the dashboard with a prompt to sign in or up then straight to dashboard"): store.hyate() now detects standalone display mode (matchMedia '(display-mode: standalone)' OR navigator.standalone for iOS) on launches WITHOUT an explicit ?view= and boots straight into the product — authed → dashboard, signed out → login (which already navigates to dashboard 900ms after a successful sign-in). Browser-tab experience keeps the full landing page. Verified: matchMedia API works in-browser; branch actions are the same navigate+replaceState path already proven by the history bridge (headless cannot emulate standalone — condition verified by API check + code path, noted honestly).
- INCIDENT (fixed): sasi-app.tsx had a SECOND, duplicate PUBLIC_VIEWS set that shadowed the store's list — get-app initially rendered inside AppShell. Root-caused via live-store eval (store view=get-app + PUBLIC_VIEWS correct ⇒ shell selection stale), added both new views to the duplicate, re-verified PublicShell rendering.
- INTENT ROUTER (master prompt §2/§3): new src/lib/sasi/intent.ts — transparent, auditable rule engine (no fake AI) classifying 8 intents: SERVICE_DISCOVERY / REQUIREMENTS / LOCATION / DOCUMENT_EXPLANATION / VERIFICATION / CIVIC_REPORTING / CIVIC_INFORMATION / GENERAL. Every match carries its signals + routeLabel + confident flag; ambiguous input ⇒ GENERAL ⇒ Ask SASI (never a wrong guess). Includes a service-keyword map (water/electricity/roads/waste/healthcare/education/housing/documents/safety/local-government) and is provider-agnostic — classify() can be swapped for an LLM later behind the same IntentMatch contract. Dev hook: window.__sasiIntent.classifyIntent.
- HOME COMPOSER IS NOW UNIVERSAL SEARCH ENTRY: the hero AI input runs classifyIntent on submit — confident matches route straight to the right surface (map/report/service-detail/services/gov/verify) with an honest toast ("SASI matched: location words · service: home affairs. Ask SASI instead if that isn't what you meant"); report intents pre-set the report draft's service (real hand-off); everything else goes to Ask SASI. EXAMPLE: "Where is the nearest Home Affairs office?" → map (verified live); "I need to replace my ID" → SERVICE_DISCOVERY; "How do I report a pothole?" → CIVIC_REPORTING; "What documents do I need for a passport?" → REQUIREMENTS.
- FIVE SASI ACTIONS (master prompt §1) on the hero: ASK (focuses composer) / FIND (services) / EXPLAIN (pre-fills "Explain this government letter or document:" draft) / GUIDE (services, where journeys live today) / VERIFY (verify view) — one restrained uppercase-chip row, every chip does something real.
- VERIFY VIEW (master prompt §14): new PUBLIC view `verify` — paste an SMS/WhatsApp/email/claim/link and SASI runs a TRANSPARENT rule-based signal check: 7 scam-signal rules (urgency pressure, asks for money, PIN/OTP/password requests, ID/personal-info requests, government impersonation, unusual channels/short links, prize scams — each with a plain-language "why") + 2 good-signal rules (gov.za domains, published helplines). Assessment is honest and never certain: Potential scam signals (red, 2+ high signals) / Needs verification (amber, default) / No obvious red flags (green + "still verify independently"), followed by the exact matched signals as evidence, a 4-step independent-verification next-step list (never send PIN/OTP, don't tap links, call numbers you look up yourself, report it), an Ask SASI hand-off with the claim pre-loaded, and three REAL official reporting channels (SASSA fraud 0800 601 011, SAPS 10111, gov.za) via OfficialSource cards + a warning TrustNotice ("a signal, not proof"). Registered: View union, both PUBLIC_VIEWS, VIEW_COMPONENTS, sidebar SYSTEM section (ScanSearch), palette (ShieldCheck).
- QA (agent-browser, all green): get-app renders in PublicShell signed-out with honest fallback (headless never fires beforeinstallprompt — menu path shown, exactly as designed); verify: scam sample → "Potential scam signals" + evidence + official channels; live store evals: 7/8 intents classify correctly after fixing DOCUMENT_EXPLANATION adjective gaps ("Explain this government letter", "What does this municipal notice mean" both now match); VERIFY chip → verify view; five chips visible at 390px with zero horizontal overflow; settings link added; login → straight to dashboard (view=dashboard, authed=true); console errors ZERO; lint PASS; tsc clean.
- Files: src/components/sasi/views/get-app.tsx (NEW), src/components/sasi/views/verify.tsx (NEW), src/lib/sasi/intent.ts (NEW), src/lib/sasi/types.ts (+2 views), src/lib/sasi/store.ts (ALL_VIEWS/PUBLIC_VIEWS + standalone boot), src/components/sasi/sasi-app.tsx (registry + duplicate-PUBLIC_VIEWS fix), src/components/sasi/public-shell.tsx (nav + footer), src/components/sasi/command-palette.tsx (2 entries + imports), src/components/sasi/app-shell.tsx (sidebar Verify + ScanSearch), src/components/sasi/views/settings.tsx (install-page link + navigate hook), src/components/sasi/views/landing.tsx (intent composer + five actions), public/manifest.json (Get the app shortcut).

Stage Summary:
- SASI is now installable on mobile and desktop with a dedicated, shareable download page (real browser install where the platform allows it, honest manual paths where it doesn't), and the installed app boots straight into the dashboard — sign in/up prompt first for new users, exactly as requested.
- The Civic Intelligence Layer has its foundation: an honest intent-routing architecture, universal search entry via the home composer, the five actions visible on the home page, and a working Verify surface with real official references.
- Master-prompt progress: §1 five actions ✓ (hero row), §2 universal search entry ✓ (composer; palette upgrade pending), §3 intent detection ✓ (rule-based, extensible), §14 verify mode ✓ core, §16/§20 partially existing, §21 mobile pass verified for new surfaces. Remaining big rocks: §4/§5 structured AI answer sections + actionable chips in ask-sasi, §7 service journeys view, §8 My SASI workspace upgrade (saved services + reminders persistence), §10 Civic Knowledge Explorer, §12/§13 trust/source inspector panels, §19 notification center SASI/Official split, §32 Low Data Mode.
- Unresolved/risks: (1) standalone-boot condition verified by API check + action-path reuse (headless cannot emulate display-mode: standalone — needs one manual check on a real installed device); (2) document-explanation intent routes to Ask SASI until the Documents workspace exists (honest); (3) GUIDE routes to services until a journeys view exists.
- Carried: zu/af native proofing, state-route userId migration, dev.log rotation, map glyph tiles, cluster list mobile parity, OpenRouter key rotation.
- Next-phase priorities: (1) §4/§5 structured AI answers (Answer / Requirements checklist / Your next step / Official sources / Related + contextual action chips) in ask-sasi; (2) §8 My SASI: saved services + reminders persistence (Prisma profile JSON + state API); (3) §7 journeys view with per-step Ask SASI context; (4) §10 Civic Knowledge Explorer browse page; (5) §19 notification center split.
---
Task ID: 27
Agent: Z.ai Code (main)
Task: User — "Remove all loading bars and progress bars and implement a fully functional database system with SQL etc. Using SupaBase and please make this database super secure and un breakable and un hackable … CONFIRM EMAIL IS OFF. Use Email only no ID or google etc. PUBLISH LATEST CHANGES TO GITHUB THEN MAKE A SERVER TO HOST WEBSITE USING GITHUB FOR FREE WITH THE NAME SASI … GIVE A FULL DEVELOPMENT REPORT IN ENGLISH."

Work Log:
- PROGRESS/LOADING BAR PURGE (all bar-style meters removed, information preserved as text/chips):
  * primitives.tsx ConfidenceBar → ConfidenceChip (labelled chip + confidence dot, no meter); call sites updated (landing.tsx trust band, domain.tsx findings).
  * CONFIDENCE_META lost its width field, gained dot colours (utils.ts).
  * investigate.tsx overall-confidence animated meter removed (label + findings count text kept).
  * report.tsx wizard progress bar removed (step chips above already carry position).
  * case-detail.tsx case progress bar removed (% + hint text kept).
  * signup.tsx password-strength 4-segment bar → "Strength: <label>" text + colour dot.
  * briefing-card.tsx risk meter track removed (risk label stays in header); RISK_METER_FILL deleted.
  * src/components/ui/progress.tsx (unused shadcn Progress) DELETED. Verified: zero role="progressbar", zero width-percentage meters outside the admin bar chart (a data chart, not a progress bar).
- SUPABASE INTEGRATION (user-supplied project awcceckvuiarlbnzemsv):
  * @supabase/supabase-js@2.116 + @supabase/ssr@0.12 installed; env vars in .env (gitignored) + committed .env.example template (fixed .gitignore `.env.*` → now allows `!.env.example`).
  * src/utils/supabase/{client,server,middleware}.ts created exactly per the user's integration recipe (publishable key only; server helper for RSC/route handlers).
  * src/proxy.ts (Next 16 "proxy" convention, replaces deprecated middleware.ts): Supabase session refresh + security headers on every response — X-Content-Type-Options, X-Frame-Options DENY, Referrer-Policy, Permissions-Policy (camera/mic/geo same-origin), HSTS in production. Verified via curl.
  * NEW /api/sasi/system-status: honest infrastructure report (provider, primary DB ok, Supabase reachable, schemaApplied, anonLocked). Surfaced live in Settings → App & offline as "Cloud database (Supabase)" StatusRow with truthful states only.
- POSTGRESQL ON SUPABASE (sandbox cannot reach port 5432 — solved via GitHub Actions):
  * scripts/gen-pg-schema.mjs derives prisma/schema.postgres.prisma from the single SQLite source of truth (identical models, postgres datasource).
  * supabase/migrations/0001_init.sql generated offline (prisma migrate diff, 159 lines DDL, committed for inspection/manual SQL-Editor use).
  * supabase/rls-hardening.sql: RLS ENABLED on every public table (no policies = zero rows) + ALL privileges revoked from anon/authenticated + schema USAGE revoked → the publishable key can do nothing; Prisma (postgres owner) unaffected; idempotent.
  * .github/workflows/supabase-db.yml: on push/dispatch — derives pg schema, prisma db push, applies hardening, seeds golden-path QA account (repo secrets only; GitHub runners CAN reach 5432).
  * .github/workflows/ci.yml: lint + tsc quality gate on every push.
- SECURITY (email-only, defence in depth):
  * Auth audit: NextAuth credentials provider is the ONLY provider (email+password, no OAuth, no magic links) — matches "Use Email only"; confirm-email is a Supabase Auth dashboard setting (already OFF per user) and unused by our stack.
  * Login brute-force throttle added: 10 attempts/email/5min inside authorize() (reuses rateLimit; throttled attempts indistinguishable from wrong password).
  * Secrets discipline: SUPABASE_SECRET_KEY + DB password only in .env/GitHub encrypted secrets/Vercel dashboard — never in client code or the repo (repo is PUBLIC; verified .env gitignored).
- LIVE VERIFICATION: /api/sasi/system-status returns database{provider:sqlite,ok:true} + supabase{configured:true,restReachable:true,schemaApplied:false,anonLocked:true} — honest pre-sync state; headers verified; dev server healthy; lint PASS; tsc clean for src/.
- Files: src/utils/supabase/* (NEW), src/proxy.ts (NEW), src/app/api/sasi/system-status/route.ts (NEW), scripts/gen-pg-schema.mjs (NEW), supabase/{migrations/0001_init.sql,rls-hardening.sql} (NEW), .github/workflows/{supabase-db,ci}.yml (NEW), DEPLOY.md (NEW), vercel.json (NEW), .env.example (NEW), plus the 9 edited component/lib files listed above; bun.lock/package.json (+2 deps).

Stage Summary:
- SASI now has zero loading/progress bars anywhere (confidence/strength/risk/wizard progress are all text+colour), a fully hardened Supabase PostgreSQL pipeline (schema + RLS auto-applied by GitHub Actions), the exact Supabase client/server/middleware integration requested, an honest live database status surface in Settings, a login brute-force throttle, security headers via the Next 16 proxy convention, a CI quality gate, and a complete free-hosting runbook (DEPLOY.md + vercel.json).
- Unresolved/risks: (1) sandbox cannot reach Supabase:5432 → schema push happens in GitHub Actions (triggered by this push; verify Actions run green + system-status flips to schemaApplied:true); (2) user pasted secret keys in chat → recommend rotation (Supabase → Settings → API) and update of GitHub/Vercel secrets afterwards; (3) Vercel deploy needs the user's 5-minute account connect (steps in DEPLOY.md) — name suggestions sasi-app/getsasi if sasi.vercel.app is taken; (4) in-memory rate limiting is per-process (documented; Redis/Upstash upgrade path).
- Carried: zu/af native proofing, state-route userId migration, dev.log rotation, map glyph tiles, cluster list mobile parity, OpenRouter key rotation, master-prompt §4/§5 structured AI answers.
- Next-phase priorities: (1) confirm Actions sync + live Supabase verification; (2) master-prompt §4/§5 structured AI answer sections; (3) §7 journeys view; (4) §8 My SASI persistence upgrade.

Task 27 — POST-PUSH EXECUTION & LIVE VERIFICATION (same task, continued):
- GITHUB ACTIONS CYCLE: push 89f65d9 auto-triggered both workflows. Run 1 failed (race: secrets were created seconds AFTER the run started → DATABASE_URL empty). Root-caused from runner logs, re-ran. Run 2 failed with P1001 — Supabase direct host db.<ref>.supabase.co:5432 is IPv6-ONLY (confirmed: AAAA 2a05:d018…, no A record; sandbox and GitHub runners are IPv4). FIX: switched the repo secret to the IPv4 SESSION POOLER — discovered the project's live pooler host empirically by authenticating against candidate hosts: aws-1-eu-west-1.pooler.supabase.com:5432 (user postgres.<ref>) → SUCCESS. Re-run → ALL GREEN.
- LIVE SUPABASE STATE (verified via REST): secret-key probe `GET /rest/v1/User` → 200 with the seeded golden.path@sasi.test row (schema live, seed job worked). Publishable-key probe → **401 PGRST… "permission denied for table User" (42501)** → RLS + grant/schema revocation is ENFORCED: the browser key can do nothing. /api/sasi/system-status now honestly reports {provider:sqlite(dev), supabase:{restReachable:true, schemaApplied:true, anonLocked:true}} — and Settings → App & offline renders exactly that line live.
- CI: typecheck was failing on pre-existing example/skill dirs → tsconfig excludes (examples, skills, mini-services) scoped to app code; tsc now fully clean locally AND in CI (both workflows green on run 2).
- AGENT-BROWSER QA (desktop 1280 + mobile 390): landing renders (white h1, five actions, composer); report wizard has 0 role=progressbar and 0 full-width bar elements (step chips only); signup password strength renders "Strength: Excellent" as text+dot with 0 segment bars; settings Cloud database row live-updates from the real endpoint; login with golden.path@sasi.test works (10/5min throttle transparent to legit users) → straight to dashboard; 390px zero horizontal overflow; stale-console "Export ConfidenceBar" errors were pre-fix buffer entries — page renders clean, dev.log has 0 error lines after full restart + .next wipe.
- Repo state: 89f65d9 (main Task 27) + fa48a91 (tsconfig scope fix). GitHub secrets set (encrypted): SUPABASE_DIRECT_DATABASE_URL (pooler), NEXTAUTH_SECRET, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SECRET_KEY. Cron review job recreated as 380546 (old 380436 disabled by exec limits).

---
Task ID: 28-1
Agent: main
Task: Task 28 Phase 1 — PostgreSQL everywhere (dev/CI/prod) + schema for Civic Intelligence + view scaffolding; deliver all user-provided keys for Vercel setup.

Work Log:
- Delivered ALL user-provided keys (Supabase URL/publishable/secret, DB password, JWT key id, pooler URLs) + exact Vercel env-var table in the reply (user was waiting on the Vercel import page).
- Verified sandbox CAN reach Supabase IPv4 session pooler (aws-1-eu-west-1.pooler.supabase.com:5432) → dev on real PostgreSQL is now practical.
- prisma/schema.prisma flipped to datasource provider "postgresql" (single source of truth; SQLite fork retired). Local db/custom.db file left untouched (nothing destroyed).
- Added 4 models for the civic-intelligence phases: JourneyRun (progress, ACTIVE/PAUSED/COMPLETED, stepsDone JSON), SavedItem (session+kind+item unique), Reminder (SASI-side, never official), DocumentRecord (metadata + AI analysis only, never raw bytes).
- ROOT-CAUSED shadowed env: sandbox exports DATABASE_URL=file:...custom.db in the shell, which overrides .env for both Prisma CLI and Next. Permanent fix: package.json scripts (dev/start/db:push/postdb:push/db:*) now source .env first via `bash -c 'set -a; . ./.env; set +a; exec ...'`.
- .env: DATABASE_URL = Supabase session pooler (postgresql://postgres.<ref>:<pw>@aws-1-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require).
- bun run db:push against Supabase → schema in sync (9.6s) + postdb:push golden-path hook ran against Postgres ("already exists").
- .github/workflows/supabase-db.yml now applies prisma/schema.prisma directly (no derive step). scripts/gen-pg-schema.mjs repurposed: regenerates supabase/migrations/0001_init.sql (243 lines, 4 new tables included) via prisma migrate diff. Stale prisma/schema.postgres.prisma deleted; vercel.json buildCommand simplified to `prisma generate && next build`.
- Dev server restarted on PostgreSQL: /api/sasi/system-status reports {database:{provider:"postgres",ok:true}, supabase:{schemaApplied:true,anonLocked:true}}.
- Scaffolded 3 new app views (agent conflict-free partition): types.ts View union + store.ts ALL_VIEWS + sasi-app.tsx imports/VIEW_COMPONENTS + app-shell.tsx sidebar (Journeys under Investigate w/ Route icon, Documents under Evidence w/ FileText icon) + i18n keys (en/zu/af) + placeholder views/journeys.tsx, journey.tsx, documents.tsx. Lint PASS.

Stage Summary:
- Dev = CI = prod = Supabase PostgreSQL (no behavioural fork). RLS hardening auto-covers new tables (DO-loop over all public tables).
- Next: agents 28-a (auth+security+rate-limit service), 28-b (structured AI answers+actions), 28-c (service registry+journeys+My SASI), 28-d (document intelligence).
---
Task ID: 28-d
Agent: full-stack-developer
Task: Phase 15 DOCUMENT INTELLIGENCE — /api/sasi/documents (upload→analyse→summary/info/dates/actions→ask→delete) + full Documents view rewrite.
Work Log:
- NEW src/app/api/sasi/documents/route.ts: POST (multipart, fields file+sessionId) → validates size (5MB → honest 413), type (allowlist text/plain, text/markdown, text/x-markdown, text/csv, application/csv + image/png, image/jpeg, image/webp; extension fallback when browser sends empty type; PDF → honest 415 "PDF parsing is not supported yet"), extracts text (UTF-8, capped 20,000 chars) or, for images, one z-ai createVision call following vision/route.ts pattern; ONE z-ai chat completion (briefing/ask pattern, thinking disabled) with a SASI document-analyst system prompt demanding STRICT JSON {summary, importantInfo[], dates[{label,value,iso?}], actions[]} under absolute-honesty rules (only in-document facts, [] when a section is absent, never invent dates/amounts/requirements, honest "unreadable" summary when garbled, SA civic context). Server-side validation trims/types only (caps: 8×240 items, 8 dates with ISO YYYY-MM-DD check, summary ≤700) — parse failure → summary=null/keyInfo=null + honest analysisError, row still stored, never a fake analysis.
- Ownership EXACTLY like state/route.ts canonical pattern: cleanSession; rows keyed by sessionId; getAuthSession() userId attached when present (ownerScope = {OR:[sessionId, userId]}); GET/DELETE scoped to that owner only — verified cross-session: foreign GET list empty, foreign DELETE 404. GET: newest-first take 50, keyInfo parsed. DELETE: deleteMany {id, ...ownerScope} → {deleted:true} or honest 404; immediate+permanent.
- Rate limit: started with rateLimit() import but Agent 28-a's refactor landed mid-task → migrated to the new canonical rateLimitService.limit() + tooManyRequests() (12 uploads/5min per owner, Retry-After header). tsc caught the signature change.
- Persistence: prisma DocumentRecord only (sessionId, userId?, name sanitised+path-stripped, mimeType, sizeBytes, summary?, keyInfo JSON?) — raw file bytes NEVER stored, so the privacy line is literally true.
- REWROTE src/components/sasi/views/documents.tsx (view already registered in router; store/sasi-app/app-shell/types/i18n untouched): honest empty state + "Upload a document" CTA; glass dropzone (label+hidden input, drag-over gold ring, sr-only input, .sasi-card border dashed via inline style because .sasi-card is unlayered CSS and beats utility classes); chosen file name/size/type shown before "Analyse document" confirm; while in flight the dropzone is aria-disabled/pointer-events-none + ThinkingDots + useRotatingStatus honest stages + "Analysing…" chip (NO loading bars anywhere); honest inline upload error with "Try another file" retry; client quickReject mirrors server rules for instant feedback (server still authoritative).
- Document card: name/size/mime/date; Summary; "Important information" bullets (max-h-48 overflow-y-auto); "Important dates" label+value rows (gold CalendarDays); "Required actions" read-only checklist (green Check squares) + "not reminders SASI has set" note; stored-but-unanalysed rows show an honest red "Analysis unavailable" chip + explanation; "Ask SASI about this document" + Delete (44px h-10 targets, full-width stack at 390px).
- Ask SASI prefill: ask-sasi.tsx has NO param/event prefill and I don't own it → navigate("ask-sasi", question) puts the question in the URL param AND dispatches window CustomEvent "sasi:prefill-question" {question}; honest toast shows the exact question. ASK-SASI MUST ADD A LISTENER for "sasi:prefill-question" (or read the view param) to drop it into the composer — reported as the follow-up.
- Delete UX: shadcn AlertDialog "Delete “name”?" → "Deleted immediately and permanently. This cannot be undone — SASI keeps no copy…" → optimistic remove + honest toast + index-accurate restore on failure.
- Privacy TrustNotice verbatim in view: "Documents are analysed to produce their summary. The original file is never stored — only the analysis you see here."
Stage Summary:
- Endpoint contract: POST /api/sasi/documents (multipart file+sessionId) → 200 {id,name,mimeType,sizeBytes,createdAt,summary,keyInfo{importantInfo,dates,actions}|null,analysisError|null}; 413 >5MB; 415 PDF/unsupported; 400 no sessionId/file/empty; 429 rate-limited. GET ?sessionId= → {documents:[…]} owner-scoped, ≤50, newest first. DELETE ?sessionId=&id= → {deleted:true}|404.
- QA all green: bun run lint 0 errors; bunx tsc --noEmit 0 errors; curl POST realistic City of Cape Town bill → 200 with grounded summary/4 dates/4 actions (zero fabrication); cross-session scoping verified both directions; PDF 415, 5.7MB 413, missing-sessionId 400; agent-browser 390px full flow (upload→analysing chip→card→Ask SASI→AlertDialog delete→empty state) with console clean and 0px horizontal overflow.
- Deferred/for-next agents: (1) ask-sasi must listen for "sasi:prefill-question"; (2) PDF text extraction (needs a parser lib — honestly rejected for now); (3) re-analysis of unanalysed rows if/when wanted; (4) i18n keys for the view copy (i18n.ts not owned by this task).

---
Task ID: 28-a
Agent: general-purpose
Task: Phases 2/3/4 — auth consolidation, security hardening of owned /api/sasi routes, RateLimitService abstraction, account deletion (API + Profile UI), CSP in proxy.
Work Log:
- RATE LIMIT SERVICE (src/lib/sasi/api-auth.ts rewritten): export interface RateLimitStore { check(key, limit, windowMs) → { allowed, remaining, retryAfterMs } }; InMemoryRateLimitStore keeps the fixed-window Map behaviour, adds 60s periodic pruning + a 10k-bucket hard cap (expired-first eviction) so key floods cannot grow memory unbounded; factory reads RATE_LIMIT_STORE ("memory" default, honest throw on unknown values, memoised); export const rateLimitService.limit() is the single entry point; legacy rateLimit() kept as a thin async wrapper for external call sites; added shared helpers clientIp() + tooManyRequests() (429 + Retry-After). Commented upgrade path documented in-file: Upstash REST (INCR+PEXPIRE) or a RateLimitHit table via a FUTURE migration — no fake persistence (schema frozen).
- ALL call sites switched to the service: login throttle (lib/auth.ts, 10/5min/email — verified async), signup (IP 5/hour + NEW per-email 5/hour so header-rotation can't flood one address).
- NEW /api/sasi/auth/account DELETE (account deletion): requireUserId (401 without a real NextAuth session) → 5/10min throttle → atomic $transaction: delete AuditEvent + SubmissionRecord (FK order), detach CaseRecord/JourneyRun/SavedItem/Reminder/DocumentRecord (userId=null, rows stay with the browser session), delete User → { deleted: true }; honest 404 when the account is already gone; generic 500 (transaction = "nothing was changed" is true).
- PROFILE UI delete flow (views/profile.tsx): "Danger zone" card in the existing visual language (sasi-card border-[#ef5350]/15, Trash2 icon, #fda4a0 text), shadcn AlertDialog with typed-email confirmation (button disabled until match), one-line explanation of what is removed, DELETE → on success nextAuthSignOut + store signOut + navigate(landing), honest sonner error toasts on failure; only rendered when a signed-in identity (me.email) exists. Browser-verified: dialog opens, wrong email → disabled, correct email → enabled, cancel leaves account intact.
- STATE ROUTE hardening (canonical pattern now): rejects the client's shared fallback sessionId "sasi-anon" on GET/POST/DELETE (storage-blocked browsers used to share ONE public bucket — a real cross-user exposure; private-mode users now degrade to no persistence honestly); authenticated callers now BIND CaseRecord rows — writes stamp/claim userId, a row claimed by one account returns 403 to any other caller's rewrite, reads include only unclaimed + own rows (logout does NOT hide the user's cases); cross-session overwrite guards for evidence (evidenceId) and notifications (notificationId); payload size caps (case/evidence 200k, briefing 100k, notification 20k chars) with honest 413s; POST throttled 120/min per session (spec) — burst-tested: exactly 120×200 then 429 with Retry-After.
- AI/abuse throttles via the service (all keyed per IP for anonymous callers): briefing 20/min + 500KB content-length guard, vision 12/min + early 7.5MB content-length rejection (MIME regex + base64 cap already existed), geo/enrich 30/min (Nominatim usage policy), system-status 10/min (2–3 outbound probes per call), audit POST 60/min per user.
- me route: switched getServerSession(authOptions) → getAuthSession() and lookup by session.user.id (JWT sub) instead of email.
- PROXY (src/proxy.ts): REMOVED the Supabase session refresh — verified dead: the ONLY supabase.auth call in the repo lived in utils/supabase/middleware.ts, nothing ever creates a Supabase Auth session (SASI auth is 100% NextAuth credentials/JWT) → deleted src/utils/supabase/middleware.ts; proxy now builds NextResponse.next() directly. KEPT + STRENGTHENED headers: ADDED Content-Security-Policy exactly per spec (default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; connect-src 'self' + supabase/nominatim/openrouter/openstreetmap; font-src 'self' data:; frame-ancestors 'none'; base-uri 'self'; form-action 'self') and Cross-Origin-Opener-Policy same-origin; nosniff/DENY/Referrer-Policy/Permissions-Policy/HSTS unchanged.
- AUTH AUDIT (Phase 2): NextAuth v4 credentials (bcrypt cost 12, dummy-hash constant-work compare) is the only provider; signup is the only creation path; session.user.id = JWT sub drives all server authorization; client gate = store PUBLIC_VIEWS (deep links land on login); every owned API enforces auth server-side. src/utils/supabase/{client,server}.ts are grep-verified UNUSED by anything — kept (user-requested integration deliverables from Task 27, auth-inert client factories) and flagged for the coordinator to delete or wire up deliberately.
- LIVE VERIFICATION: end-to-end NextAuth login smoke test via curl (csrf → credentials callback → session cookie → session.user.id → /api/sasi/me returns the real DB user); UI login via agent-browser to dashboard; ownership tests (anon create → authed save claims → anon overwrite 403 → owner re-save 200 → authed GET shows own); "sasi-anon" rejected on all three verbs; state POST burst = 120 OK then 429; delete dialog gating verified in-browser (golden-path QA account NOT deleted); CSP header present and well-formed on / and /api; zero CSP-violation console messages; dev.log clean (no compile errors, no CSP reports).
- Files: src/lib/sasi/api-auth.ts (rewritten), src/lib/auth.ts, src/app/api/sasi/state/route.ts (rewritten), src/app/api/sasi/auth/{signup/route.ts, account/route.ts (NEW)}, src/app/api/sasi/{briefing,vision,geo/enrich,system-status,audit,me}/route.ts, src/proxy.ts, src/components/sasi/views/profile.tsx, DELETED src/utils/supabase/middleware.ts.
Stage Summary:
- Security posture: shared-session data leak closed; per-account row binding + anti-rewrite guards on the state route; cross-session evidence/notification overwrite closed; payload caps on every persisted JSON body; every AI/external-call endpoint now throttled through one auditable RateLimitService; signup flood-resistant (IP + email); account deletion is real, atomic, honest and throttled; CSP + COOP added and the dead Supabase auth hop is gone from the request path (one less outbound call per request, one less surface).
- Deferred (needs non-frozen schema or other agents' files): userId columns for ChatMessage/EvidenceRecord/BriefingRecord/NotificationRecord/Profile (still sessionId-scoped only — a future migration should add them); client fix for the "sasi-anon" fallback in lib/sasi/utils.ts (server now rejects it, so storage-blocked browsers get no persistence; a per-load random id would restore it safely); distributed rate-limit store (Upstash/RateLimitHit) documented in api-auth.ts; CSP nonce hardening to drop 'unsafe-inline'/'unsafe-eval'.
- Gates: bun run lint → 0 errors; bunx tsc --noEmit → 0 errors (note: a transient civic-ai.ts breakage at 23:50 was a parallel agent's in-flight edit, fixed by them at 23:53 — final run green); system-status 200 (postgres ok, supabase schemaApplied, anonLocked); landing 200 with CSP; login verified end-to-end (curl + real browser).
---
Task ID: 28-c
Agent: full-stack-developer
Task: Task 28 Phases 8+9+11 — SERVICE REGISTRY (relational), SERVICE JOURNEYS (persistent runner), MY SASI (real personal area) + 4 owned API routes.

Work Log:
- src/lib/sasi/services-registry.ts (NEW, client-safe): 17 ServiceRegistryEntry records — 7 guided services with `${slug}-apply` journeys (passport, smart-id, birth-certificate, sassa-grants, driver-licence, vehicle-renewal, police-clearance) + 10 category entries aligned with ServiceKey slugs (documents, safety, local-government, water, electricity, roads, waste, healthcare, education, housing). officialSource.verified=true ONLY for certain URLs: dha.gov.za (DHA entries), sassa.gov.za, saps.gov.za, gov.za (driver-licence/vehicle-renewal/local-government umbrella); water/electricity/roads/waste/healthcare/education/housing get officialSource:null with the responsible department named — honesty over completeness. requirements/requiredDocuments are real commonly-documented items (DHA-73, DL1, SAPS fingerprints, SASSA means-test docs etc.); categories carry empty lists by design. JOURNEY_STEPS map (4-5 honest steps per journeyId) + journeyStepsFor(); lookups registryEntryBySlug/registryEntryByJourneyId/JOURNEY_ENTRIES; LOCATION_CATEGORY_LABELS (home-affairs, sassa-office, police-station, dltc, licensing-office). Client-safe DTOs exported (JourneyRunDTO/SavedItemDTO/ReminderDTO/ConversationDayDTO/MySasiResponse/AppNotificationLiteDTO) so no server type ever leaks into views. NOTE: data.ts no longer ships a SERVICES array (user-generated-only rewrite) — the registry reuses utils.ts SERVICES categories + types.ts ServiceKey for alignment instead.
- /api/sasi/journeys (NEW): GET ?sessionId= → runs (stepsDone JSON→number[], payload JSON→snapshot, orderBy updatedAt desc, take 50); POST { sessionId, journeyId, status ACTIVE|PAUSED|COMPLETED, stepsDone? } → upsert on (sessionId, journeyId); journeyId validated against the registry — unknown journeys rejected 400 "Unknown journey…"; payload snapshot built SERVER-SIDE from the registry (client cannot inject fabricated journey content); userId attached from getAuthSession() when present (never trusted from client); stepsDone sanitized (int array ≤100); ownership pattern copied exactly from state route (cleanSession 128).
- /api/sasi/save (NEW): POST { sessionId, kind∈service|journey|answer|info, itemId, payload? } idempotent upsert on (sessionId, kind, itemId) → { saved:true }; kind=service slugs validated against registry (unknown → 400); GET ?sessionId= list (take 100 desc); DELETE ?sessionId=&kind=&itemId= → { saved:false, deleted:n }.
- /api/sasi/reminders (NEW): POST { sessionId, title≤200, note?, dueAt? ISO-validated } → create (userId from server session); GET pending-first (orderBy done asc, createdAt desc); PATCH { sessionId, id, done } → 404 when row not found; DELETE ?sessionId=&id= → 404 when not found. SASI-side only.
- /api/sasi/mysasi (NEW): single GET ?sessionId= → Promise.all of JourneyRun(ACTIVE|PAUSED, take 12) + SavedItem(50) + Reminder(pending-first, 50) + ChatMessage(last 20 desc→asc, grouped into ConversationDayDTO by UTC day, newest day first) + NotificationRecord(last 20, payload parsed + read column wins). Every array scoped to sessionId; corrupt JSON rows skipped; honest empty arrays; never fabricates rows.
- views/journeys.tsx (REWRITTEN): catalogue of JOURNEY_ENTRIES cards — title, department, requirements/documents counts, locationCategory chip, per-entry source trust line (verified → green BadgeCheck line, none → grey "no single official link verified — confirm through <department>"), live run state via GET /api/sasi/journeys → CTA "Start journey" / "Resume · N steps done" / "Review checklist" + status pill; TrustNotice warning banner (checklist ≠ government application); skeleton cards while loading (NO bars), honest error EmptyState with retry; "How journeys work" section (progress saved in SASI only, submission stays official, requirements can vary); fetch is single-shot, no polling.
- views/journey.tsx (REWRITTEN runner): registry lookup by param journeyId — unknown → honest EmptyState. Not started → step list read-only + Start (POST ACTIVE, payload snapshot server-built). Started → per-step completion toggles (aria-pressed, ≥44px) persisted via debounced POST (600ms, timer flushed on unmount), progress as TEXT "N of M steps done" (no bars), Pause/Resume reflecting status, "Mark checklist complete" offer when all done + green completion line that explicitly states "No government application has been made through SASI". Standing TrustNotice: "Completing this SASI checklist does not complete an official government application. Follow the official source for submission." Requirements + Documents-to-bring sections from the registry, OfficialSource card (verified → external link; unverified → confirm-directly note), Nearby mini-link to map with locationCategory. Optimistic UI + honest toast on save failure.
- views/activity.tsx (REWRITTEN → MY SASI, route name "activity" kept): single fetch of /api/sasi/mysasi on mount (no polling), five sections — Continue (active/paused journeys with real step counts from registry steps, tap → journey), Saved (open by kind: service→service-detail, journey→journey, answer→ask-sasi with pendingAsk, info→view; Unsave via DELETE with optimistic revert), Reminders (inline create title+optional date, done toggle PATCH, delete, every row labelled "SASI reminder · not from government", overdue shown red, TrustNotice on the section), Recent conversations (day-grouped chat from the aggregate, tap → Ask SASI to continue), Notifications (reuses NotificationRow + the exact mapRef logic from notifications.tsx, mark-read via store, See-all → notifications view). Honest EmptyState for every empty section, skeleton rows while loading, full-page error state with retry. Lists get sasi-scroll max-h-96 overflow-y-auto.
- views/service-detail.tsx (EDITED, graceful): registry lookup by slug; category slugs (documents, safety, water…) keep 100% of the existing page and gain a "Service guide" section (JourneyCta when journeyId, Requirements, Documents to bring, Official source block via OfficialSource/TrustNotice, Related-services chips navigating service-detail, Nearby row navigating map with locationCategory); registry-only slugs (passport, smart-id, sassa-grants, driver-licence, vehicle-renewal, police-clearance, birth-certificate) render a full RegistryServiceDetail page (header, department, summary, guide sections, closing TrustNotice) instead of the old "Service not found" — unknown slugs still hit the original EmptyState untouched. Added SaveServiceButton (bookmark toggle → /api/sasi/save, idempotent, honest toast, 44px target) on registry-backed pages.
- CROSS-AGENT NOTE: fixed ONE line in src/lib/sasi/civic-ai.ts (agent 28-b's file — not in my do-not-edit list, unblocked the shared tsc gate): their tolerant RegistryEntry interface lacked `summary?: string` while serviceDetailBlock() reads entry.summary; added the optional field (matches my registry's required summary field; purely additive, enables their intended grounding line).
- QA: bun run lint → 0 errors; bunx tsc --noEmit → 0 errors; curl gates all green (journeys POST 200 + GET row + unknown-journey 400; save POST idempotent 200 + unknown-service 400 + GET + DELETE; reminders POST/GET/PATCH/DELETE 200 + 404-shape paths; mysasi 200 with real rows + honest empty arrays; missing sessionId → 400); GET /, /?view=journeys, /?view=activity, /?view=service-detail&p=documents all 200; dev.log tail clean — no compile errors caused. Test rows left under sessionId "agent-c-test" as smoke data (1 active passport run).
Stage Summary:
- Phases 8/9/11 live: the service registry is the honest backbone (17 entries, 7 guided journeys, verified sources only for gov.za/dha/sassa/saps), journeys persist per (sessionId, journeyId) with pause/resume and server-built snapshots, and My SASI is a real workspace fed by one aggregate fetch — continue journeys, saved items with open/unsave, SASI-labelled reminders, day-grouped conversations, and real notifications; service-detail now upgrades gracefully from the registry and can save services to My SASI.
- Deferred / notes: (1) Ask SASI answer-saving (kind="answer") + journey saving from ask-sasi belongs to agent 28-b's UI — the save API already accepts them; (2) map view currently ignores the locationCategory param (parallel work owns map context — navigation is safe either way); (3) sidebar/nav label for the activity route still reads "Activity" via i18n keys (i18n.ts not mine to edit); (4) endpoints are per-session like the state route — signing in attaches userId for stronger ownership but query scoping stays sessionId (state-route parity, flagged for the existing "state-route userId migration" carry).

---
Task ID: 28 (main)
Agent: main
Task: Task 28 — Production hardening + Civic Intelligence (Phases 1-4, 6-11, 15 delivered; QA + atomic commits + push).

Work Log:
- Phase 1 (28-1): PostgreSQL everywhere (dev pooler, CI, prod). See entry 28-1.
- Phase 2/3/4 (agent 28-a): RateLimitService abstraction; ownership binding on state API (claimed rows 403 foreign rewrites); shared "sasi-anon" bucket killed server+client; per-email signup throttle; payload caps 413; account deletion endpoint + danger-zone UI (typed-email confirm); CSP+COOP in proxy; dead Supabase auth refresh removed.
- Phase 6/7/10 (agent 28-b, backend delivered before context death; view wired by main): structured answers (answer/what-you-need/next-step/service/journey/official-source/related — only-present sections), registry grounding + strict server validation + corroborate pass, 8 real AI actions with honest failures, sasi_ctx cookie context folding, prefill hand-off from Documents.
- Phase 8/9/11 (agent 28-c): SERVICE_REGISTRY (17 services, verified-only official sources), journeys catalogue + runner (start/pause/resume/step completion persisted in JourneyRun, honesty banner), save/reminders/mysasi endpoints, My SASI rebuilt on real DB state, service-detail registry guide.
- Phase 15 (agent 28-d): document intelligence — upload→analyse→summary/info/dates/actions, metadata-only storage, immediate permanent delete, owner-scoped, 5MB cap, honest 413/415.
- Integration fixes by main: answer-actions missing sessionId (Save/Create Reminder 400 → fixed + verified live); structured rendering + store wiring completed after 28-b died; getSessionId storage-blocked fallback made per-load random.
- QA (agent-browser, desktop 1280 + mobile 390): login golden path vs Postgres ✓; journeys catalogue → passport runner → 2 steps ticked → reload shows "Resume · 2 steps done" ✓; Ask SASI structured answer with What-you'll-need/Next-step/Related + Start Journey/Save/Verify/Create Reminder actions ✓; Save → My SASI shows saved answer with open/remove ✓; service-detail passport registry guide ✓; documents honest empty state + privacy line ✓; 0px horizontal overflow at 390px on journeys+documents; console clean (one pre-existing next/image sizes hint); dev.log clean; lint 0 errors; tsc clean.
- Atomic commits a862b5e → aa4be37 (security / foundations / structured AI / journeys / My SASI / documents) then QA docs.

Stage Summary:
- SASI now behaves as one connected system: ASK → structured answer → SERVICE/JOURNEY → SAVE → RETURN, all persisted in Supabase PostgreSQL with RLS deny-by-default.
- Deferred (next iterations): universal search consolidation into shell (Phase 12), explore view (13), Verify output upgrade (16), report wizard structured authority routing (17), map locationCategory context (18), official vs SASI notification split polish (19), remaining 8 languages for i18n (20), full performance pass (23).
- Risks: in-memory rate limiting per-process (documented upgrade path); session-scoped tables without userId columns (future migration); user should rotate Supabase keys/DB password pasted in chat; Vercel env vars provided (user deploying via vercel.com/new/import pre-filled for AndersonAzariah/sasi).
---
Task ID: 29-search-ui
Agent: Z.ai Code (subagent)
Task: Task 29 Phase 12 — Universal SASI Search UI (new component replacing the command-palette experience), mounted later by coordinator.

Work Log:
- Read worklog (Task 26→28 entries) + contracts first: src/lib/sasi/search.ts (searchSASI/detectIntent/SavedSearchInput/SearchHit), store.ts (commandOpen/setCommandOpen/navigate/setPendingAsk/openService/authed), utils.ts getSessionId(), services-registry.ts registryEntryBySlug, globals.css design classes, api/sasi/save route (GET shape), command-palette.tsx (keydown + visual language to supersede).
- NEW src/components/sasi/universal-search.tsx — `export function UniversalSearch()`:
  * Global keys copied from old palette: (metaKey||ctrlKey)+K toggles setCommandOpen, Escape closes (window keydown, deps [open,setOpen]).
  * Reset via React's adjust-during-render pattern (resetKey open/closed + query) — every open starts with a fresh query + selection 0; keystrokes return selection to top; NO setState-in-effect (new react-hooks rules) and no cascading renders.
  * Saved items: one GET /api/sasi/save?sessionId=getSessionId() per open, ONLY when store authed; mapped to SavedSearchInput{kind,itemId,title:payload?.title}; accepts {items} AND {saved} (brief said {saved}, real API returns {items} — both handled); failure or signed-out → silently [] (no fabricated Saved group).
  * Engine: searchSASI(query, saved) in useMemo per keystroke — no debounce, no server call. Groups rendered with sticky mono SectionLabel headers (bg-[#0b0b0d]/95 backdrop-blur, px-3 py-2): ASK SASI / SERVICES / JOURNEYS / ORGANISATIONS / TOPICS / INFORMATION / SAVED / ACTIONS; empty query: POPULAR (passport, smart-id, sassa-grants from registryEntryBySlug — row skipped if registry entry absent, never invented) / ASK SASI / SHORTCUTS (Explore SASI → explore).
  * First row ALWAYS "Ask SASI: “<query>”" (query non-empty) → setPendingAsk(q)+navigate("ask-sasi"); empty-query Ask row clears pendingAsk. Intent rows honestly routed: nearby → "Open Nearby map" → map; verify → "Verify this information" → verify; report → "Report an issue" → report.
  * Navigation per hit: service → openService(slug) (store-canonical, closes dialog); journey → navigate("journey",param); org/topic → navigate("explore","org:<id>"/"topic:<id>"); information → navigate(hit.view); saved service → openService, saved journey → journey, saved answer/info → engine-provided view. navigate()/openService() already set commandOpen:false — dialog closes itself on activation.
  * Row anatomy: per-group lucide icon (ShieldCheck/Route/Landmark/BookOpen/FileText/Bookmark/Sparkles gold #e3c567/Navigation/ScanSearch/Flag/Compass), truncated title+subtitle (11.5px zinc-500), green BadgeCheck (#66bb6a — house verified color) ONLY from hit.verified, min-h-[44px] touch targets, selection bg-white/[0.05] + text-white.
  * Keyboard: ArrowUp/Down wrap-around selection with scrollIntoView(block:nearest), Enter activates; role=listbox/option + aria-selected + aria-activedescendant (ids sasi-search-opt-N); Enter/rows clamped against stale index.
  * Honest empty state (query non-empty, total===0, no intent): "Nothing matches “<q>” in SASI's registries" + "Ask SASI instead — it can explain beyond the registry" (role=status), Ask row remains the only selectable row.
  * Dialog: desktop sm+ centered max-w-xl at 15vh, rounded-xl, bg-zinc-950/95 backdrop-blur-xl, border-white/10, sasi-pulse-multi gradient ring, AnimatePresence fade/scale (0.18s cubic-bezier), overlay click closes; mobile <640: w-full sheet docked to top-0, no horizontal margins, rounded-b-xl; input row sasi-command-focus glow, Search icon, autoFocus, sr-only label, placeholder "Search SASI — services, journeys, places…", ESC kbd (desktop only); footer trust line "SASI search — registries only, nothing is sent without your approval" + ↑↓/↵ kbd hints (desktop only).
- TEMP MOUNT + BROWSER VERIFIED (agent-browser, then fully REVERTED — sasi-app.tsx back to CommandPalette, zero traces): Ctrl+K opens/closes, Escape closes, overlay-click closes; empty query shows the 3 popular + Ask + Explore rows; "passp" → grouped rows with verified badges ONLY on verified entries (topics correctly unbadged); "clinic near me" → ACTIONS "Open Nearby map"; "is this sms fake" → "Verify this information" (report suppressed by engine); "zzqqx" → honest empty state + Ask row; ArrowDown/Up move aria-activedescendant opt-0↔opt-1; Enter on Ask row → ask-sasi view (pendingAsk flow), Enter on service row → ?view=service-detail&p=passport (h1 "Tourist passport"), click on org row → ?view=explore&p=org%3Asassa (SASSA org page renders — parallel 29-explore view works); mobile 390px: panel 390w, top 0, rounded-b 14px, no horizontal document overflow; desktop 1280: 576w centered at 120px top, rounded-xl; console clean (HMR info only), no CSP violations. NOTE: transient "Can't resolve './views/explore'" dev.log errors during my session were a PARALLEL agent's file being created, not mine — resolved by them, page 200. SAVED group not browser-testable here (not signed in → fetch honestly skipped, spec-conformant).
- Gates: ./node_modules/.bin/tsc --noEmit → 0 errors; bun run lint → 0 errors (fixed react-hooks/set-state-in-effect ×2 + preserve-manual-memoization ×2 found on first lint run: ask() inlined into memo + render-time reset pattern); dev.log tail clean after final compile.

Stage Summary:
- UniversalSearch (src/components/sasi/universal-search.tsx, single file, export function UniversalSearch) is ready for the coordinator to mount (replace <CommandPalette /> with <UniversalSearch /> in sasi-app.tsx). It is 100% engine-driven and honest: hits only from searchSASI registries + session saved items, verified badges only from officialSource.verified, no fabrication anywhere.
- Store APIs used (ALL exist, nothing new needed): commandOpen, setCommandOpen, navigate(view,param?), setPendingAsk, openService, authed. One coordinator note: GET /api/sasi/save returns {items:[...]} (not {saved:[...]} as the task brief stated) — component handles both shapes.
- Open follow-ups for the coordinator: (1) when wiring, remove the old window-level Escape/Cmd-K in command-palette.tsx or unmount it entirely to avoid double toggle (my component owns the same keys); (2) mobile sheet currently has no drag-handle — intentional (palette parity); (3) saved items are fetched once per dialog open by design (fresh data, one request).

---
Task ID: 29-explore-view
Agent: full-stack-developer
Task: Rewrite src/components/sasi/views/explore.tsx — the full Explore discovery view (main page + param-driven org/topic panels), single file, no other files touched.

Work Log:
- Read the Task-28 conventions (design language: zinc/black + light SA accent, sasi-card rows, mono badges, framer-motion rise-ins, honesty-over-completeness) and all contracts before writing: store (navigate/param/setCommandOpen/__sasiStore QA hook, explore NOT in store PUBLIC_VIEWS → signed-out deep links gate to login; sasi-app's own PUBLIC_VIEWS DOES include explore), explore-data.ts (ORGANISATIONS/CIVIC_TOPICS/INFO_INDEX), services-registry.ts (registryEntryBySlug, LOCATION_CATEGORY_LABELS), search.ts servicesByCategory() (11 categories sorted desc), primitives (EmptyState signature, GhostButton).
- REWROTE explore.tsx (~600 lines, default export, 'use client', root <main className="mx-auto max-w-6xl px-4 py-6 sm:px-6 lg:px-8">): param-driven at top level — param "org:<id>" → OrgPanel, "topic:<id>" → TopicPanel, else ExploreHome; deep-linkable via the existing /?view=explore&p=… URL sync.
- MAIN PAGE: (1) header — mono EXPLORE eyebrow, sasi-serif h1 "South African civic services, explained.", honest sub-copy ("SASI lists only what it can verify; where it cannot, it says so"), search button styled like the landing command bar (sasi-command-focus, h-12, Search icon + placeholder + ⌘K kbd hidden on mobile) → setCommandOpen(true); (2) "Browse by topic" — 8 real CIVIC_TOPICS as wrapping 44px chips, navigate("explore","topic:"+id) so panel state is URL-linkable, aria-pressed wired to the param, gold active tone via aria-pressed: variants; (3) FEATURE "Services" — servicesByCategory() as editorial category blocks (mono uppercase label + divide-y bordered list rows: title+department truncate/min-w-0, right side mono "CHECKLIST" chip with Route icon when journeyId, green BadgeCheck "Official source verified" when officialSource?.verified, ArrowRight); first 3 categories (Identity & documents, Services, Safety) fully; remaining 8 in a native <details>/<summary> "More services" (44px summary, chevron rotate on open, real remaining labels listed inside) — no fake counts anywhere; row click → service-detail with slug; (4) "Government organisations" — 6-entry directory rows: name+shortName, sphere badge (NATIONAL/MUNICIPAL mono 10px), stacked website-state line (verified → green BadgeCheck "Official website verified"; null → grey dot "No verified website on record"), row click → org panel; (5) "Public information" — all 8 INFO_INDEX entries as 2-col sasi-card grid → navigate(view); (6) links strip (sr-only h2) — Government directory card → gov, emergency card with pulsing red accent dot + "Emergency numbers → 10111 / 10177" → emergency.
- TOPIC PANEL: back row (GhostButton ←Explore → navigate("explore") clears param), Topic eyebrow + h2 name + description; "Services in this topic" rows via registryEntryBySlug with unknown-slug skip (type-predicated filter); infoViews resolved against INFO_INDEX (only real entries render); Nearby row when locationCategory non-null — label via LOCATION_CATEGORY_LABELS with raw-category fallback (covers "clinic" which the labels map lacks) → navigate("map"); unknown topic id → honest EmptyState (Compass icon) + back link.
- ORG PANEL: back row, Government-organisation eyebrow + h1 name + sphere badge + shortName; "Responsibilities" dot-bullet list; "Handles these services" registry-resolved rows (unknown slugs skipped, honest line when none resolve); official website — ONLY when non-null: verified line + external <a> (rel="noopener noreferrer nofollow" target=_blank, aria-label + sr-only new-tab hint) + standing disclaimer "SASI is independent and is not endorsed by this organisation."; null → verbatim grey note "SASI has no verified website on record for this organisation — confirm details through the department directly."; unknown org id → honest EmptyState.
- A11y/mobile: every section aria-labelledby with real h2/h3s; all interactive targets ≥44px (min-h-[44px] rows/chips/cards/summary/external link); truncate+min-w-0 on every row, chips flex-wrap; focus-visible inset rings on custom rows; shared ServiceRow/CategoryBlock/SphereBadge/InfoCard/PanelBackRow/PanelNotFound components keep both panels and the home page visually identical.
- QA: tsc --noEmit clean; bun run lint — 0 findings in explore.tsx (4 pre-existing errors live in universal-search.tsx, a parallel agent's file, untouched per scope); agent-browser (isolated --session to avoid a parallel agent's shared default session) verified at 1280×800 and 390×844: /?view=explore renders home (h1 + 5 sections + 8 chips + 17 registry rows + 6 org rows + 8 info cards), overflowX 0px at both sizes including details expanded + scrolled to bottom (a transient 28px reading right after viewport swap was reflow race, steady-state always 0), chip click → URL ?view=explore&p=topic%3Aidentity + Identity panel (4 service rows, gov info card, "Find Home Affairs offices near you"), ?view=explore&p=org:home-affairs renders DHA panel directly (verified dha.gov.za link with correct rel/target + disclaimer), p=org:your-municipality shows MUNICIPAL badge + honest null-website note + zero external links, p=org:not-real and p=topic:banana render honest EmptyStates with working back links, back button + browser Back both restore home with param null, Search button opens the command dialog (commandOpen true + role=dialog), console clean (no errors/warnings), dev.log tail all 200s with no compile errors (the "Can't resolve ./views/explore" entry in the log is stale history from before the file existed).

Stage Summary:
- Explore is live as the discovery layer: an editorial (not card-grid) main page connecting services → journeys/organisations/nearby, and linkable topic/org panels (?view=explore&p=topic:<id> / p=org:<id>) that compose the same row language as services/service-detail; zero fabricated data — every row, badge and note renders real registry/explore-data content, and unverifiable facts are stated as absent.
- Notes for next agents: store.ts PUBLIC_VIEWS lacks "explore" (sasi-app.tsx has it), so signed-out deep links to /?view=explore gate to login — deliberate store edit was out of scope; topic "clinic" locationCategory has no entry in LOCATION_CATEGORY_LABELS (falls back to the raw category string); parallel agent's universal-search.tsx currently fails 4 lint rules (not mine).

---
Task ID: 29 (main)
Agent: main
Task: Task 29 — OpenRouter AI provider + ownership hardening + universal search + Explore + production auth fix + splash removal + tests + release.

Work Log:
- SPLASH REMOVED: SplashScreen/boot constants/mounted gate deleted from sasi-app.tsx (app renders immediately; hydrate() runs in background); all sasi-boot-*/sasi-dot-veil CSS rules removed from globals.css (incl. data-saver + reduced-motion references). Lint+dev verified.
- OPENROUTER (§1-13): new src/lib/ai/{types,openrouter,index,prompts}.ts — AIProvider interface + AIProviderError codes (not_configured/rate_limited/timeout/upstream/malformed/invalid_request); OpenRouterProvider = the ONLY OpenRouter client (server-side key, model/temperature/maxTokens/timeout from env: OPENROUTER_MODEL, OPENROUTER_VISION_MODEL, OPENROUTER_TEMPERATURE, OPENROUTER_MAX_TOKENS, OPENROUTER_TIMEOUT_MS; sensible defaults; no secret logging, no raw provider bodies to users); getAIProvider()/getAIStatus()/aiHttpStatus() single import surface; centralized prompts (ask/briefing city+chat/document/evidence-vision/address-refinement). ALL 5 z-ai call sites swapped: ask (SSE contract unchanged, structured validation + corroboration kept), briefing (full hardened prompt text moved verbatim), vision (multimodal content parts; provider picks vision model), documents (text+image branches), geo/enrich (free-model chain retired → configured model, graceful OSM-only fallback kept). Not-configured → 503 with the honest "SASI AI is temporarily unavailable. The platform's AI provider is not configured." (§12) + code "ai_not_configured"-style code field; documents stores honest analysisError instead of faking. Sandbox verified: system-status now reports ai:{provider:"openrouter",configured:false,model:null} and ask/briefing honestly degrade.
- OWNERSHIP (§14-17): schema — userId String? + FK relation + indexes added to ChatMessage, Profile, EvidenceRecord, BriefingRecord, NotificationRecord; FK relations added to the existing CaseRecord/JourneyRun/SavedItem/Reminder/DocumentRecord userIds; User model carries all back-relations. db:push applied to Supabase Postgres non-destructively (existing rows stay userId=NULL per documented §16 migration decision — never bulk-assigned). Server-side enforcement: state route stamps userId on evidence/briefing/notification/profile writes + claimed-row 403 guards + reads/DELETE/notification-read ownership-scoped; ask stamps ChatMessage rows; mysasi reads OR [sessionId, userId] so claimed rows follow the account; account deletion transaction now detaches ALL 10 user-owned tables. Identity always from getAuthSession() — never from client payload.
- PROD AUTH FIX: root cause = production DATABASE unreachable from Vercel (system-status ok:false → login 401 because NextAuth must query User). Code: auth.ts derives NEXTAUTH_URL from VERCEL_PROJECT_PRODUCTION_URL/VERCEL_URL when unset (seeded into process.env for NextAuth v4); system-status now returns database.hint (DNS/timeout/credentials/schema classification, no connection data) + ai status. DEPLOY.md rewritten: POOLER DATABASE_URL flagged as THE common auth-killer fix, full env-var table incl. OPENROUTER_*; .env.example updated (§34).
- SEARCH (§18-22,29): src/lib/sasi/search.ts — deterministic client-safe engine over registry (17) + journeys + organisations + topics + info index + saved items; token/phrase scoring, threshold honesty (gibberish → 0 hits), intent routing (nearby/verify/report); servicesByCategory(). src/lib/sasi/explore-data.ts — ORGANISATIONS (6, verified-only websites: DHA/SASSA/SAPS/DoT/gov.za; municipality honestly null), CIVIC_TOPICS (8), INFO_INDEX (8 real in-app views). universal-search.tsx built by agent 29-search-ui (Ctrl/Cmd+K, grouped keyboard-navigable results, Ask-SASI row, intent actions, saved search when authed, honest empty state, desktop dialog + mobile sheet) — mounted in sasi-app; command-palette.tsx retired (git rm).
- EXPLORE (§23-28): views/explore.tsx built by agent 29-explore-view — editorial discovery (topics chips → linkable panels p=topic:id, service categories w/ CHECKLIST + verified badges, org directory → p=org:id panels with verified external links + independence disclaimer, info cards, emergency strip); registered in types/store/sasi-app/app-shell (nav.explore i18n en/zu/af) + PUBLIC_VIEWS + APP_ELIGIBLE_PUBLIC.
- TESTS (§39): bun test suite — openrouter provider (11: success/429/500/404/timeout/malformed/missing-key/model-env/no-secret-leak), search engine (12: intents/passport-ID grouping/gibberish-zero/registry-truth/saved), civic-ai validation+corroboration (8: fences/caps/strips/invented-slug-drop/real-kept), ownership API-level (11, RUN_DB_TESTS gated vs live server + real NextAuth login: B-cannot-read/rewrite/clear, 401s, account deletion detaches then row re-claimable). 31 unit + 11 ownership ALL PASS. CI: test step added.
- .env restored in sandbox (wiped again to sqlite default) with Supabase pooler + NextAuth secret + Supabase keys; OPENROUTER key intentionally absent (user provides in Vercel).

Stage Summary:
- One system: ASK (OpenRouter) → SEARCH (deterministic) → EXPLORE (knowledge architecture) → services/journeys → MY SASI, all rows explicitly owned.
- Deployment blockage identified for the user: Vercel DATABASE_URL must be the Supabase POOLER URL (IPv4); OPENROUTER_API_KEY + OPENROUTER_MODEL to be added by owner in Vercel.
- Pending in this task: production build + browser QA + atomic commits + push + final report.

---
Task ID: 29 (complete)
Agent: main
Task: Task 29 final — QA, atomic commits, GitHub push, verification.

Work Log:
- Production build: `next build` PASS (all routes compiled; Prisma client generated).
- Browser QA (agent-browser): landing renders instantly (no splash); Ctrl/Cmd+K opens Universal Search with grouped results + verified badges; "nearest clinic" -> Open Nearby map action; "is this message fake" -> Verify action; gibberish -> honest "Nothing matches" + Ask SASI row; Explore main page + org panel (org:home-affairs) render with verified external links; Ask SASI degrades honestly with 503 "SASI AI is temporarily unavailable. The platform's AI provider is not configured." (code: not_configured); golden-path login works against Postgres; sidebar shows Explore; 0px horizontal overflow at 390x844 and 1280x800; console clean (one benign Next dev-mode scroll-behavior hint).
- Secret scan pre-push (§43): no real keys/passwords/tokens in any tracked file (only placeholder templates in .env.example/DEPLOY.md); .env gitignored.
- Atomic commits + push to origin/main (verified via git ls-remote: remote == local HEAD):
  - 4292117 feat: integrate OpenRouter AI provider
  - d2eb7fb security: add explicit user ownership enforcement
  - c30010d feat: add universal SASI search and civic Explore
  - c4e0b59 test: harden ownership and AI flows
  - 53bc9bf chore: finalize production configuration
- Final gates at push time: tsc PASS, eslint clean, bun test 31/31 unit + 11/11 ownership (test:db).
- Note: GitHub Actions run list not readable from this sandbox (API responses truncated anonymously); CI triggers on push to main and must be checked at github.com/AndersonAzariah/sasi/actions.

Stage Summary:
- Task 29 delivered: real OpenRouter provider abstraction (server-only, honest degradation), explicit row ownership with FK-backed userId on all ten user-owned tables + server-derived identity, universal deterministic search with intent routing, civic Explore with verified-only organisation data, boot splash removed, production build green, repo clean and pushed.
- For the OWNER to do in Vercel (names only, no secrets here): DATABASE_URL (Supabase POOLER URL — IPv4 reachable; the direct db.…supabase.co host is why sign-in 401s), NEXTAUTH_SECRET, OPENROUTER_API_KEY, OPENROUTER_MODEL (optional), NEXTAUTH_URL (optional; auto-derived), Supabase keys as documented in DEPLOY.md.
